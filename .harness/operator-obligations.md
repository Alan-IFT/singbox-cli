# Operator obligations

Steps a **human** must perform on a host this project's agents cannot reach. Ids are permanent:
append with the next unused id, never renumber, never delete a discharged row (mark it).

| id | obligation | why an agent cannot do it | opened by |
|---|---|---|---|
| 1 | After installing a build that carries T-20, run `sc doctor` as root on a real install and confirm: the report prints all nine sections, exits with the mapped status (0 / 1 / 2), and the service witness (`systemctl is-active sing-box`, the TUN device, the Clash API port) is unchanged before and after. | AC-B14's subject is the *shipped invocation* — the installed `/usr/local/bin/sc` running as root against the live service. Installing a candidate over `/usr/local/bin/sc` is forbidden by `02_SOLUTION_DESIGN.md` K-18, and no agent in this pool holds an interactive root credential. Stage 6 reported it **BLOCKED** rather than substituting a weaker artifact check (the R-31 / R-41 / R-47 precedent). | T-20 `doctor-extended-checks`, stage 6 |
| 2 | Discharge T-07's `[VM]` criteria AC-6 … AC-13 and AC-20's VM half. **Recipe.** (R-1) Boot a throwaway systemd VM (Debian/Ubuntu) that will be destroyed afterwards; `apt-get install -y curl python3 git`; install the `sing-box` binary; `ls /dev/net/tun \|\| { mkdir -p /dev/net && mknod /dev/net/tun c 10 200; }`; confirm `/etc/sing-box/nodes.json`, `/etc/sing-box/config.json` and `/etc/sing-box/rules/*.srs` are all absent and `SB_RULES_BASE` is unset. (R-2) `git clone <repo> /root/singbox-cli` at the delivered commit. (R-3) `sudo bash /root/singbox-cli/.harness/scripts/restricted-network-regression.sh --i-will-destroy-this-vm \| tee /root/t07-run1.txt`; keep the printed `work dir (kept): /tmp/tmp.XXXX` — it holds `install.capture` and `update-rules.capture`. (R-4) Paste the six `E1…E6` lines, the run's exit status, and both captures into the T-07 delivery record; they discharge AC-6 (E1) / AC-7 (E2) / AC-8 (E3) / AC-9 (E4) / AC-10 (E5) / AC-11 (E6) / AC-12 (the six `pair=` fields) / AC-20's VM half (exactly six lines, exit 0 iff all six PASS). (R-5) On a **second** throwaway VM prepared as in R-1 but with `/etc/sing-box/rules/` pre-populated with any file named `*.srs`, repeat R-3: the expected result is six `UNMET` lines naming the populated directory and **no** installer invocation — that discharges AC-13. (R-6) Destroy both VMs; never re-run on a used host. **Read the transcript against T-07 `05_CODE_REVIEW.md` RES-4**: (a) does E5 report `PASS` while `systemctl show -p NRestarts sing-box` is non-zero (the >2 s crash-loop the 1 s sampler cannot see); (b) did any `E3`/`E4` line read `BLOCKED` where its own blackout observation was already falsified (CR-13); (c) did any `obs=` carry `unknown` / `absent` / `?` and, if so, was the resulting verdict `FAIL` the right vocabulary (CR-4). | The scenario needs root on a disposable systemd VM with `/dev/net/tun`, and it runs `install.sh`, writes `/etc/hosts`, `/etc/sing-box`, `/var/log/sing-box` and systemd units. This pipeline's host runs the owner's live VPN and the artifact refuses on it at K-3 gate 2 by design (`nodes.json` present, exit 3). No container or VM runtime is usable here — `docker` needs sudo, `podman`/`systemd-nspawn`/`qemu-system-x86_64`/`vagrant` are absent, LXD is uninitialised, `bwrap --unshare-net` EPERMs on loopback setup — and installing one is out of scope (`01_REQUIREMENT_ANALYSIS.md` out-of-scope 4). No agent in this pool holds an interactive sudo credential. Stage 6 reported all eight criteria **BLOCKED** rather than substituting artifact inspection, which `Q-15`/`AC-19` make a defect (the R-31 / R-41 / R-47 / R-52 precedent). | T-07 `restricted-network-regression-test`, stage 6 |
