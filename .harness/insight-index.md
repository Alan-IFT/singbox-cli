# Insight Index — singbox-cli

> Cross-task truths the project has learned the hard way. ≤30 lines.
> Read at the start of design/implementation tasks; write only after evidence-backed surprises.
> See `.harness/rules/05-insight-index.md` for the contract.

<!-- Append new insights below, one per line. Format:
-->
- 2026-08-15 · `docs/dev-map.md`'s mandated fixture-loader recipe cannot load `bin/sc` at all under the very environment every locale criterion needs — its bare `open("bin/sc").read()` decodes with the **locale** codec while CPython reads a script as UTF-8 (PEP 263), so `LC_ALL=C PYTHONUTF8=0` gives `UnicodeDecodeError: 'ascii' codec can't decode byte 0xe2 in position 29`; `encoding="utf-8"` is required and its absence reads like a harness bug rather than a recipe defect · evidence: output-layer-contract
- 2026-08-15 · sing-box's Clash API exposes DNS-cache control only as a **mutating** fake-IP flush (`clashapi.cacheRouter` → `flushFakeip`) and as **configuration** options (`disable_cache`, `independent_cache`, `cache_capacity`), never as a read-only request parameter — and the `/dns/query` JSON body carries no cache-hit indicator either, so no `sc` probe can ever ask this install for an uncached resolution *or* detect that it got one; a row wanting that fact must narrow its claim rather than chase a measurement · evidence: doctor-rows-establish-their-fact
- 2026-08-15 · Removing a `[PROBLEM]` row from `sc doctor` can **raise** the exit code: `DOCTOR_EXIT` maps UNKNOWN→2 while the severity ordering feeding `worst = max(...)` is OK<UNKNOWN<PROBLEM, so a PROBLEM row **masks** any UNKNOWN row in the exit status — measured as HEAD `EXIT=1` → candidate `EXIT=2` on a wholly healthy init-less host, where `_doctor_service()` returns two unconditional UNKNOWN rows (`bin/sc:2741-2742`); the exit code is a class label, not a severity scale, and "this fix can only improve the exit code" is false · evidence: doctor-rows-establish-their-fact
- 2026-08-15 · A `bin/sc` fixture **cannot call `main()` twice in one process**: `main()`'s `io.TextIOWrapper` re-wrap leaves the previous run's wrapper over the same `BufferedWriter`, and replacing it closes that buffer, so every later `print()` raises `ValueError: I/O operation on closed file` — with stderr discarded the fixture then prints *nothing* and reads as a probe that produced no rows rather than a harness that broke; one case per process is the only reliable shape · evidence: doctor-rows-establish-their-fact
- 2026-08-15 · `telemetry: block` is the **absent-key default**, so `_telemetry_overlay()` is a second `sc`-authored writer of `dns.rules` on an *ordinary* host, not a hypothetical future one — its `$before {"clash_mode":"Global"}` anchor resolves by search and lands its rule at index **2** (behind the `hosts_dns` rule), which is what leaves the AAAA rule's index 0 intact; any future check of a rule's emitted position must be composed against this second writer, not against a bare base template · evidence: doctor-rows-establish-their-fact
- 2026-08-16 · The file `/harness-upgrade` copies over a plugin-vendored script is **not** a newer copy of that script: `upgrade-project.sh:56` resolves the source to the plugin's `skills/harness-init/templates/common/…`, which for `archive-task.sh` is a **425-line rewrite** of this project's 151-line file, so "revert my two hunks" and `git checkout -- <path>` are both wrong models — the refresh replaces `refresh_set`'s seven script pairs wholesale with no backup and no marker preservation (`:186-194`, `:195-227`), while `verify_all.{sh,ps1}` alone is excluded and gets a splice, a HALT and a timestamped `.bak` (`:136-141`, `:535-573`) · evidence: harness-self-maintenance
- 2026-08-16 · `archive-task.sh`'s `## Insight` harvest matches the heading exactly (`^##\s+Insights?\s*$`, `:58`), so a delivery writing `## Insight to surface` — the heading `04_DEVELOPMENT.md` itself uses — harvests **zero entries, exits 0 and prints nothing**, which is indistinguishable from a task that surfaced no insight and silently loses the one artifact of a task that outlives it · evidence: harness-self-maintenance
- 2026-08-16 · `archive-task.sh`'s index rewrite reconstructs the file as `echo "$header"` + remaining + harvested where `header=$(grep -vE '^\s*-\s')` (`:114-123`), so it **hoists every non-bullet line to the top and loses trailing ones to command substitution**: an index whose last line is a non-bullet line comes back one line shorter with a mid-file marker moved (measured 33 → 29, marker line 19 → 9) — and because that lands *under* the cap, **F.4 passes over the loss**, while the two shapes that land over it (unterminated final line; zero non-bullet lines) merely WARN · evidence: harness-self-maintenance
- 2026-08-16 · Bash's `errexit` exempts a failing command inside an `&&` list unless it is the command **after the final `&&`**, so `[[ cond ]] && cmd` as an ordinary statement under `set -e` does **not** abort when the test fails — measured exit 0 on `archive-task.sh:82`'s HEAD form, refuting a claim two of this task's own upstream documents had asserted and that a reviewer, a developer and a gate had each carried forward before it was run · evidence: harness-self-maintenance
- 2026-08-16 · A `sys.modules` `os`-shim that neutralises `bin/sc`'s import-time auto-elevate by **name prefix** (`exec`/`spawn`/`fork`) leaves three POSIX process-start names live — `os.popen`, `os.posix_spawn`, `os.posix_spawnp` — and this is not theoretical: a `bin/sc` copy whose elevate guard called `os.popen` **started a shell** (probe artifact present on disk) while the harness reported `14 defined, 14 run, 14 passed`, **exit 0**, so a fully green test run can carry a process started from `bin/sc`'s import, and only an enumeration of every process-start name in `dir(os)` — not a prefix — closes it · evidence: committed-test-suite
- 2026-08-16 · `verify_all` A.1 cannot see the directory that holds every committed test artifact: its pathspec excludes `.harness/*` wholesale (a git pathspec `*` crosses `/`), and its regex is case-**sensitive**, so `restricted-network-regression.sh:43`'s `TOKEN='--i-will-destroy-this-vm'` matches only under `-i` — measured `case-sensitive: 0 / case-insensitive: 1` — which means any rule that relies on A.1 catching a fixture credential certifies nothing, and the constraint must be enforced by the artifact's own criterion instead · evidence: committed-test-suite
- 2026-08-16 · Deleting the `encoding="utf-8"` argument from `_write_private()`'s `fdopen` is **invisible** to an assertion that non-ASCII text reaches disk as UTF-8 on this host (`PASS … 5 text characters -> 11 UTF-8 bytes`), because the locale codec is already UTF-8; only a **codec substitution** (`latin-1`, which raises `UnicodeEncodeError` on `节点 ✓`) kills it — so a mutation sweep that deletes the argument rather than substituting the codec reports a **false kill** and certifies the opposite of what it claims · evidence: committed-test-suite
- 2026-08-16 · CPython's `backslashreplace` has **three** spellings and only one of them is legal JSON — `\xNN` (Latin-1 range) and `\UNNNNNNNN` (above the BMP) are not JSON escapes but `\uNNNN` (the rest of the BMP, i.e. **every CJK character**) is, so `sc config > file` under a non-UTF-8 stdout yields a file that **still parses** for a Chinese-tagged document and fails to parse for a `café` or emoji one; a two-spelling enumeration reads as complete and is wrong for the most common tag this project sees · evidence: state-file-contract-completion
- 2026-08-16 · `json.dumps()`'s `ensure_ascii=True` **default** puts pure ASCII on disk, so a locale fixture written as `json.dumps(doc).encode("utf-8")` contains no byte the codec under test can fail on and **passes identically on broken and fixed code** — `ensure_ascii=False` is what makes the control fail with `'ascii' codec can't decode byte 0xe8`, and this is a second, independent way for a locale criterion to certify nothing even after `PYTHONUTF8=0` is set correctly · evidence: state-file-contract-completion
- 2026-08-16 · A non-ASCII tag passed to a `bin/sc` fixture through `argv` under `LC_ALL=C` is decoded with `surrogateescape`, so the run dies inside the **fixture's own writer** (`UnicodeEncodeError: … surrogates not allowed`) before `sc` is reached — the failure looks like a product defect but is the harness's, and the only reliable transport is building the tag from source escapes and asserting it surrogate-free · evidence: state-file-contract-completion
- 2026-08-16 · `main()`'s read-only enumeration at `bin/sc:3769` gates `_init_files()` and `_resolve_clash_port()` **only** — `_load_lang()` runs on *both* arms — so the once-per-run `⚠️ Cannot use settings.json …` line is written by every command, `doctor` and `config` included; three documents in a row read that `if/else` as gating the announcement, because the two facts sit on adjacent lines and only one of them is enumerated · evidence: state-file-contract-completion
- 2026-08-16 · `generate_config()` raises `OverrideError` from **four** sites carrying four different `.path` values (override, settings, node store, and a composition fault carrying `None`), so a bare `except OverrideError:` at either call site is a **four-document catch** wearing the shape of a one-document one — it silently destroys the provenance the envelope exists to preserve, and the resulting build ships green because no criterion written for the document you just added measures the other three · evidence: state-file-contract-completion
- 2026-08-16 · `sing-box check -c` reads the file it is handed regardless of extension, **interpolates that path verbatim into its message**, and **colours its output even into a pipe** (`^[[31mFATAL^[[0m`) — so a check-before-write design may use any temp name, but must substitute the path back out of the message before a user sees it, and only removing the **complete** CSI sequence yields a pasteable line; measured against 1.13.15 with byte-identical documents at a `*.json` and a `config.json.check.*` name, and **no stub checker can exhibit any of the three** · evidence: validate-before-baseline
- 2026-08-16 · Replacing `config.json` with `os.replace(candidate, CFG_PATH)` instead of a second `_write_private()` call shows **0 observable differences across 13 cases** — a 9-case behavioural sweep plus a symlinked target, a `0666` target, `umask 000` and the real checker — while the committed suite reports `18 defined, 18 run, 18 passed`, so **no byte- or mode-comparing assertion can pin "the one writer installs the document"**; only a structural check (an `ast` clause or a grep) can, and until one exists that property is held by a decision record and nothing else · evidence: validate-before-baseline
- 2026-08-16 · An exception raised in a `finally` clause propagates out of the **whole** `try` statement, so a `finally`'s own body is not protected by the `try` it belongs to — measured as `os.unlink(None)` raising `TypeError` **past** both an inner `except OSError` and an outer `except (OSError, ValueError)` and replacing the pending return — which makes "everything is inside one guarded region" false for exactly the block most likely to be edited later, and is the member three successive enumerations of `generate_config()`'s unguarded statements all omitted · evidence: validate-before-baseline
- 2026-08-16 · `verify_all` B.4 compares the suite's passing count against `baseline.json`'s `test_count` and **never against that floor's own history**, so lowering `test_count` from 18 to 17 with 18 assertions present leaves B.4 **PASS** — the floor is a ratchet only by convention, and every criterion of the form "the floor does not fall" has had no control at all · evidence: validate-before-baseline
- 2026-08-16 · Absorbing a rejection arm into the sibling `try` whose `except OSError` handles the same call reads as an equivalent tidy-up and is not: a rejection whose own `sys.stderr.write` raises `OSError` is then caught, re-reported as a *different* verdict, and falls through to the install — measured as a build that returns `True`, installs the rejected document, re-baselines the drift digest and tells the user it *was installed without being checked*, with the committed suite fully green · evidence: validate-before-baseline
