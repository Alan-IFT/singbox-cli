# Delivery Summary

## Summary

- Task: **T-32 `record-accuracy-sweep`** — correct the eleven filed sentences that claim something the code does not do, and add nothing else.
- Mode: **full** (7 stages). Invoked by `/harness-batch`, pool `closeout`; **final row of the pool and of a three-pool, 20-task programme.**
- Stages traversed: 1 (req) → 2 (design) → 3 (gate, **BLOCKED ON REQUIREMENT**) → **1′** (req rework) → **3′** (gate, APPROVED WITH CONDITIONS) → 4 (dev) → 5 (review, APPROVED) → **4′** (dev rework) → **5′** (review, APPROVED) → 6 (QA, APPROVED FOR DELIVERY) → 7 (delivery, 2026-08-17).
- Rollbacks: **2.** (1) Gate **F-1 BLOCKER** — AC-3's fallback authorised running a historical `bin/sc`, which re-execs `sudo /usr/local/bin/sc` at import to a hard-coded absolute path, so "a scratch tree" gave no containment; the gate overturned stage 2's design-level mitigation and required the *criterion text* be amended, citing this project's own R-110(a). (2) **PM override on CR-1/CR-2** — the reviewer rated both MINOR and APPROVED; I spent the round anyway because CR-1 shipped a false enumeration inside `.harness/rejected-decisions.md`, the permanent record that declines a mechanism for catching false enumerations, and CR-2 would have made a literal-minded QA fail AC-14 against a compliant tree.
- Final verify_all result: **PASS** — `PASS 20 / WARN 0 / FAIL 0 / SKIP 1, exit 0`, B.3 the single SKIP, B.4/B.5/B.6 all PASS. Measured by the PM at task start, after stage 4, after stage 4′, and at delivery; **identical every time**, and re-measured rather than inherited at each checkpoint.
- Baseline changes: **none.** `baseline.json`'s `test_count` stays **19**; no assertion added, removed or weakened; B.6's ratchet unmoved. Two of my own dispatch figures were **stale and were corrected by downstream stages**: `test_count` was **19**, not the 18 I carried, and the insight-index entry citing `bin/sc:3769` had drifted to `:3837`.
- Outstanding risks: none blocking. Six rows filed (**R-112 … R-117**). Four criteria were reported **NOT-DISCRIMINATING rather than passed**, including **AC-19 itself** — no committed assertion quotes any corrected sentence, and `verify_all.sh` reads none of the five documents, so the entire mechanical suite returns a byte-identical verdict on a mutant whose AAAA advice is inverted and false of the code. When the artifact *is* the deliverable, the machine goes blind; the controls that decided truth here were first-hand re-derivation at three independent stages.
- Files changed: **8 product/record files, 166 insertions / 59 deletions.** `bin/sc` 16, `docs/tasks.md` 76, `docs/tasks-archive.md` 59, `.harness/rejected-decisions.md` 30, `.harness/rules/80-delivery-policy.md` 12, `docs/dev-map.md` 8, `.harness/rules/50-singbox-cli.md` 4, `CHANGELOG.md` 2. **26 changed lines outside rule 80's process paths against NFR-2's 30.** `docs/batches/closeout/*` are PM/batch-owned and left unstaged.
- Zero executable lines: `bin/sc` parsed at HEAD and at delivery with every `str` constant normalised gives **identical ASTs** (15550 nodes, 113 top-level defs, same order); exactly **three** string constants differ; `py_compile` exit 0; `sha256 0afdc3b6…f669`. QA audited the normalisation itself and proved it folds **nothing but `str`** (`int`/`bytes`/`bool`/`NoneType` counts identical on both sides, and mutants M-03/M-04/M-05 kill it).
- Next steps for user: **operator obligations 1-6 in `.harness/operator-obligations.md` remain open** and none was added here — this task ended **0 BLOCKED**. R-30 still stands: a `bin/sc` change reaches the running host only when a human installs it.

### Dispositions — all eleven, as the brief required

**Closed here (7):** R-63, R-79, R-82, R-83, R-85, R-91, R-94.
**Already closed by a later task, edited nowhere (3):** **R-77, R-78, R-84** — all three discharged by **T-28** in the `docs/dev-map.md` loader recipe. Re-verified against the current text at stages 1, 3 and 5 rather than inherited from `docs/tasks-archive.md`'s claim that they were closed.
**Left open, deliberately (1):** **R-74** — amended in place, not closed. It has no closure predicate, the only mechanism that would give it one is declined, and closing it would swap eleven corrected instances for a claim about future sentences: R-74's own defect. The amendment states that what has no owner is the **truth** of a sentence about code, **not** that nothing constrains a shipped sentence — B.4's sentence assertions pin the English keys they name and B.2 pins `install.sh` key parity (gate G-12).

**Two of the eleven rows' own filed repairs were false of the code and were refuted rather than shipped** — the inverted-R-22 trap, caught: R-83's "four directives reach the AAAA row" is **three**, and its "`sc reload` is ineffective only for `$replace`" is wrong for **every** override-caused displacement; R-85's filed 「没有哪台机器的退出码会变小」 is false because `DOCTOR_EXIT` maps OK/UNKNOWN/PROBLEM to 0/2/1 — a label set, not a scale.

### Obligations discharged

- **`archive-task.sh` rotation:** reported below, a sixth independent confirmation after T-27's fix and T-28/T-29/T-30/T-31.
- **Not fixed, as instructed:** R-89/R-90/R-92 (`archive-task.sh:109-136`, blocked on the owner's R-87 decision) and R-86 (`guard-rm.sh`, T-27's scope ruling stands). **R-86 fired a sixteenth time**, on the developer; bypass never set, worked around with scratch-file scripts, and `git commit -F` used for the commit.
- **`R-88` unchanged:** `.harness/scripts/entropy-cadence` and `task-state.js` do not exist on this host. Both handled fail-open. **The delivery-time entropy watch resolved NOT-DUE**, so no scan ran and no `## Entropy watch` section appears — the standing consequence R-88 records.
- **Safety:** live host untouched. `/etc/sing-box` and `/var/lib/sing-box` never written; `is-active` never invoked; no `sc` — historical, current or installed — executed as a program at any stage.

## Insight

- 2026-08-17 · A host witness carried as a cross-task invariant decays on a schedule the project itself ships: `MainPID=2566751` was asserted in all 20 dispatches of a three-pool programme while `sing-box-rules-update.timer` restarts the service weekly, and it went false mid-task (`LastTriggerUSec` 00:44:43 → `/usr/local/bin/sc update-rules` → the unit's own `ActiveEnterTimestamp` 00:44:47, all four `.srs` at mtime 00:44, `NRestarts` still 0, no reboot, `/etc/sing-box` mtime unmoved) — so the only stable witness that a task disturbed nothing is the *(before, after)* pair taken inside one run, never a PID quoted from a prior task · evidence: record-accuracy-sweep
- 2026-08-17 · When a task's deliverable IS prose, every committed check goes blind at once: a mutant inverting the shipped AAAA sentence's advice — false of the code, at all three sites, placeholders preserved — produces a byte-identical verdict from B.4, `py_compile`, the folded-AST identity, the placeholder-equality assertion and a full `verify_all` run, because no assertion quotes any shipped sentence and `verify_all.sh` reads none of the five documents; such a task must therefore declare which of its criteria are regression controls and which decide truth, or it will report a green suite as evidence for a claim the suite cannot reach · evidence: record-accuracy-sweep
- 2026-08-17 · A one-directional subset assertion is read as an equality assertion by everyone who quotes it: `check-sc-contracts.py`'s `zh_placeholders_are_a_subset_of_their_key` computes `got ⊆ want` and reports offenders only from `got`, so a `zh` entry that DROPS a placeholder passes the run — and three documents in one pipeline cited it as establishing "the same placeholder set" until a gate ruling forced the pair to be read in both directions · evidence: record-accuracy-sweep
- 2026-08-17 · A "this is what the design costs" clause should be tested for reachability before it is written, not after: `docs/dev-map.md`'s `backslashreplace` clause recorded a lost `surrogateescape` round-trip at two named sites where every build that shipped without the codec died earlier in the same run at an `sc`-authored non-ASCII character — verified by `git show` over all 21 commits that ever touched `bin/sc`, so the loss was never observable on any shipped build and the honest form of the clause is prospective · evidence: record-accuracy-sweep
- 2026-08-17 · A citation into a file that the very event it describes re-lands is a self-falsifying record, and the repair is to delete the coordinate rather than re-take it: rule 80's durability section cited four ranges into `upgrade-project.sh` whose sub-line boundaries three documents disagreed about by a line or two, and replacing all four with greppable tokens (`refresh_set`, `known`, `VERIFY-SPLICE`, `VERIFY-HALT`, `"$proj_file.bak-$stamp"`) dissolved every dispute at once while making a future drift fail loudly instead of silently · evidence: record-accuracy-sweep

## Verdict

DELIVERED
