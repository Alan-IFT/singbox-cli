> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | PASS | Every criterion names a property of a delivered artifact that a reader can decide against code: AC-3 now settles by retrieval-and-reading with a BLOCKED-and-file terminal, AC-9 binds the lead's transition set to equality with the derived set in both directions over a population fixed by `CHANGELOG.md:29`'s three rows, and the three remaining requirement-side imprecisions are settled by gate rulings rather than left open (G-7, G-9, G-12). |
| 2 | Design completeness | WARN | Every FR still carries a ledger row, an interface and a verification step and I-6's deletion of both copied counts remains the correct smaller repair, but `02_SOLUTION_DESIGN.md` was not re-emitted, so I-2's condition clause still misses a reachable branch (F-5), K-3's mitigation is still not executable as written (F-4), and V-8 still carries the population and the comparison direction the amended AC-9 has outgrown (F-3, F-13). |
| 3 | Reuse correctness | PASS | Every code claim the two documents rest on resolves, and the one this round turned on was re-read first-hand: `bin/sc:124-126` is the import-time `os.execvp("sudo", ["sudo", "/usr/local/bin/sc"] + sys.argv[1:])`, alongside `_apply_directive`'s insert positions (`:1436-1454`), the override-merged-last composition order (`:2103-2113`), `stored_delays()`'s two-clause guard (`:2295-2296`, `:2303`), `DOCTOR_EXIT` + `max` (`:2546-2550`, `:3089-3105`), the single-element `$prepend` payload (`:1782`) and the three attrition clauses (`docs/dev-map.md:211`, `:234-240`). |
| 4 | Risk coverage | WARN | The one safety risk is now closed at its source rather than routed around it — AC-3 no longer authorises the act, and its own statement of the re-exec is true of `bin/sc:124-126` — and the newly surfaced BLOCKED-coverage risk is ruled acceptable and bounded by G-16, leaving one gap: the residual list still omits the artifact carrying R-91's hazard class outside the eleven (F-10, bound by G-14). |
| 5 | Migration safety | PASS | No data or API change, `config.json` stays byte-identical because no composition input changes, every ledger row has a per-file `git checkout --` rollback, and M-1 correctly makes the `docs/tasks.md` rotation a precondition of every addition rather than a cleanup after it — with the PM's measured `wc -l` = 299 giving that rotation one line of margin. |
| 6 | Boundary handling | WARN | Null/absent handling is designed for the override, the drift record and the state documents, and the new terminal boundary (a site whose source order cannot be settled) is now designed rather than escalated, but the rewritten AAAA sentence's condition clause still does not cover `config.json` carrying no `dns.rules` array at all (F-5, bound by G-6) and the zh-parity boundary is still one-directional in the run that certifies it (F-7, bound by G-9). |
| 7 | Test feasibility | WARN | Both amended criteria are now discharged without touching a program — AC-3 by `git show` plus a trace in the retrieved text, AC-9 by a two-directional set comparison that an incomplete lead fails — and the residue is bound rather than open: AC-7's `[B]` class still overstates what B.4 establishes (F-7/G-9) and V-8's derivation is still scoped to two of three changed probes (F-3/G-3). |
| 8 | Out-of-scope clarity | PASS | Both documents enumerate the exclusions with their rulings, out-of-scope item 4 now records the PM's landed Q-8 ruling in the same terms `PM_LOG.md` states it (scope not widened, rows re-homed at delivery), the frozen set names the recipe block, `dev-map.md:76`, `bin/sc`'s "as the eighth" comment and the archived quotations, and K-13 closes the one path by which a sweep would leak into `docs/features/_archived/**`. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-1 | BLOCKER | `01_REQUIREMENT_ANALYSIS.md` §Acceptance criteria, AC-3 | **CLOSED round 2** — the amended AC-3 carries no clause under which any stage executes a historical, current or installed `sc` as a program (its only acts are `git show` and a trace *in the retrieved text*), states the import-time hard-coded-path re-exec truthfully against `bin/sc:124-126`, refuses the act under NFR-4, and terminates at **BLOCKED and a row is filed** per BC-8 where source order cannot settle a site. |
| F-2 | MAJOR | `01_REQUIREMENT_ANALYSIS.md` §Acceptance criteria, AC-9 (against FR-7) | **CLOSED round 2** — AC-9 now requires the delivered lead's transition set to be *equal* to the derived set ("no derivable transition is absent from it", compared "in both directions", "a lead stating a strict subset of the derived set FAILs"), which inverts the comparison rather than adding an adjective, and binds the derivation's population to every doctor row the T-26 changelog entry names as changed. |
| F-3 | MAJOR | `02_SOLUTION_DESIGN.md` §Verification plan, V-8 (and I-11(b)) | **OPEN, bound by G-3** — V-8 still scopes the derivation to "the two changed probes" while `CHANGELOG.md:29` names **three** rows it changed (IPv6 (AAAA), 节点延迟, DNS 解析), so the derivation's own population is smaller than its subject's; `02` was not re-emitted and the amended AC-9 now outranks V-8 on population. |
| F-4 | MAJOR | `02_SOLUTION_DESIGN.md` §Constraints, K-3 | **OPEN, bound by G-5** — the "grep that the new English string occurs exactly twice" check is not executable as written: the `TRANSLATIONS` key is one physical line (`bin/sc:312`) while the call-site literal is implicitly concatenated across `bin/sc:2800-2802`, so a whole-sentence grep returns one occurrence and a substring grep proves neither identity nor exactly-two. |
| F-5 | MAJOR | `02_SOLUTION_DESIGN.md` §Interfaces, I-2 | **OPEN, bound by G-6** — I-2's condition clause ("the first `dns.rules` entry on disk is not the rule this build emits") is untrue of a reachable branch that returns the same PROBLEM row: a `config.json` whose `dns` is absent or not a dict, where `rules` is `None` and there is no first entry at all (`bin/sc:2793-2802`). |
| F-6 | MINOR | `01_REQUIREMENT_ANALYSIS.md` §Acceptance criteria, AC-5 | **SETTLED by gate ruling G-7 and verified not re-opened this round** — AC-5's predicate ("the set of directives it names is exactly the set that can change `dns.rules`' first element") is not literally satisfied by a sentence that names no directive, which is what FR-6's permissive wording and the design's K-6 both require. |
| F-7 | MINOR | `01_REQUIREMENT_ANALYSIS.md` §Acceptance criteria, AC-7 | **SETTLED by gate ruling G-9 and verified not re-opened this round** — AC-7 is class `[B]` on B.4, but `zh_placeholders_are_a_subset_of_their_key` (`check-sc-contracts.py:455-475`) tests one direction only, so a `zh` entry that **drops** `{override}` passes the run while failing AC-7's own "same placeholder set". |
| F-8 | MINOR | `02_SOLUTION_DESIGN.md` §Change ledger, E-8 and E-9 | **OPEN, bound by G-10** — the per-file ceilings (≤4 and ≤2) are asserted rather than measured against the paragraphs' physical layout: rule 80's four ranges sit on `80-delivery-policy.md:70-73` of a reflowing paragraph that I-8 gives six named tokens, and rule 50's two sites are `50-singbox-cli.md:29-30` plus `:47`. |
| F-9 | MINOR | `02_SOLUTION_DESIGN.md` §Verification plan, V-17 | **CLOSED by measurement** — the PM's task-start run reports `[F.4] insight-index.md <=30 lines … PASS` at `wc -l` = 30 and `PASS 20 / WARN 0 / FAIL 0 / SKIP 1, exit 0`, so V-17's expected observable was correct and my 31-count was the rendered-line artifact my own caveat named; the baseline is now measured rather than inherited and G-11 stands for stage 4's own post-change run. |
| F-10 | MINOR | `02_SOLUTION_DESIGN.md` §Residuals travelling | **OPEN and travelling to the PM as a filed-row candidate per G-14, not repaired here** — the residual list omits `.harness/insight-index.md:10`, which carries four line ranges into `upgrade-project.sh` (`:186-194`, `:195-227`, `:136-141`, `:535-573`) — R-91's own hazard class, in the file every task start reads. |
| F-11 | MINOR | `01_REQUIREMENT_ANALYSIS.md` §FR-10 / AC-17 | **SETTLED by gate ruling G-12 and verified not re-opened this round** — "No code owner exists for it" is true of the *truth* of a shipped sentence and false of its *spelling* (B.4's sentence assertions pin the English key they name, `docs/dev-map.md:244-246`; B.2 pins `install.sh` key parity), so an amendment generalising to "nothing constrains a shipped sentence" would over-claim in the act of recording R-74. |
| F-12 | NOTE | `02_SOLUTION_DESIGN.md` §Change ledger, E-5/E-6/E-9/E-10 | **OPEN, unchanged** — R-94's sub-letters in this design do not match the PM dispatch's sub-letters for the same five sites, so a letter cited across documents identifies nothing; the site list is the only stable identifier. |
| F-13 | NOTE | `02_SOLUTION_DESIGN.md` §Verification plan, V-8 | **NEW this round** — V-8's stated observable ("every transition in the delivered lead appears in it") is exactly the one-directional form the amended AC-9 replaces, so a developer following `02` alone would build the check F-2 named; AC-9 governs as the contract and G-2/G-3 bind stage 4, so `02` still needs no re-emission. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| G-1 | AC-3 carries no clause under which any stage executes a historical, current or installed `sc` as a program; where source order cannot settle a site, the criterion's outcome is BLOCKED-and-file per BC-8. | stage 1 | **DISCHARGED round 2** — the amended AC-3 read against `bin/sc:124-126`; F-1 closed. |
| G-2 | The delivered R-85 lead's transition set is stated as **equal** to the derived set, and `04_DEVELOPMENT.md` states that equality rather than only that each stated transition is derivable. | stage 4 | the requirement half is discharged by the amended AC-9; the stage-4 half by I-11(b)'s table read against the delivered lead at stage 5 |
| G-3 | The I-11(b) derivation covers every doctor probe T-26 changed — `CHANGELOG.md:29` names three rows — or states, per unchanged probe, why no class it can return moved. | stage 4 | `04_DEVELOPMENT.md`'s table, checked at stage 5 |
| G-4 | The AC-10 host is delivered with a **concrete witness**, and that witness's head displacement does not itself trip the drift row (`bin/sc:2706-2715`), since a hand-edit makes drift PROBLEM and removes the "no PROBLEM row" premise. | stage 4 | the named before/after pair in the delivery |
| G-5 | Key/call-site identity is established by a method that survives the implicit concatenation at `bin/sc:2800-2802`, and the method is written down; a whole-sentence grep returning `1` is not evidence of a drifted key. | stage 4 | `04_DEVELOPMENT.md`'s stated method + K-3's intent |
| G-6 | The corrected AAAA sentence is true of the branch where `config.json` carries no `dns.rules` array, which reaches the same PROBLEM return. | stage 4 | the delivered sentence read against `bin/sc:2793-2802` |
| G-7 | Gate ruling, so QA does not re-open it: a shipped sentence that names **no** directive satisfies AC-5, because FR-6 binds only an enumeration the sentence carries and K-6 requires it to carry none. | stage 6 | this row |
| G-8 | Gate ruling: AC-13 is satisfied by a sentence carrying both numbers with their properties — nine repointable `Path` constants enumerated, the function-body-only property asserted of the eight that have it, `CFG_DIR` named as the exception; the count-only repair (eight → nine with the property left universal) FAILs AC-13. | stage 4, stage 6 | I-5's delivered sentence |
| G-9 | AC-7's placeholder-set equality is established by reading the pair in both directions; a B.4 PASS is recorded as evidence of the subset direction only. | stage 4, stage 6 | the side-by-side read in the delivery |
| G-10 | A ledger ceiling that the edit exceeds is **reported** as a design defect, never crammed; NFR-2's 30 is the binding number and it has ten lines of headroom over the ledger's 20. | stage 4 | `04_DEVELOPMENT.md`'s per-file count against V-21 |
| G-11 | Stage 4 runs and records its own post-change `verify_all` against the measured task-start baseline `PASS 20 / WARN 0 / FAIL 0 / SKIP 1, exit 0` (B.3 the single SKIP); AC-19 requires no FAIL plus F.2/F.5/E.5 PASS, and a step whose status this task does not own is compared, never inherited. | stage 4, stage 6 | the recorded run output against the PM's task-start run |
| G-12 | R-74's amended row states that what has no owner is the **truth** of a sentence about code, not that nothing constrains a shipped sentence. | stage 4 | the amended row read against AC-17/V-15 |
| G-13 | After M-1's rotation, each of the eleven dispositions is still reachable from `docs/tasks.md` — a pointer line that leaves a disposition unfindable defeats FR-12 and is a defect, not a size saving. | stage 4 | V-20 plus the FR-1 classification table |
| G-14 | F-10's residual (`.harness/insight-index.md:10`'s four ranges into `upgrade-project.sh`) travels to the PM at delivery as a filed-row candidate; it is **not** repaired here. | stage 7 / PM | the delivery's residual list |
| G-15 | RS-1's wording imprecision in AC-13 travels to the PM as a filed-row candidate even after G-8 settles this task's reading. | stage 7 / PM | the delivery's residual list |
| G-16 | Gate ruling on AC-3's BLOCKED terminal: the delivered FR-4 clause still **names both sites**, and a site whose source order cannot be settled is reported BLOCKED with a filed row naming the site and the `git show` that would settle it — dropping a named site (which would make AC-3 vacuous for it) or asserting for it a claim the retrieved source did not establish is a defect, not a narrowing. | stage 4, stage 6 | the delivered clause plus the filed row, read against FR-4's two sites |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| PQ-1 | How many override directives can change `dns.rules`' first element, and is the filed "four" right? | **Three** — `$prepend`, `$replace`, and `$before` when its anchor resolves to element 0; `$after` inserts at `i+1` where `i` is a matched index ≥ 0 (`bin/sc:1443-1446`) and `$append` concatenates at the tail, so neither reaches index 0 while the array is non-empty, and it always is (`generate_config` refuses a non-list `dns.rules` at `:2123-2126` and two `sc`-authored writers always populate it). The filed "four" is refuted; note also that the probe compares the head only because `_dns_overlay` prepends exactly one element (`:1782`). |
| PQ-2 | Which binding does I-1's clause describe, and how is its consumer count taken? | The name `userinfo` bound at `bin/sc:792`; its only consumer is `_b64dec(userinfo)` at `:794` (the `try:` at `:793` sits between, so I-1's "on the next line" is off by one). Count by **binding**, not by token: `_userinfo(body)` at `:797` consumes `body`, and the base64 arm's `decoded.rsplit("@", 1)` at `:803` is a different split with a different product. |
| PQ-3 | Is the committed assertion count 19, and which documents state it wrongly? | Yes: `check-sc-contracts.py:846-857` defines 19 names and `baseline.json:4` reads `test_count: 19`. The wrong statements are `.harness/rules/50-singbox-cli.md:29` (**14**), `docs/dev-map.md:87` (**18**) and `docs/tasks.md:238` (**18**); `docs/dev-map.md:76`'s 18 is a frozen past-tense measurement attributed to T-30 and is correctly left alone. |
| PQ-4 | Where exactly do R-91's citations land, and what must I-8's tokens satisfy? | `80-delivery-policy.md:73`'s `:548-556` is the **HALT branch alone** (`upgrade-project.sh:548-556`); the splice is `:534/535-542` and the `.bak` write is `:570-573`, which is why the one-line-apart disagreement across three documents dissolves once I-8 carries no range. Each named token must be one that actually greps — `.bak-<stamp>` does not appear in the script; the literal is `bak="$proj_file.bak-$stamp"` at `:571`. |
| PQ-5 | Do R-77, R-78 and R-84 need any edit? | No. The recipe reads its source with an explicit codec at `docs/dev-map.md:211`, and the three clauses stand at `:234-240`; `main()`'s read-only arm really is `("doctor", "config")` at `bin/sc:3837`, so R-84's clause is true of the delivered file. Report them; edit nothing inside that block. |
| PQ-6 | Will editing the AAAA sentence redden B.4? | No. No committed assertion quotes that sentence — `dns_overlay_prepend_is_head_of_dns_rules` (`check-sc-contracts.py:439-452`) compares rule dicts, and the zh assertion is generic over `TRANSLATIONS` — so changing the key and its `zh` value together keeps `19 defined, 19 run, 19 passed` without touching the frozen `.harness/scripts/**`. |
| PQ-7 | AC-3 says no stage executes an `sc` "as a program" — does that forbid `verify_all` B.4 (which loads `bin/sc`) or AC-2's `py_compile`? | No, and neither is a loophole either. `py_compile` compiles without executing the module body. B.4 is an **in-process import through the mandated recipe**, explicitly carved out by NFR-4 and BC-9, and the recipe neutralises the elevate branch before the body runs — `assert os.geteuid() != 0` then `shim.geteuid = lambda: 0` under a `sys.modules["os"]` shim restored in a `finally` (`docs/dev-map.md:204-214`). What AC-3 forbids is a **process invocation** of any `sc` build, which is the act that reaches `bin/sc:125-126`'s `os.execvp("sudo", ["sudo", "/usr/local/bin/sc"] + sys.argv[1:])`. |
| PQ-8 | AC-9 may force a longer transition enumeration in the changelog lead — does that breach NFR-2's ≈1 line for R-85? | No. `CHANGELOG.md` entries are each one very long physical line, so any rewrite of the T-26 lead costs exactly **one** changed line under K-1 regardless of length; NFR-2's 30 has ten lines of headroom over the ledger's 20, and G-10 governs a genuine breach by reporting rather than cramming. |
| PQ-9 | What do I do if `git show` leaves one FR-4 site unsettled? | Report that site **BLOCKED** and file a row naming the site and the `git show` that would settle it (AC-3, BC-8) — and keep the site named in the delivered clause: G-16 makes dropping it a defect, because a clause that names no site satisfies AC-3 vacuously. This is the project's standing trade (`.harness/operator-obligations.md` ids 1-6, the R-31/R-41/R-47/R-52/R-60/R-68 precedent): report BLOCKED, file the row, substitute nothing. |

## Verdict

APPROVED WITH CONDITIONS — F-1 and F-2 are closed at stage 1, F-9 is closed by measurement, nothing else moved; development proceeds under G-2 … G-16 (G-1 discharged), with G-16 governing the one FR-4 site that may end BLOCKED.
