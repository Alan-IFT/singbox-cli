> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

# T-28 · committed-test-suite — Gate Review

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behaviour is testable and unambiguous — AC-1's four-number prediction was re-measured in round 2 and **is** derivable — except one NFR sentence that claims more coverage than the 14 assertions deliver (F-6). |
| 2 | Design completeness | PASS | FR-1 … FR-19 each map onto a change-ledger row and at least one interface; the 14 interfaces I-17 … I-30 cover FR-7 … FR-13 clause by clause, and FR-14 … FR-19 land on C-2 … C-7 with per-file allocations. |
| 3 | Reuse correctness | PASS | Every reused symbol was read and behaves as the design claims — `_userinfo`, `_write_private`, `_read_state`, `_merge`/`_directive_of`, `_filter_rules`, `_redact`, `_dns_overlay`/`_aaaa_rule`/`_telemetry_overlay`, `OverrideError.path`, `DIRECTIVES`/`_directive_list()`, `uncoverable()`, E5's verdict shape at `restricted-network-regression.sh:273`, `baseline.json`'s unread `test_count: 0`, and the `docs/dev-map.md:129-139` shim recipe; nothing existing was missed. |
| 4 | Risk coverage | WARN | R1 … R9 are the real risks and R3/R4/R9 are strong, but two are absent: the import-time process-replacement capability the shim does not neutralise (F-1) and the zero headroom under F.4's and F.5's caps that a stage-7 harvest or a new ledger row consumes (F-2, re-measured and reduced to MINOR in round 2). |
| 5 | Migration safety | PASS | No data migration, no flag and no installed consumer exists; the C-1 → C-9 order is load-bearing and correct (C-4 precedes C-2 so B.4 can never read a `0` floor and pass vacuously), and every row carries a real rollback. |
| 6 | Boundary handling | WARN | BC-1 … BC-16 cover null/empty/max/concurrency (`mkdtemp`, no fixed path) and every error path of the load, but not the failure path a future `bin/sc` refactor creates (F-1) nor the live third-party writer inside a witnessed directory (F-7). |
| 7 | Test feasibility | PASS | All 24 criteria are observable on this host without root, without network and without touching the live service; I-2's shape makes the AC-10 sweep mechanical, the three declared non-discrimination points are honest, and I found no undeclared vacuity among the 14. |
| 8 | Out-of-scope clarity | PASS | Twelve out-of-scope items, a frozen set and K-2's forbidden-call list bound the work; C-8 is declared and ruled not scope creep, and C-9 is conditional with a hard ≤3 bound and a re-homing rule. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-1 | MAJOR | `02_SOLUTION_DESIGN.md` I-4 / K-3 / K-15(b), inherited from `docs/dev-map.md:125-139` | The neutralisation defeats a **predicate** (`geteuid`) and not a **capability**: the shim copies the real `os.__dict__`, so `shim.execvp` is the real `os.execvp`, and any future `bin/sc:125` refactor to `os.getuid()`, `os.getresuid()` or `os.geteuid() > 0` re-executes `/usr/local/bin/sc` under `sudo` from inside `verify_all` on the owner's live machine — R-78's exact incident — while the recipe clause C-6 preserves still claims it "fails closed if `geteuid` moves". |
| F-2 | MINOR | `01_REQUIREMENT_ANALYSIS.md` AC-1 / Q-2, risk absent from `02_SOLUTION_DESIGN.md` R1 … R9 | **Amended in round 2 after re-measurement — the falsified premise is withdrawn**: `docs/tasks.md` is **300** lines and `.harness/insight-index.md` **30**, F.5 and F.4 compare strictly (`verify_all.sh:224` `n > 300`, `:216` `n > 30`), so both PASS, `warns` is 0 and the script takes `exit 0` (`:248-250`), which makes AC-1's stated start baseline exact and its `PASS 19 / WARN 0 / FAIL 0 / SKIP 1` prediction derivable (18 steps today, C-2 adds two); what survives is that both files sit at **exactly** their caps with zero headroom, so a single line added at stage 7 — an `## Insight` harvest line, or a new ledger row — flips F.4 or F.5 to WARN and the script to exit 1 for a reason no change of this task caused, and neither AC-1 nor `02`'s R1 … R9 carries that as a risk. |
| F-3 | MAJOR | `02_SOLUTION_DESIGN.md` K-13 / `01` NFR 2 | The 60-line external budget is not credible as allocated and its metric is undefined ("added or changed"): C-5's three hunks alone need ~15 added lines (E3 ≈ 6, E4 ≈ 6, R-56 2, R-58 1) against an allocation of **6**, and C-2's B.4+B.5 need ~20 against 18 — an honest plan totals ≈ 73 added lines, not 59, and every clause in it is required by a requirement row, so the cap and not the content is what is wrong (R-61). |
| F-4 | MINOR | `02_SOLUTION_DESIGN.md` K-14 + `02_RATIONALE.md` "Line budget derivation" | The 303-line element table charges a blank line only *inside* the assertion budget, leaving the ~11 other top-level blocks' ~22 separator lines unallocated; the honest floor is **≈ 325**, so 330 is ~2 % headroom rather than the claimed ~8 %, which is R-61's provenance defect one turn further on. |
| F-5 | MINOR | `02_SOLUTION_DESIGN.md` I-5 (and `02_RATIONALE.md` R2) | "Every `pathlib.Path`-valued attribute … and any future constant, **by construction**" is overstated: `PERIODIC_DIRS` (`bin/sc:79-83`) is a **dict of `Path`s** and `SB_BIN` is a `str`, so a scan over Path-valued module attributes reaches neither — harmless today (no assertion reaches them) but the claim is what BC-2 rests on. |
| F-6 | MINOR | `01_REQUIREMENT_ANALYSIS.md` NFR 5 | "The suite asserts these" claims more than the 14 deliver: nothing asserts that `_write_private()` is the **only** writer of `config.json` (no assertion reaches a successful `generate_config()` write), and "`sc config` remains always-redacted" is asserted only at `_redact`'s level, since no assertion drives `cmd_config` — R-74's shape exactly. |
| F-7 | MINOR | `02_SOLUTION_DESIGN.md` I-7 / `01` BC-5 | A **running** sing-box owns a file inside a witnessed directory — `bin/sc:1354` emits `experimental.cache_file.path = /var/lib/sing-box/cache.db` — so the witness is safe only because it covers that directory and not its entries (unlike `/etc/sing-box`), a load-bearing asymmetry the design never states, and any cache implementation that replaces the file by rename moves the directory mtime and reddens B.4 for a reason the suite did not cause. |
| F-8 | MINOR | `02_SOLUTION_DESIGN.md` K-17 | K-17's stopping rule is not the rule applied: FR-7's three clauses became three assertions while FR-9's three (top-level shape, `default` split, `.path`) are one (I-24) and FR-10's two are split by fixture rather than by clause — non-vacuity survives because RS-1 sweeps per **clause**, so the wording is what is wrong, not the granularity. |
| F-9 | NIT | `02_SOLUTION_DESIGN.md` I-13 / C-3 | A reason printed inside a `.ps1` `Step` scriptblock lands *before* the status word, because `Step` writes `"[$id] $name ..."` with `-NoNewline` and only then invokes the block — unavoidable, since `Step` is outside the `HARNESS:B-CUSTOM` markers that K-11 freezes. |
| F-10 | NIT | `01_REQUIREMENT_ANALYSIS.md` Q-10 / out-of-scope | R-75 names T-28 as a conditional owner ("or T-28 when it pins the grep"); this task does not pin the `失败：` / `failed: ` grep, so R-75 stays open with its other owner and must not be reported as closed. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| BC-A | The load path must fail closed against **process replacement itself**, not only against the `geteuid` predicate — a `bin/sc` whose elevate guard reads any other uid source must abort the suite loudly instead of re-executing anything. | 4 (C-1) | `04_DEVELOPMENT.md` names the mechanism and records a run against a scratch `bin/sc` copy whose guard reads `os.getuid()`; stage 5 re-checks it as a design-fidelity row. |
| BC-B | C-6's recipe hunk must not carry forward `docs/dev-map.md`'s "fails closed if `geteuid` moves" claim unnarrowed; the recipe must state what it does and does not guarantee. | 4 (C-6) | AC-23's read of the delivered `docs/dev-map.md`. |
| BC-C | `verify_all.sh` must be run from the repository root and its four-number summary recorded **before the first edit** of this task; AC-1 is then discharged as the **full** `PASS 19 / WARN 0 / FAIL 0 / SKIP 1` with **exit 0**, and no residual WARN may be attributed away, because the pre-edit baseline is measured at `PASS 17 / WARN 0 / FAIL 0 / SKIP 1` with exit 0. `.harness/insight-index.md` (30/30) and `docs/tasks.md` (300/300) carry zero headroom: if a stage-7 insight harvest or a new ledger row tips F.4 or F.5, clear it by rotating per `.harness/rules/70-doc-size.md` (`archive-task` for the index, oldest Completed rows to `docs/tasks-archive.md`) and re-measure — never by widening this task's scope, never by editing a cap. | 4, then 6, then 7 | `04_DEVELOPMENT.md` carries the pre-edit summary; `06_TEST_REPORT.md` carries the post-edit summary; `07_DELIVERY.md` carries the post-rotation re-measurement and, if `archive-task` rotated, the rotation count it printed — the second independent confirmation T-27's R-18 fix is waiting on. |
| BC-D | K-13 is **amended**: the metric is added (`+`) lines as `git diff --numstat` reports them, and the cap is **80** across C-2 … C-7, re-derived as C-2 20 / C-3 15 / C-4 3 / C-5 15 / C-6 12 / C-7 8 = 73. C-8's 6 lines stay declared separately. No required clause may be dropped to fit. | 4 | `04_DEVELOPMENT.md` reports the actual per-file additions against this table. |
| BC-E | K-14 is **amended**: the cap is **350**, over a re-derived floor of 325 (the design's 303-line element total plus the ~22 top-level separator lines the element table does not allocate). The trim order and the "report your own element table on overrun" clause stand unchanged. | 4 | `04_DEVELOPMENT.md` reports the delivered line count, with an element table if it exceeds 325. |
| BC-F | I-5's invariant must be stated as what it is — every module attribute that **is** a `Path` — with `PERIODIC_DIRS` named as the known container escape, either in the suite's header or in C-6's recipe clause. | 4 | AC-8's check plus the suite header text read at stage 5. |
| BC-G | NFR 5's "The suite asserts these" must not be repeated in the delivery; the two clauses it overclaims (`_write_private()` exclusivity, `sc config` always-redacted end to end) are named as uncovered. | 6, 7 | `06_TEST_REPORT.md` coverage statement and the delivery note. |
| BC-H | The witness's `/etc/sing-box`-entries vs `/var/lib/sing-box`-directory asymmetry must be stated where the witness is defined, and AC-5 must be observed at least once with the sing-box service **live**. | 4, then 6 | Suite header comment; `06_TEST_REPORT.md` records the service state during the AC-5 run. |
| BC-I | K-17's wording is corrected to the rule actually applied, and RS-1's per-clause mutation list — not the assertion count — is recorded as the binding non-vacuity mechanism. | 4, then 6 | `04_DEVELOPMENT.md` restates K-17; `06_TEST_REPORT.md` reports one mutation per clause, not per assertion. |
| BC-J | B.4 must invoke the suite with **no** `NAME` argument and must compare `{p}` against `baseline.json`'s `test_count`, never against `{r}`; `--list` and name selection are developer/stage-6 surfaces only. | 4 (C-2) | AC-13's two FAIL cases plus a read of the delivered B.4 block. |
| BC-K | C-9 is expected to stay empty — this gate independently read every `zh` entry of `bin/sc:132-392` and found **zero** offenders. If the delivered run disagrees, stop and report before editing any translation string; BC-11 then governs. | 4 | `04_DEVELOPMENT.md` records assertion 14's offender list (expected: empty). |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| PQ-1 | FR-6 says the run exits 0 "if and only if every assertion **defined** was executed and passed" — must a name-selected run therefore always exit non-zero? | **No. I-10's reading is ruled correct and the requirement is satisfied as read; this is not a rollback to stage 1.** FR-5 makes selection a first-class mode, and a reading that makes AC-3 and AC-10 unverifiable is not the requirement's own reading. The ratchet does not rest on a selected run's exit code: B.4 invokes the unselected run, where `run == defined` is FR-6's clause exactly, and BC-10 is enforced by the `passed < test_count` floor plus the `defined`/`run`/`passed` triple (BC-J). |
| PQ-2 | Is C-8 (`CONTEXT.md`, one glossary entry, ~6 lines) scope creep beyond the requirement's six files? | **No.** `CONTEXT.md:198-203` already defines **assertion floor** in terms of "the committed test step", a step that does not exist until this task ships — the glossary carries a forward reference today. C-8 lands the missing term beside it; while there, confirm that the existing entry reads true once C-2/C-4 land. It stays declared outside K-13's budget. |
| PQ-3 | Will assertion 14 (FR-13) find any offending `zh` entry, i.e. is C-9 live? | **No — verified independently by this gate, not accepted from RS-5.** Every `zh` value in `bin/sc:132-392` was read against its key: every field name a translation uses appears in its key, there is no auto-numbered `{}`, no positional `{0}` and no stray brace. Assertion 14 is a forward guard with no current subject; it is still discriminating, since the mutation that kills it (an offending `zh` string in the clone) is trivially reachable. |
| PQ-4 | Does adding `*@*` to `uncoverable()` break the four shipped rule-set bases, two of which contain an `@`? | **No.** `derive()` calls `uncoverable "$h"` with `host_of`'s output, and `host_of` cuts at the first `/` after the scheme — so bases 1-2's `@` (`meta-rules-dat@sing/geo`) is in the **path** and the tested host is `cdn.jsdelivr.net`. AC-18's `u@cdn.example` is a userinfo *authority* and is correctly rejected. |
| PQ-5 | Can B.4's FAIL detail — which prints the suite's own `PASS  <name>  <evidence>` lines — inflate `verify_all.sh`'s PASS count? | **No.** `pass_count` greps the `report` array, which holds only `id\|name\|status` records; `step`'s detail is echoed to the screen and never appended. BC-14 still binds the id and the name. |
| PQ-6 | I-26 drives `generate_config()` — does it reach `_write_private()` or `subprocess.run([SB_BIN, …])`? | **No.** The `$append`ed sentinel string reaches `_filter_rules` at `bin/sc:1079` (`rule.get` on a `str`) and raises `AttributeError` inside the envelope at `:2133-2136`, which is before `:2140`'s write and before `:2147`'s checker. `_warn_degraded()`/`_warn_drift()` are also never reached, so the assertion adds no stderr text and AC-21 holds. |
| PQ-7 | How is AC-13's "`baseline.json` absent" case taken without leaving the tree dirty or racing another run? | Take it in a scratch clone or by a move-and-restore recorded in `06_TEST_REPORT.md`; never during a run that is also producing an AC-1 measurement, and restore before the AC-15 `git diff`. |
| PQ-8 | Will `/harness-upgrade` delete the suite or drop B.4/B.5? | **No.** `check-sc-contracts.py` is project-authored and is not one of `refresh_set`'s plugin-vendored script pairs, and `verify_all.{sh,ps1}` is excluded from the wholesale refresh in favour of a marker-preserving splice with a HALT and a timestamped `.bak` — which is exactly why BC-13 confines every added line to `HARNESS:B-CUSTOM`. |
| PQ-9 | `docs/tasks.md` and `.harness/insight-index.md` sit at exactly 300 and 30 — is `verify_all` red before I start? | **No — measured twice, independently of the PM.** ripgrep numbers the last content line of `docs/tasks.md` **300** and the index is 30 lines; F.5 and F.4 test `n > 300` and `n > 30`, so both PASS, and `:248-250` exits **0** at `warns == 0`. Take the pre-edit baseline anyway (BC-C) — the caps have zero headroom, so the number you must not assume is the one after stage 7's harvest. |

## Verdict

APPROVED FOR DEVELOPMENT — subject to binding conditions BC-A … BC-K (full-mode equivalent: APPROVED WITH CONDITIONS); no finding routes back to stage 1 or stage 2.
