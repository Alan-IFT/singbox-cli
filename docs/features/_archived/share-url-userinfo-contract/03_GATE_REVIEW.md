> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

# 03 — Gate Review · T-22 `share-url-userinfo-contract`

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behavior is testable and pinned to a constant the fixture URL was written to carry, but `01` FR-6's third sentence puts the ss plaintext `method` / `password` boundary at "the first colon of the **decoded** userinfo", which contradicts FR-2, BC-10, I-6 and K-2 for that arm. |
| 2 | Design completeness | WARN | Coverage is complete — every FR lands on a named call site (I-2…I-6) or is shown to need zero edits (I-7, re-confirmed at `bin/sc:1880`) — but K-12's budget instrument miscounts its own elements (its maxima sum to 23 against a ≤22 cap) and declares six hunks for seven change sites. |
| 3 | Reuse correctness | PASS | Every cited symbol and range resolves as claimed: `bin/sc:637`, `:696`, `:715`, `:717`, `:720`, `:724`, `:725`, `:732`, `:744`, `:763-768`, CPython `_userinfo` / `_hostinfo` (`parse.py:194-217`), `unquote`'s `errors='replace'` default (`:688`), the T-13 recipe (`docs/dev-map.md:116-149`), A.1 (`verify_all.sh:33-34`), and `CHANGELOG.md`'s `## [Unreleased]` → `### 修复` target. |
| 4 | Risk coverage | PASS | RS-11 and RS-12 now carry the two vacuous-green routes round 1 found, and the twelve risks are the real ones — I hunted a thirteenth (an `ss://` body whose last `@` sits in a `?plugin=` tail) and found it already held by RS-2 and pinned unchanged by K-6. |
| 5 | Migration safety | PASS | Q-1 re-verified in code (`cmd_add` `:2295` → `:2301` → `:2308`; nothing persists the URL), so no migration is expressible and none is claimed; the only user-visible migration instrument is K-14 (b)'s explicit `sc reload` **negative**, which is correct. |
| 6 | Boundary handling | PASS | BC-1…BC-12 each survive the stdlib first-hand: `rpartition('@')` (`parse.py:196`) gives BC-2, `_hostinfo`'s bracket strip (`:206-217`) gives BC-3, `unquote`'s `errors='replace'` (`:688`) gives BC-8 with no exception, and `_userinfo` being total over every `str` means no call site gains a raise. |
| 7 | Test feasibility | WARN | AC-2 is now satisfiable and genuinely red — I re-derived all nineteen V-1 fixtures against `parse.py:198` and HEAD fails exactly the eleven it names — but V-6's two pattern groups can go red on a compliant implementation, and no criterion separates vless's first-field reading from the whole-userinfo one. |
| 8 | Out-of-scope clarity | PASS | The rework added exactly one criterion (AC-16) and one question (Q-12) and no code element: I-1's shape, the five call sites, K-12's cap and NFR-3's two-file diff are unchanged, so nothing was smuggled in under a correction. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| GF-1 | MAJOR — discharged | `01` AC-2 · `02` V-2 | The expected-mismatch set is now the eleven fixtures HEAD actually fails (five tuic `F-a`…`F-e`, three trojan `F-a`, three hysteria2 `F-a`), and the eight trojan / hysteria2 `F-b`…`F-e` matches are declared expected by construction — each of the nineteen re-derived here against `parse.py:198`, not accepted. |
| GF-2 | MAJOR — discharged | `01` AC-12 · `02` K-14 (a) | The damage clause is now four exact predicates plus two explicit non-claims, and every row of the derivation matches HEAD's code (`bin/sc:637`, `:696`, `:717`, `:724`, `:732`, `:744`, `:763-768`); the README parity clause fires only if a README sentence changes. |
| GF-3 | MINOR — discharged | `02` RT-6 / K-8 / K-9 | The ss-plaintext `method` decode is now K-8 delta 2, RT-6 states three deltas with a per-parser enumeration behind it, and AC-3 / AC-4 exclude `%` from the **whole** userinfo; my own independent per-parser re-derivation found the same three and no fourth. |
| GF-4 | MINOR — discharged | `02` V-6 group (ii) | The sweep now carries `partition(':'`, `split(":"`, `rsplit(":"` with permitted hits enumerated in advance — five, not four; see BND-4, corrected in place, and GF-11 for the pattern's literalness. |
| GF-5 | MINOR — discharged | `01` `F-a` definition · `02` V-1 | `F-a` carries `::`, `:pw` and `pw:` for trojan and hysteria2; `pw:` is the shape a two-value implementation rebuilding `first + ":" + rest` cannot emit, so the class now does the work the third projection exists for. |
| GF-6 | MINOR — discharged | `01` AC-7 | AC-7 demands `server` = `2001:db8::1` unbracketed, which is exactly what `_hostinfo` (`parse.py:206-217`) yields at HEAD, and it says in-row that filing the missing brackets is out of scope. |
| GF-7 | MINOR — discharged, residual bound | `02` K-13 | Scoping the docstring's no-second-opinion sentence to URI text clears `bin/sc:715` / `:724` (base64-recovered material), but the tag decodes at `:575` / `:710` are also URI text, so the sentence must be scoped to userinfo **fields** — BND-7 corrected accordingly. |
| GF-8 | MINOR — discharged | `01` AC-1 preamble · `02` K-21 | Per-class explicit fixture text is stated in both documents and in V-1 / V-5, a blanket `quote()` is banned by name, and RS-11 states the exact failure it would produce. |
| GF-9 | MINOR — discharged, residual bound | `01` AC-16 / Q-12 | AC-16 positively observes FR-7's decode half (HEAD emits `a%2Db` from `bin/sc:637`, the design emits `a-b`) and sits in no byte-identity corpus and no HEAD-mismatch set; it does **not** separate the first-field reading from the whole-userinfo one, which BND-10 closes for one fixture's cost. |
| GF-10 | MINOR | `01` FR-6, third sentence | "the `method` / `password` boundary at the first colon of the **decoded** userinfo" is true of the two base64 arms and false of the plaintext arm, where FR-2 / BC-10 / I-6 / K-2 all require the split on the raw text and the decode afterwards (`a%3Ab:pw` is the case where the two orders diverge). |
| GF-11 | MINOR | `02` V-6, both pattern groups | The expected observables are literal to a spelling: group (i) expects one `netloc` read "inside `_userinfo`" while K-5 puts four `p.netloc` reads at the call sites and I-1 names the parameter `authority` (zero inside), and group (ii)'s patterns are quote-specific, so `split(':', 1)` at a call site evades the sweep while `partition(":")` inside `_userinfo` is not counted as its permitted hit. |
| GF-12 | MINOR | `02` K-12 | "exactly six hunks" cannot be a `git diff` count — the seven change sites (the function, four `urlparse` parsers, `parse_ss` `:717` and `:732`) are each more than six lines apart, so a fully compliant diff shows seven — and the element list's own maxima (13 + 10) sum to 23 against the ≤22 cap. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| BND-1 | AC-2's expected-mismatch set is exactly the five tuic `F-a`…`F-e` fixtures plus the three trojan and three hysteria2 `F-a` fixtures; the trojan / hysteria2 `F-b`…`F-e` matches at HEAD are expected by construction and are never read as a broken harness. | requirement-analyst (01 AC-2), architect (02 V-2) | **discharged at round 2** — corrected text verified fixture-by-fixture against `parse.py:198` |
| BND-2 | The shipped `CHANGELOG.md` bullet states K-14 (a)'s four damage predicates, makes neither named non-claim, and states the remove-and-re-add repair with its `sc reload` negative. | developer (CHANGELOG), QA (V-7) | the bullet read clause by clause at V-7 / AC-12 |
| BND-3 | Three pre-declared behaviour deltas and no fourth (K-8 ×2, K-9); AC-3 / AC-4 byte-identity corpora carry no `%` **anywhere in the userinfo**, and AC-3's corpus excludes any ss fixture HEAD refuses to parse. | architect (02 RT-6) — discharged; QA (06) | **document side discharged at round 2**; corpus side by `06_TEST_REPORT.md` |
| BND-4 | **Corrected in place — five permitted hits, not four.** V-6 group (ii) permits exactly: `bin/sc:715` (SIP002 base64-userinfo arm, survives because CL-6 replaces only the `except` arm), `:724` (legacy whole-body arm), `:720` and `:725` (host/port), and the one inside `_userinfo`. A sixth fails AC-10 whatever the credential assertions say. | architect (02 V-6) — already corrected; developer (04) / code-reviewer (05) to run | sweep output in `04_DEVELOPMENT.md` + `05_CODE_REVIEW.md` |
| BND-5 | The `F-a` class carries all three BC-4 shapes (`::`, `:pw`, `pw:`) for trojan and hysteria2, so a two-value implementation cannot pass; tuic's `F-a` is one fixture, which is why V-1 counts 19 and AC-2 counts 11. | requirement-analyst (01) — discharged; QA (06) | **document side discharged at round 2**; fixture side by `06_TEST_REPORT.md` |
| BND-6 | Fixture URLs are written per class as explicit text (raw `:`, literal `%3A`, literal `%2525`, explicit `%XX`, raw `@`); no known credential is ever passed through a blanket `quote()`, and every expected value is the constant the URL text was written to carry. | developer / QA | the harness listing pasted into `06_TEST_REPORT.md` (RT-5) |
| BND-7 | **Corrected in place.** `_userinfo`'s docstring scopes its no-second-opinion sentence to **userinfo fields taken from URI text**, so it is falsified neither by the base64 colon splits at `bin/sc:715` / `:724` (material that was never URI text) nor by the tag decodes at `:575` / `:710` (URI text, but a tag rather than a userinfo field). | developer (04) | the shipped docstring, read at `05_CODE_REVIEW.md` |
| BND-8 | AC-7 is read as K-4 reads it — `server` is `2001:db8::1`, unbracketed, exactly HEAD's bytes — and no stage files the missing brackets as a defect. | QA (06) | `06_TEST_REPORT.md` AC-7 row |
| BND-9 | The harness is created outside the repository worktree, is never committed, and keeps every credential literal ≤7 characters wherever it could reach a tracked non-`.md` file; A.1 (`verify_all.sh:33-34`, threshold 8, `git grep` over tracked files excluding `*.md`) stays PASS. | every stage that runs it | V-8 PASS with A.1 still PASS |
| BND-10 | AC-4's vless corpus includes at least one `%`-free fixture whose userinfo carries a **raw colon** (e.g. `vless://a:b@host:443`), expected byte-identical to HEAD; it is the only instrument that separates FR-7's first-field reading from FR-5's whole-userinfo reading, which AC-16 alone does not. | QA (06), developer (harness) | `06_TEST_REPORT.md` AC-4 row naming that fixture |
| BND-11 | V-6 is executed with corrected expectations: group (i) permits **four** `p.netloc` argument passes at the call sites (K-5) plus **one** application of the last-`@` rule inside `_userinfo`, whose parameter is `authority`; group (ii) is run quote-agnostically (e.g. `(partition\|split\|rsplit)\(\s*['"]:`) so a single-quoted `split(':', 1)` cannot evade it. | developer (04), code-reviewer (05) | the sweep command and its output, quoted in `05_CODE_REVIEW.md` |
| BND-12 | The ss plaintext arm's `method` / `password` boundary is the first colon of the **raw** userinfo, decoded afterwards (FR-2, BC-10, I-6, K-2); `01` FR-6's "first colon of the decoded userinfo" phrase governs only the two base64 arms and is superseded for the plaintext arm. | developer (04), code-reviewer (05) | `04_DEVELOPMENT.md` naming the order, plus AC-3's `F-j` fixture |
| BND-13 | K-12 is read as ≤22 added / ≤11 removed across the **six named edit locations** (CL-1 plus the five parsers); a `git diff` showing seven hunks satisfies it because `parse_ss` carries two, and if the element maxima would sum to 23 the docstring is cut to ≤5 lines rather than the cap raised. | developer (04), code-reviewer (05) | the diffstat reported in `04_DEVELOPMENT.md` |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| PQ-1 | `p.username` / `p.password` already split the raw userinfo at its first colon — may I just use them for tuic? | No: K-3 forbids it, AC-10 sweeps for it, and `parse.py:198` is the hidden premise that produced this bug; the file states the rule once, inside `_userinfo`. |
| PQ-2 | Must `_userinfo` care about a bracketed IPv6 host or the port? | No — both live right of the last `@` (`parse.py:206-217`), so `whole` never contains them and `p.hostname` / `p.port` stay untouched. |
| PQ-3 | `parse_ss`'s base64 arms still split at a colon (`:715`, `:724`) — does that violate FR-1? | No: they split base64-**recovered** material, which was never URI text; K-7 pins them, BND-4 pre-enumerates them, and routing them through `_userinfo` would percent-decode them — BC-9's exact defect. |
| PQ-4 | Why five permitted colon-split hits when the design deletes two? | CL-6 replaces only the `except` arm at `:717` and CL-5 deletes tuic's `:765`; `:715`, `:724`, `:720`, `:725` all survive, plus the one inside `_userinfo` — hence five (BND-4). |
| PQ-5 | How many fixtures does V-1 build, and how many should be red at HEAD? | 19 built, 11 red: tuic contributes one fixture per class (`F-a`…`F-e` = 5, all red), trojan and hysteria2 seven each (`F-a` ×3 red, `F-b`…`F-e` green by construction). |
| PQ-6 | AC-6 needs a `100%2525` fixture but K-15 caps credential literals at 7 characters — which wins? | Both: A.1 greps only **tracked** files excluding `*.md`, and the harness lives outside the worktree, so the 8-character literal never reaches a tracked file. |
| PQ-7 | K-19 says baseline and candidate run at the **same** fixture root — must I reset it between runs? | Rewrite `nodes.json` / `settings.json` and delete `.config.sha256`; never move the root, because `RULES_DIR` is emitted verbatim into `route.rule_set[].path` (`bin/sc:1884`). |
| PQ-8 | Can the harness just call `main()` with `sc.LANG` set? | No — `main()` reassigns `LANG` and `CLASH_PORT` and `_init_files()` `mkdir`s the literal `/var/lib/sing-box` (`bin/sc:532`); drive `generate_config()` directly and assert both globals after every run (K-17). |
| PQ-9 | Does `_userinfo` need `or ""` guards against `None`? | No — `p.netloc` is always a `str` and `str.rpartition` / `str.partition` always return three strings, which is what makes I-1's totality claim true; the `None` that HEAD leaked into vless's `uuid` is exactly K-9's delta. |
| PQ-10 | My function comes to 14 lines and the budget's element list sums to 23 — did I blow K-12? | The cap is the binding number (BND-13): each element is an upper bound, so trim the docstring to ≤5 lines rather than negotiate the cap, and expect seven `git diff` hunks, not six. |

## Verdict

APPROVED FOR DEVELOPMENT
