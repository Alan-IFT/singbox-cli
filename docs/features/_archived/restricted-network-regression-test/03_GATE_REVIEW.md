> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behaviour is stated as an observable with a status vocabulary, but FR-10 is not literally satisfiable for E1 — no observation in a successful blackout run is one in which "success banner, exit 0" fails (F-1, F-10). |
| 2 | Design completeness | PASS | Every FR-1…FR-16, BC-1…BC-14 and AC-1…AC-20 lands on a named element in `02_RATIONALE.md`'s coverage table, checked row by row; no in-scope behaviour is left without one. |
| 3 | Reuse correctness | PASS | Every cited symbol and line range exists at HEAD as claimed — `RULESET_BASES` (`bin/sc:113-118`), `_ruleset_bases` (`:1052-1061`), `generate_config` (`:1992-2081`), `install_report` (`install.sh:257-302`), the `umask 027` log probe (`:561-563`), step 6 (`:566-574`) — and `check-i18n-parity.sh` really is the no-`.ps1`, unignored precedent claimed. |
| 4 | Risk coverage | WARN | R-A…R-H are the real risks and each has a mechanism behind it, but two live ones are absent: both `verify_all` caps AC-2 depends on sit exactly at their limit today (F-4), and the `[6/7]` guard is a regex character class (F-3). |
| 5 | Migration safety | PASS | There is no data migration and no flag; the single host mutation is one file copied before the edit and restored byte-for-byte, every ledger row carries a rollback, and the single-use VM is the rollback of record for the scenario itself. |
| 6 | Boundary handling | PASS | Null/empty (BC-13 zero bases), unreadable (BC-10 log, K-3's `-e`-or-`-L` node store), absent-observation (BC-12 ⇒ BLOCKED), re-run (BC-11), pre-populated start state (BC-1) and the fail-closed pre-mutation gate order (K-3) are each designed rather than assumed. |
| 7 | Test feasibility | WARN | The `[HOST]` half (AC-1…AC-5, AC-14…AC-18, AC-20's host half) is fully dischargeable here, but two criteria are unfalsifiable as written — V-17's `dns_refs≥0` (F-2) and the self-check's covered arm, which validates a derivation against itself (F-6). |
| 8 | Out-of-scope clarity | PASS | Twelve requirement exclusions, seven design exclusions and a per-path frozen set with a stated reason each, plus the standing instruction that an edit to a file with no ledger row is a defect — the developer cannot widen the diff by accident. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-1 | MEDIUM | `02_SOLUTION_DESIGN.md` `## Constraints` K-11 (E1 clause) | E1's `pair=` is "the failure-banner count from the same capture", which is the negation of E1's own assertion over the same bytes rather than an observation in which the assertion fails — and E1 is the one condition an entirely **unrestricted**, healthy install satisfies identically, so its `pair=` carries no information about whether the blackout took effect. |
| F-2 | MEDIUM | `02_SOLUTION_DESIGN.md` `## Verification plan` V-17 | `dns_refs≥0` is true of every possible document including the degraded one (`dns_refs=0`), so FR-9's "references them again" is unfalsifiable on the DNS side; the recovered document actually carries three `rule_set`-bearing `dns.rules` entries (`bin/sc:1249,1250,1253`). |
| F-3 | MEDIUM | `02_SOLUTION_DESIGN.md` K-12, and `## Verification plan` V-12/V-14 | `[6/7]` used as a `grep` pattern is a POSIX bracket expression matching any line containing `6`, `/` or `7`, so K-12's "did the installer reach the rule-set step" guard would pass on almost any non-empty capture; `❌`, `OK (` and `ruleset(s) failed to update` carry metacharacters with the same exposure. |
| F-4 | MEDIUM | `01_REQUIREMENT_ANALYSIS.md` AC-2; `02_SOLUTION_DESIGN.md` `## Migration & edit sequence` step 5 | F.4 counts `wc -l` over the **whole** `.harness/insight-index.md` (header included), which is at exactly 30 today, and F.5 counts `docs/tasks.md`, at exactly 300 — one harvested insight or one board row at delivery makes `WARN 0` become `WARN 1`; `archive-task.sh:85-95` rotates on **bullet** count only (30 bullets = 38 file lines) and so will not prevent it. |
| F-5 | MEDIUM | `02_SOLUTION_DESIGN.md` V-16 / K-7; `01_RATIONALE.md` §8 | E5 read at the first positive `is-active` inside the settle loop is implied by E1's own success arm (which requires `systemctl start` to have returned 0) and cannot distinguish a service that starts and dies inside the window — `systemd/sing-box.service:11` `Restart=on-failure` makes that state look `active`/`activating` intermittently, so as specified E5 agrees with E1 instead of constraining it. |
| F-6 | MEDIUM | `02_SOLUTION_DESIGN.md` I-2/I-7 and V-5 | The self-check's covered arm is self-consistent by construction — coverage is computed from the same parse it validates — so a derivation that under-matches (three of four bases) still reports "all covered, exit 0"; the only thing that catches under-derivation is a `[VM]` run in which E3/E4 fail, and the `[HOST]` half is the whole of the evidence this pipeline can produce (RS-5). |
| F-7 | LOW | `02_SOLUTION_DESIGN.md` K-3 / K-1 | The four-gate order is pinned but the **position of `mktemp -d`** is not, so nothing in the contract stops the work directory being created before gate 1 or on the `--self-check` path — the one thing that would falsify AC-3's "no write outside its own temporary area" framing and I-2's "writes nothing anywhere". |
| F-8 | LOW | `02_SOLUTION_DESIGN.md` K-10 / I-2 | K-10's dependency list (bash, coreutils, curl, python3, systemd, `getent`) excludes `git`, yet both `--source`'s default `<repo>/bin/sc` and the checkout's `install.sh` need a repo root, which the natural implementation takes from `git rev-parse --show-toplevel`. |
| F-9 | LOW | `02_SOLUTION_DESIGN.md` `## Change ledger` C-3, C-4 | C-3 (`rejected-decisions.md`) and C-4 (`CONTEXT.md`) have no FR/BC/AC behind them and no verification step reads them — they are the only two ledger rows the contract neither requires nor checks. |
| F-10 | INFO | `01_REQUIREMENT_ANALYSIS.md` FR-10 | FR-10 as written demands, for E1, an observation in the same run in which "the installer completes, prints the success banner and exits 0" does not hold — which a single-arm successful run cannot contain; the requirement, not only the design, is why F-1 exists, and the remedy substitutes a discriminating observation for a true counter-observation. |
| F-11 | LOW | `02_SOLUTION_DESIGN.md` K-10 | Summed across the I-15 guide block, four gates, the derivation, the hosts block and its proof, six condition observations with paired values, the `cfg_facts` heredoc and the recovery arm, the element list lands at roughly 240-265 lines, so the ≤250 cap has no margin and K-10's "target ≤235" is not credible. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| GC-1 | E1's `pair=` carries a same-capture observation that separates a **degraded** success from a healthy one — the step-6 rule-set-failure warning read from the same capture — rather than the failure-banner count; the banner count may remain as a companion but FR-10 may not rest on it. | 4 | `04_DEVELOPMENT.md` states the chosen value; `05_CODE_REVIEW.md` reads it in the code. |
| GC-2 | Every marker match against the capture or the install log is a fixed-string match (`grep -qF` / `grep -cF`), specifically for `[6/7]`, `ruleset(s) failed to update`, `OK (`, `✅ Install complete` and `❌`. | 4 | `05_CODE_REVIEW.md`, line by line. |
| GC-3 | E6's config assertion requires a **non-zero** `dns_refs` as well as `defs=4` and non-zero `route_refs`, so FR-9's DNS clause can fail; the degraded document's `dns_refs=0` stays E6's `pair=`. | 4 | `04_DEVELOPMENT.md`; `05_CODE_REVIEW.md`. |
| GC-4 | E5's `obs=` is the state string read at the **end** of the ≤10 s settle window (or two reads whose `MainPID` agrees), never the first positive read, and the string is printed verbatim in the condition line. | 4 | `05_CODE_REVIEW.md`. |
| GC-5 | `--self-check`'s covered arm prints the derived base list **and its count**, and `06_TEST_REPORT.md` compares that list character-for-character against `bin/sc:113-118` instead of accepting exit 0. | 4, 6 | `06_TEST_REPORT.md`, V-5 evidence. |
| GC-6 | No filesystem object is created before all four K-3 gates pass: `--self-check`, the usage path and both refusal paths create no `mktemp -d`, no capture file and no `/etc/hosts` backup. | 4 | `05_CODE_REVIEW.md`; AC-3/AC-4 evidence in `06_TEST_REPORT.md`. |
| GC-7 | The repository root is derived from the artifact's own path, never from `git`; `git` is not added to K-10's dependency list. | 4 | `05_CODE_REVIEW.md`. |
| GC-8 | AC-2 is measured **before** any insight harvest or task-board edit, and delivery keeps `.harness/insight-index.md` at ≤30 `wc -l` lines and `docs/tasks.md` at ≤300 by rotating rather than by accepting a WARN. | 7 | `07_DELIVERY.md` plus the final `verify_all` transcript. |
| GC-9 | If the artifact cannot fit 250 lines, the overrun and what it buys are recorded per the NFR's rule-85 clause in `04_DEVELOPMENT.md`; no condition, no `pair=` field and no I-15 guide element is dropped to fit. | 4 | `04_DEVELOPMENT.md`. |
| GC-10 | Nothing in stages 4-6 runs `install.sh`, `uninstall.sh` or `/usr/local/bin/sc`, or writes `/etc/hosts`, `/usr/local/bin`, `/etc/sing-box`, `/var/lib/sing-box`, `/var/log/sing-box` or any unit on this host; V-3 and V-4 are the only executions of the artifact here and both must stop at I-3 or K-3 gate 2. | 4, 5, 6 | `06_TEST_REPORT.md` AC-18 witness pair (`MainPID` + `ActiveEnterTimestamp`, never `is-active`). |
| GC-11 | AC-16's baseline is a `git clone` under an ignored path (`test/…`), never a `git worktree` (RS-3), and its presence is confirmed not to be reachable by any `verify_all` `find` root before the counts are read. | 6 | `06_TEST_REPORT.md`, V-8. |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| A-1 | Is stage 1's inversion of T-01's AC-9 really true at HEAD — does a total-blackout install exit 0? | Yes, re-derived first-hand: `install.sh:567` leaves `PHASE_RULESETS=failed` and prints step6_warn (`:570-573`); `:590` `sc reload` → `reload_or_restart` (`bin/sc:2098-2102`) → `generate_config` deletes the empty `route.rule_set` (`:2060-2061`) and filters **both** rule arrays (`:2062-2063`), so `sing-box check` passes and `PHASE_CONFIG=ok`; `:593` starts the service (already started by `restart_service()` inside `sc reload`) so `PHASE_SERVICE=started`; `install_report`'s success arm (`:260-273`) prints `  ✅ Install complete` and returns 0; `:615` exits 0. E1 as written is correct and AC-9's last two clauses are refuted. |
| A-2 | Does I-6's `sed`+`grep` really yield exactly the four bases? | Yes. `^RULESET_BASES = (` matches `bin/sc:113` and the range closes on `^)` at `:118`; `[^"]+` stops at the closing quote, so each of the four lines yields exactly one match and the comment at `:112` is outside the range. Base 3 yields the single string `https://ghfast.top/https://raw.githubusercontent.com/MetaCubeX/meta-rules-dat/sing/geo`, whose `/`-field-3 is `ghfast.top`. The derived strings are **byte-identical** to what `cmd_update_rules` prints after `failed:` (`bin/sc:3274`, `:3284`), which is what makes E3's per-source assertion exact rather than approximate. |
| A-3 | Which language will the assertions see, and can K-6's `printf '1\ny\n'` be wrong about it? | English on both sides. `1` selects English at `install.sh:376`; install.sh persists `lang` into `/etc/sing-box/settings.json` at step 3 (`:492-506`), **before** step 6; and `_load_lang()` (`bin/sc:388-392`) reads only that file and never `$LANG`, so `sc`'s log lines are English too. Insight-index line 12's `LANG` vacuity trap does not fire here. |
| A-4 | Will `systemctl enable` actually succeed for both units, so E2 is not a false FAIL on correct code? | Yes — `systemd/sing-box.service:15-16` carries `[Install] WantedBy=multi-user.target` and `sing-box-rules-update.timer:9-10` carries `[Install] WantedBy=timers.target`; `install.sh:581-583` enables both **before** config generation, each with `|| true`, so E2 is independent of E1 and of the rule-set outcome. |
| A-5 | Can the new file move a `verify_all` count on its own? | No. B.1's list is fixed (`verify_all.sh:63` = `install.sh`, `uninstall.sh`), B.2 is `check-i18n-parity.sh install.sh` by name (`:71`), A.1 excludes `.harness/*` (`:34`), E.4's sync check covers `.harness/agents|skills` only, and no F.* cap covers `.harness/scripts/`, `CONTEXT.md` or `.harness/rejected-decisions.md`. The only movers are F.4 and F.5 at delivery — GC-8. |
| A-6 | `0.0.0.0` is a connect failure, not a resolution failure — does the design survive that? | Yes. `getent hosts`, `curl` and `urllib` all go through the same glibc NSS `files` source, so I-9 observes exactly the resolution the fetchers get; Linux then maps a connect to `0.0.0.0` onto loopback, which is refused immediately. E3 asserts only that each `failed:` line **names each base** — never the cause text — so the resolve/connect distinction cannot break it, and the immediate refusal is what keeps the run inside K-7's budget. |
| A-7 | Does `install.sh` consume both piped answers, or will the stray `y` break something? | Only the language prompt is unconditional (`:372-380`); the root-install confirmation (`:383-390`) fires only when `SUDO_USER` and `logname` both yield nothing or `root`. When the operator invoked the artifact through `sudo` from a named account the `y` is simply discarded with the pipe, and `install.sh` performs no further `read`. `printf '1\ny\n'` is correct in both cases. |

## Verdict

`APPROVED WITH CONDITIONS` — T-07 may proceed to development subject to gate conditions GC-1 … GC-11.
