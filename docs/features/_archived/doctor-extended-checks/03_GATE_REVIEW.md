> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behaviour is stated as a testable row with a named class, but FR-4's second sentence ("carries the other decision") is narrower than the membership test FR-4's first sentence requires, and AC-B8 — the single adversary for four of the five new rows — carries an exit-0 clause that depends on five sections this requirement does not own. |
| 2 | Design completeness | WARN | Every FR and every BC has a named home that I re-derived at the code, but two contract-internal contradictions decide against the design's own invariants: I-6 calls `_dns_overlay()` (which re-enters `ipv6_decision()`, breaking K-6 and voiding E-3's whole justification) and K-3 bans `.stat()` "anywhere in the diff" while I-9 mandates `CFG_DIR.stat()` plus a per-entry `lstat()`. |
| 3 | Reuse correctness | PASS | Every reused call exists with the claimed shape at the claimed line — `ruleset_states()`' 6-tuple carries `mtime` from the one `os.fstat` inside the single read (`bin/sc:824`, `:845-859`), `_age_text` (`:933`), `_drift_state` (`:1879`), `ipv6_decision` (`:1580`), `_dns_overlay` (`:1618`), `stored_delays` (`:2077`), `clash_api` (`:2045`), `_saved_clash_port` (`:364`), `CRED_MODE` (`:41`), `DOCTOR_MSG_LINES` (`:2335`) — and `_doctor_rulesets()` already destructures with `*_rest` (`:2434`), so FR-1 is one call in a loop that already runs. |
| 4 | Risk coverage | WARN | R-a…R-h are the real risks and R-c names the fixture-vacuity class correctly, but its mitigation is wrong at the code: K-19 makes `is_running()` true by stubbing `subprocess.run`, which the recipe K-19 invokes never reaches because it sets `sc.SYSTEMD = sc.OPENRC = False` (`docs/dev-map.md:132`). |
| 5 | Migration safety | PASS | No data migration, no on-disk format change and no new file or setting; the one compatibility consequence (exit 0 → 1, and also → 2 for the new UNKNOWN states) is stated and is disclosed in E-11/E-12's exit-status lists, and the three behaviour-neutral seams land first in order 1 with V-11's byte comparison gating every row that follows. |
| 6 | Boundary handling | WARN | I re-derived BC-1…BC-26 against the code and all but two land by construction (skew is already clamped in `_age_text:944`; `_config_digest()`'s `None` gives BC-6 for free; the no-port and no-answer branches of `_doctor_clash` give BC-10…BC-12 with no request issued) — the two open edges are a per-entry `lstat()` on a path that vanished mid-listing and a `doc["dns"]` that is not an object, both of which escape their stated guards into the section's UNKNOWN. |
| 7 | Test feasibility | WARN | Every criterion is verifiable in principle and V-1…V-19 column onto them, but three discharges are unsound as written: AC-B8's exit-0 clause, AC-S1's "at import or definition time" (a deletion raises `NameError` at call time, which `cmd_doctor`'s `except Exception` turns into a graceful UNKNOWN row and exit 2), and V-16's static sweep for `.stat()`. |
| 8 | Out-of-scope clarity | PASS | Out-of-scope 1-12, the frozen set and K-8…K-16 name the same boundary three times and each declined item carries a reason and a destination — including R-38, whose ledger row offers T-20 as an owner and which is declined because the fix lives in `cmd_status` and travels as RS-5. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-1 | MAJOR | `02_SOLUTION_DESIGN.md` `## Interfaces` I-6 | I-6's membership test is spelled over `_dns_overlay()["dns"]["rules"]["$prepend"]`, and `_dns_overlay()` calls `ipv6_decision()` itself (`bin/sc:1635`), so the probe as specified calls it twice per run — the exact double call K-6 forbids and the one E-3's extraction is justified by in `## Smaller alternative rejected`. |
| F-2 | MAJOR | `02_SOLUTION_DESIGN.md` `## Constraints` K-3 (with `01` AC-S2 and V-16) | K-3 requires that `Path.stat` / `.stat()` "appear nowhere in the diff" while I-9 mandates `CFG_DIR.stat()` and a per-entry `lstat()`, so V-16's sweep as written either produces a false FAIL or pushes the implementer off the call I-9 requires. |
| F-3 | MAJOR | `02_SOLUTION_DESIGN.md` `## Constraints` K-19 | K-19's `subprocess.run` stub cannot make `is_running()` true under the recipe it cites, because `is_running()` (`bin/sc:2068-2074`) returns `False` from its final line without reaching `subprocess.run` when both `SYSTEMD` and `OPENRC` are false — so the node-delay matrix degrades to the vacuous twin on candidate and control alike. |
| F-4 | MAJOR | `01_REQUIREMENT_ANALYSIS.md` AC-B8 (with `02` V-12) | AC-B8 is the only adversary for four of the five new rows, and its exit-0 / "no `[PROBLEM]`, no `[UNKNOWN]`" clause is satisfiable only if five sections this task neither owns nor provisions (version, `sing-box check`, service, boot autostart, TUN, egress) are all green in the fixture — the pressure to weaken it is exactly the pressure that lets the PROBLEM-everywhere build through. |
| F-5 | MINOR | `01_REQUIREMENT_ANALYSIS.md` AC-S1 | Deleting `_age_text` / `_drift_state` / `stored_delays` / `ipv6_decision` raises `NameError` at call time, not at import or definition time, and `cmd_doctor`'s per-section `except Exception` (`bin/sc:2634-2639`) converts it into one UNKNOWN row and exit 2 — so a test asserting an import failure cannot pass on a correct build and a test asserting only "the run failed" cannot fail on a wrong one. |
| F-6 | MINOR | `02_SOLUTION_DESIGN.md` `## Constraints` K-20 | K-20 routes a *contradicting* P-2 body back to stage 2 but has no branch for a P-2 that cannot be run at all (no live service, or no answer), which is the state in which a developer is likeliest to write the DNS row from the design's inferred body shape. |
| F-7 | MINOR | `02_SOLUTION_DESIGN.md` `## Interfaces` I-9 (BC-26) | I-9 guards an `OSError` from `CFG_DIR.stat()` and from `sorted(CFG_DIR.iterdir())` but not from a per-entry `lstat()`, and a `_write_private()` temporary appearing and being renamed away mid-listing is BC-26's own scenario — the whole permission section then degrades to one UNKNOWN row. |
| F-8 | MINOR | `02_SOLUTION_DESIGN.md` `## Interfaces` I-6 | The traversal to `doc["dns"]["rules"]` is guarded only by `isinstance(doc, dict)` and `isinstance(rules, list)`, so a `dns` value that is not an object raises `TypeError`, which K-7's `(OSError, ValueError)` does not catch and which costs the section its row. |
| F-9 | MINOR | `02_SOLUTION_DESIGN.md` `## Change ledger` E-11 / E-12 | The README paragraph that promises "**`sc doctor` changes nothing**" (`README.md:270`) is not in the declared edit list, while FR-6 makes the command ask the running service to perform a real name resolution on a host whose `experimental.cache_file` is enabled at `/var/lib/sing-box/cache.db` (`bin/sc:1235`). |
| F-10 | MINOR | `01_REQUIREMENT_ANALYSIS.md` AC-B1 | No acceptance criterion observes the age phrase on a *healthy* rule-set row: a build that renders `{age}` only on stale rows satisfies AC-B1, AC-B1's control and AC-B8, and only V-2 — which is columned to BC-1/BC-2 and to no AC — would catch it. |
| F-11 | INFO | `02_SOLUTION_DESIGN.md` `## Residuals travelling` RS-1 | **Ruled: the architect's reading of NFR-2 is confirmed.** NFR-2 forbids a *second* read of a fact an existing reader already returns; no existing reader returns the document's AAAA rule (`_config_digest()` reads the bytes for a digest and returns none of them, `bin/sc:1834-1856`), so FR-4's single guarded `read_text()` + `json.loads` is irreducible and is not a second opinion. |
| F-12 | INFO | `02_SOLUTION_DESIGN.md` `## Residuals travelling` RS-3 | **Ruled: the row's wording is accepted as written.** It states a property of the document ("does not carry this decision") rather than a cause, that statement is true for every cause including the older-build one, and its next step is correct for all of them; narrowing the subject would need a document version stamp that nothing in the tree has. |
| F-13 | INFO | `02_SOLUTION_DESIGN.md` `## BC-16 probe and ruling` | **Ruled: the symbol-table inference holds and FR-6 ships.** I reproduced all six P-1 counts first-hand and added two controls the design did not run — `clashapi.configRouter` and `clashapi.proxyRouter` are both present (1 each) and both serve routes proven live on this host, while a fabricated `clashapi.queryDNSNotReal` matches 0 — with the residual that `clashapi.scriptRouter` is present too and `/script` answers "not supported", so P-1 establishes a **mounted** route and never a supported body. |
| F-14 | INFO | `01_REQUIREMENT_ANALYSIS.md` FR-4 (with `02` I-6) | The design's test fires wider than FR-4's second sentence — "does not carry this decision" also covers a document carrying neither decision (RS-3, RS-4) — and this is accepted because FR-4's first sentence commissions exactly that membership test and the row's value text states only what was tested, which is the inverse of the R-22 defect. |
| F-15 | INFO | `.harness/rules/70-doc-size.md` | The file still defines no `## Stage-doc boundary rule` (R-37, fifth confirmation, now recorded at stage 3 of this task as well), so I applied the gate schema exactly as written and carried the re-derivation narrative, the probe controls and the verified-good notes in `03_RATIONALE.md`. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| GC-1 | AC-B8's adversarial force is carried by its **row-level** clauses — every new row `[OK]`, no new row naming a path or a next step, and exactly +5 rows against a HEAD run of the same fixture — and those three are asserted independently of the exit-status clause; if exit 0 cannot be reached because a section this task does not own is not green, that is reported as a named partial with the offending section, never by dropping a row-level clause. | 4, 6 | V-12 run with the row-level assertions listed separately in `06_TEST_REPORT.md`, plus the exit value recorded whatever it is. |
| GC-2 | The AAAA probe consumes `_aaaa_rule(suppress)` with the `suppress` from its **single** `ipv6_decision()` call; `_dns_overlay()` is not called from anywhere in the `# doctor` block, and `ipv6_decision()` has exactly one call site inside a `sc doctor` run. | 4 | A grep of the diff for `_dns_overlay(` and `ipv6_decision(` call sites (V-16) **and** V-5's "at most one IPv6 stderr line per run", which must be asserted as an exact count, not as "no traceback". |
| GC-3 | K-3, AC-S2 and V-16's static sweep are scoped, in the words the developer records, to "no `st_size` anywhere, and no `.stat()` / `getmtime` on a **rule-set** path"; `_doctor_permissions()`'s `CFG_DIR.stat()` and per-entry `lstat()` are the sanctioned mode reads and form no second opinion of any reported fact. | 4, 6 | V-16 re-run under the scoped sweep, with the two sanctioned call sites named in `04_DEVELOPMENT.md`. |
| GC-4 | Every fixture step whose expected observable depends on `stored_delays()` issuing a request sets `sc.SYSTEMD = True` **in addition to** stubbing `sc.subprocess.run` (the stub returning an object carrying both `.returncode` and `.stdout` bytes, so `_doctor_run()` still works and no `systemctl` is ever exec'd), and the AC-B4 corner asserts at the stub server that the `/proxies` request **was received**. | 4, 6 | V-6 and V-12 with the stub's request log quoted in `06_TEST_REPORT.md`; V-8's mirror assertion (no `/proxies`, no `/dns/query`) stays as written. |
| GC-5 | AC-S1's four deletion tests assert that the **named** section degrades and that the row names the deleted symbol, and that every other section still prints — never that the import fails and never merely that the run exited non-zero. | 6 | Four runs recorded with the UNKNOWN row's text quoted per deletion. |
| GC-6 | If probe P-2 cannot be run at stage 4, the developer reports **BLOCKED and routes back to the solution-architect** under BC-16's no-code outcome; the DNS row is not written from the design's inferred body, and no V-7 stub body is invented. | 4 | `04_DEVELOPMENT.md` carrying either P-2's verbatim body or the BLOCKED record with the reason. |
| GC-7 | The behaviour on a per-entry `lstat()` `OSError` and on a `dns` value that is not an object is decided and recorded in `04_DEVELOPMENT.md`; either behaviour is admissible provided no traceback reaches the user and every other section still prints (BC-23). | 4 | The recorded choice plus one V-13 sub-run per case. |
| GC-8 | E-11/E-12 amend `README.md:270` / its zh mirror alongside the declared edits, so the "changes nothing" promise stays exactly as wide as the behaviour once one lookup is issued through the running service, and section 6's amended text names that lookup. | 4 | The README diff read at stage 5 against this condition. |
| GC-9 | V-2 is mandatory and asserts an age phrase on at least one **usable, non-stale** row, so FR-1's "beside the status and byte count already there" is observed on a healthy row and not only on a stale one. | 4, 6 | V-2's capture quoted in `06_TEST_REPORT.md`. |
| GC-10 | Every `{e}`, every filesystem-sourced path and every mode string the new rows print goes through `_plain()`, and no new zh string contains `失败`. | 4 | V-15 and V-17 as written, plus a read of the new call sites at stage 5. |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| PQ-1 | Does `/dns/query` need a query-string escape, a new envelope, or a `Request` change in `clash_api()`? | No. `clash_api()` builds `f"http://127.0.0.1:{port or CLASH_PORT}" + path` (`bin/sc:2054`), so a query string needs no plumbing; `EGRESS_HOST` is a bare hostname needing no quoting; and K-1 forbids a second envelope — a 404, a 500, a reset and a timeout all arrive at the caller as `None`. |
| PQ-2 | What does `{n}` count when the directory holds both wide-mode paths and symlinks? | The summary row is PROBLEM (the wide mode wins over the unjudgeable link), `{n}` counts the wide-mode paths only, and the link lines still print as detail lines under the same `DOCTOR_MSG_LINES` cap; AC-B13 asks only that the link be reported and the target's mode absent. |
| PQ-3 | `config.json` parses, but `doc["dns"]` is a string — PROBLEM, UNKNOWN, or a raise? | PROBLEM, through I-6's `False` branch: the document demonstrably does not carry the rule. Guard each level with `isinstance` in the `stored_delays()` house style; `TypeError` is not in K-7's tuple and must not be the mechanism. |
| PQ-4 | Do the new UNKNOWN rows change the exit status? | Yes — 0 → **2** through the unchanged mapping, on a host with no drift record, no recorded Clash port or no init system. BC-22 names only the exit-1 case; the disclosure lives in E-11/E-12's exit-status `2` cause list, which is already declared. |
| PQ-5 | Does `sc doctor` now write something, given FR-13? | Not itself: the DNS row issues an HTTP `GET` and touches no path. The *running service* performs the resolution and may persist its own state (`experimental.cache_file` is enabled at `/var/lib/sing-box/cache.db`, `bin/sc:1235`). AC-B10's snapshot is the fixture root and is unaffected; do not widen FR-13's claim to cover the service (see GC-8). |
| PQ-6 | Is a `GET /dns/query` barred by out-of-scope item 5, "any probe request to the Clash API other than reads"? | No. That item targets a fresh **latency** measurement (`/proxies/<name>/delay`) and a widened `stored_delays()`; FR-6 commissions exactly one lookup, the method stays `GET` under K-8, and no row claims a fresh delay measurement. |
| PQ-7 | Which timestamp does the staleness verdict use, and does it need `_age_seconds()`? | The same 6-tuple element `_age_text()` renders (`ruleset_states()`, `bin/sc:845-859`). No helper: `_age_text()` already clamps with `max(0, ...)` (`:944`) so both readings are non-negative from one `mtime`, and at exactly the threshold the row prints `60 days ago` while the verdict flips — number and verdict agree. |
| PQ-8 | How does a fixture make the five sections this task does not own green for V-12, without root and without the live host? | In process: `sc.SB_BIN` is repointable at a stub script (`docs/dev-map.md:134`, no `PATH` games), `_doctor_binary` / `_doctor_config` / `_doctor_service` / `_doctor_tun` all reach the outside through `sc.subprocess.run`, and `_doctor_egress` through `sc._egress_ip` — so one argv-dispatching stub plus one `_egress_ip` replacement covers all five, and K-18 is never approached. |

## Verdict

APPROVED WITH CONDITIONS — GC-1 … GC-10 bind stage 4 and stage 6; no requirement gap routes back to the requirement-analyst and no design gap routes back to the solution-architect, because F-1 … F-8 are each decided by an invariant the design already states.
