> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behaviour is testable and the phantom is refuted with first-hand evidence, but AC-B2's "exactly one value line under each of its section headings" is unsatisfiable over the two subprocess sections (F-4) and BC-14 under-states the change (F-3). |
| 2 | Design completeness | WARN | One edit inside `clash_api()` covers FR-1/FR-2 and I3's five callers are each already correct against I1 (verified at `:2026`, `:2148`, `:2230`, `:2503`, `:2580`), but the forced effect on `cmd_use` and `cmd_mode` for BC-1…BC-4 is neither stated nor verified (F-3). |
| 3 | Reuse correctness | PASS | Every reuse citation resolves first-hand — `clash_api()` `:1978-1992`, `stored_delays()`'s isinstance idiom `:2004-2048`, `_egress_ip()` `:391-400` with call sites `:2237`/`:2520`, the two keys at `:143-144` — and the exception hierarchy matches the **installed** stdlib line for line (`urllib/error.py:19,35`; `http/client.py:1511,1533,1559,1566,1571`). |
| 4 | Risk coverage | WARN | Risks 1-6 are the real ones and each carries a control, but the vacuity set is incomplete: `main()` reassigns `CLASH_PORT` (`bin/sc:3125`) exactly as it reassigns `LANG`, which K-10/K-11 do not cover (F-7). |
| 5 | Migration safety | PASS | There is genuinely nothing to migrate — no persisted-state, config-document, digest or flag change — and the three-step edit sequence reverts with one `git checkout`, which I checked against the ordering constraint that `http.client` is never referenced before it is imported. |
| 6 | Boundary handling | PASS | BC-1…BC-8 each land on a named family or on the isinstance gate, `{}` survives BC-8 because `{}` is itself a `dict`, and the two classes that escape (`RecursionError`, `MemoryError`) are disclosed under BC-12 rather than hidden — only the published wording of that fact is wrong (F-5). |
| 7 | Test feasibility | FAIL | Two criteria cannot be executed as written: AC-B2/V7's per-heading line count (F-4), and V6's HEAD control for BC-5, which HEAD does not exhibit — `_doctor_clash()` returns `[OK] … yes` for `5`/`"x"`/`[1,2]` and agrees with the candidate for `null` (F-1, F-2). |
| 8 | Out-of-scope clarity | WARN | The frozen set, the `_egress_ip()` freeze and the eleven out-of-scope items are tight enough that a developer cannot drift, but out-of-scope item 5 names "`sc doctor`'s … exit status" while the design necessarily moves it (F-10). |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-1 | MAJOR | `02_SOLUTION_DESIGN.md` § Verification plan, V6 | V6 expects the HEAD control to "raise" for BC-1…BC-5, but at HEAD `_doctor_clash()` (`bin/sc:2503-2509`) raises only for BC-1…BC-4 — a body of `5`/`"x"`/`[1,2]` returns `[OK] Clash API: …` + `[OK] Clash API responding: yes`, which is the lying row `01_RATIONALE.md` E-7 itself describes. |
| F-2 | MAJOR | `01_REQUIREMENT_ANALYSIS.md` AC-B3 (and `02` V4, V5) | BC-5 bundles four body values whose control classes differ — `null` decodes to `None`, so HEAD prints `(unavailable)` and *agrees* with the candidate at `cmd_status` and at `cmd_use`, while `5`/`"x"`/`[1,2]` traceback — so a single declared control class over BC-5 is the bundled-control defect `.harness/insight-index.md:30` names. |
| F-3 | MAJOR | `01` BC-14 + `02` R1 and K-8 | The behaviour-change enumeration is still incomplete after R1: for BC-1…BC-4 `cmd_use` (`bin/sc:2147-2153`) moves from a traceback to `reload_or_restart()` — regenerating the config and printing "(service restarted)" — and `cmd_mode` (`:2579-2581`) moves from a traceback to printing `Route mode → <mode>` while the running process keeps its old mode; no verification step observes either, and K-8's bullet names neither `sc mode` nor the restart. |
| F-4 | MAJOR | `01` AC-B2 (and `02` V7) | "prints exactly one value line under each of its section headings" cannot pass: `=== Service status ===` is followed by `systemctl status --no-pager -n 5` and `=== TUN interface ===` by `ip -br addr show` (`bin/sc:2219-2225`), so only FR-3's four fact sections are one-line by construction. |
| F-5 | MAJOR | `02` K-7 (and I1's "**Total.**", inherited from `01` FR-1) | The published `docs/dev-map.md` row is to state "never an exception" as an absolute while the same document's `## Out of scope` records `RecursionError` and `MemoryError` escaping by design — a published surface that is false in exactly the case BC-12 declines to defend. |
| F-6 | MINOR | `02` § Verification plan, stand-in paragraph | "(c) and (d) rule out a bare `http.server` handler" is not true — `BaseHTTPRequestHandler` can write arbitrary non-UTF-8 bytes to `self.wfile` and can declare a `Content-Length` larger than it writes — so the raw-socket fixture is a fine choice but not a forced one, and the sentence must not read as a constraint on QA. |
| F-7 | MAJOR | `02` K-10 / K-11 (vacuity set) | A third vacuity trap of the same family is unnamed: `main()` reassigns `CLASH_PORT` (`bin/sc:3125` via `_resolve_clash_port()`) and `_doctor_clash()` reads `_saved_clash_port()` only, so a fixture whose `settings.json` records no `clash_api_port` gets a *free* port probed and persisted and every BC state silently degrades to "nothing listening" on candidate and control alike. |
| F-8 | MINOR | `01` AC-B1 | The equality between `sc`'s egress line and an independent echo endpoint has no declared tie-break, so a multi-address egress makes AC-B1 fail for a reason that is not a defect in `sc`. |
| F-9 | MINOR | `02` R2 / R3 / R6 | The three PM-routed residuals land in `docs/tasks.md` (at F.5's 300-line cap) and `.harness/rejected-decisions.md`; nothing in this task's diff touches them, but appending without rotating would turn a WARN-free baseline into a WARN. |
| F-10 | INFO | `01` Out of scope item 5 | Item 5 places "`sc doctor`'s … exit status" out of scope while the design's forced consequence moves it; the two are reconciled by C-1 rather than left as a standing contradiction. |
| F-11 | MINOR | `01` AC-S2 | R4 confirmed first-hand: there are five literal call sites of `clash_api()` (`bin/sc:2026`, `:2148`, `:2230`, `:2503`, `:2580`), with `sc ls` reaching it indirectly through `stored_delays()` (`:2101`); the criterion's substance ("no new handler at any call site") holds over both readings. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| C-1 | BC-14 is widened **here**, with no stage-1 round: for the rest of this task it reads "`sc doctor` meets BC-1 … BC-5", covering the port row surviving and the `[PROBLEM] Clash API responding` row replacing the collapsed `[UNKNOWN]` section. | 3 (ruled) → 7 | `07_DELIVERY.md` records the widened statement as an owned, user-visible contract change |
| C-2 | Every statement of the exit-status move — K-8's changelog bullet, V6's expected column, `07_DELIVERY.md` — reads "the Clash section moves `[UNKNOWN]` → `[PROBLEM]`, so the run's exit moves 2 → 1 whenever no other section reports `[PROBLEM]`", never an unconditional 2 → 1. | 4, 6, 7 | stage 5 reads the changelog bullet; stage 6 reports V6's rows and exit codes |
| C-3 | T-18 may claim T-05's DEF-2 ("a hung Clash port loses S6's port row") closed, but only on V6's BC-1 evidence showing both the port row and the `[PROBLEM]` row on the candidate where the control raises. | 6 → 7 | `06_TEST_REPORT.md` V6 record; PM files the closure against T-05's row |
| C-4 | AC-S2's "the six call sites" is corrected in place to "five literal call sites (`bin/sc:2026`, `:2148`, `:2230`, `:2503`, `:2580`) plus `sc ls`'s indirect reach through `stored_delays()` (`:2101`)"; the criterion is otherwise unchanged. | 4, 6 | V2's diff read names the five lines |
| C-5 | A control class is declared per (state, body value), not per BC: `null` is an **agreement** state at `cmd_status`, `cmd_use` and `_doctor_clash()`; `5` / `"x"` / `[1,2]` are the defect states, and V6's HEAD control for them is `[OK] Clash API responding: yes` — not a raise. A run whose control does neither is reported inconclusive per NFR-5. | 6 | `06_TEST_REPORT.md` per-state control table |
| C-6 | One observation of `sc use` and one of `sc mode` at BC-1 (hung port), candidate and control, are added; K-8's bullet gains `sc mode` and gains the fact that `sc use` on those states now regenerates the config and restarts the service instead of tracebacking. | 4 (changelog), 6 | stage 5 reads the bullet; stage 6 records both observations |
| C-7 | AC-B2 and V7's "exactly one value line per heading" is scoped in place to FR-3's four fact sections (current node, route mode, Clash API address, egress); the service and TUN sections carry `systemctl` / `ip` output verbatim and are counted only for "no `Traceback`". | 6 | V7's captured run, reported per section |
| C-8 | K-7's `docs/dev-map.md` row and K-9's docstring state the contract in a form that survives BC-12 — the three families named, plus the one clause that a pathological loopback body can still raise `RecursionError` / `MemoryError` — instead of an unqualified "never an exception"; K-1's tuple itself is unchanged. | 4 | stage 5 reads `docs/dev-map.md:39` and the docstring |
| C-9 | Every fixture that reaches `clash_api()` through `main()` records `clash_api_port = <stand-in port>` in the `mkdtemp()` copy of `settings.json`, and V6's `_doctor_clash()` fixture does the same (it reads `_saved_clash_port()` only); each run reports the port it actually talked to, and a run whose port is not the stand-in's is inconclusive. | 6 | `06_TEST_REPORT.md` fixture description + per-run port |
| C-10 | If AC-B1's two endpoints disagree, QA records both values, repeats once against a third endpoint, and reports the observation with the numbers; AC-B1 fails only if `sc`'s value is not a public address this host egresses from. | 6 | V7's record |
| C-11 | The BC-1 `sc use` observation runs with `sc.SYSTEMD = sc.OPENRC = False` (so `restart_service()` is a no-op) and with all eight path constants repointed and asserted; the report states that no `systemctl` / `rc-service` invocation was issued and that `sing-box check` ran only against the fixture config. | 6 | `06_TEST_REPORT.md` V5 extension |
| C-12 | V7's five preconditions are checked **and recorded** before the run; if any is false — in particular if `settings.json` records no `clash_api_port`, which would make `sc status` probe and persist one — the run is not performed and AC-B1/AC-B2 are reported blocked, never substituted with an artifact check. | 6 | pre/post mtime + size witness and the precondition record |
| C-13 | `06_TEST_REPORT.md` carries the heading `## Adversarial tests`, unnumbered (`verify_all` E.6 matches `^##\s+Adversarial\s+tests` over **every** `docs/features/**/06_TEST_REPORT.md`, not only archived ones). | 6 | V10's `verify_all` run ends with no FAIL |
| C-14 | R2, R3 and R6 stay outside this task's diff and PM-owned; when the PM files them, `docs/tasks.md` (300-line cap) and `.harness/insight-index.md` (30-line cap, currently exactly at it) are rotated per rule 70 rather than appended past the cap. | PM | post-delivery `verify_all` still reports WARN 0 |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| PA-1 | Where does the `isinstance` gate go relative to the empty-body branch, and does the order really matter? | Keep `json.loads(text) if text else {}` first and gate afterwards: `{}` is itself a `dict`, so the gate passes it and BC-8 survives. The order that matters is that the test is `isinstance`, never truthiness. Note the widened catch makes a BC-8 regression **silent** rather than a traceback (`json.loads("")` raises `ValueError`, which is now caught), so V3 must assert `{}` by value *and* type. |
| PA-2 | Is deleting `import urllib.error` safe? | Yes. It has exactly two occurrences in the file (`bin/sc:15` and `:1991`), and `urllib.request` imports `urllib.error` itself, so no other binding in the file depends on the statement. Re-grep the **final** file, not the hunk (R5). |
| PA-3 | `cmd_status:2230` is the line the traceback comes from — should I not guard it there? | No. K-4 and AC-S2 forbid it, and all five sites are already correct against I1 — `:2026` isinstance, `:2148` `is not None`, `:2230` `(r or {})`, `:2503` `is not None`, `:2580` value discarded. A caller-side guard fixes one of six symptoms and leaves `sc ls` broken. |
| PA-4 | Should the tuple also catch `RecursionError` / `MemoryError` so the function is literally total? | No — BC-12 declines that threat model and catching `MemoryError` is worse than the disease. Instead the *wording* must stop claiming absolute totality: C-8. Do not add a fourth family and do not fall back to `except Exception`. |
| PA-5 | `sc use` and `sc mode` behave differently on a hung Clash port after this change — bug or intent? | Intent: those states now behave exactly as a refused port behaves today (`sc use` falls through to regenerate + restart, `sc mode` prints its line). It is FR-2's whole point and no caller may be edited; it must be observed and disclosed (C-6), not "fixed". |
| PA-6 | Which port will my fixture actually talk to? | Whatever `main()` last assigned to the module global `CLASH_PORT` (`bin/sc:326` at import, reassigned at `:3125`) — so a `main()`-driven fixture must record `clash_api_port` in its own `settings.json` (C-9), or `_resolve_clash_port()` probes a **free** port and every state reads as "nothing listening". |

## Verdict

APPROVED WITH CONDITIONS — C-1 … C-14 binding; no stage-1 or stage-2 round is required (R1 and R4 are ruled here, and dimension 7's FAIL is a defect in two criteria's wording that C-5, C-7 and C-9 correct in place at stage 6 without touching a single design decision).
