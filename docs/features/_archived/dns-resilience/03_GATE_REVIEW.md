> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behaviour is stated as something observable and all 34 criteria are reachable in principle, but two assert outcomes the measured software cannot produce — AC-B10's `direct`-mode stall (F-2) and AC-12/BC-11's write-free `sc ipv6 show` (F-4). |
| 2 | Design completeness | PASS | Every FR maps to a named interface, constraint or frozen row whose anchors I re-read in `bin/sc` — I-7 at `dns.rules[0]`, L-4's deletion of `sc:1101`, I-11's sites at `sc:1543`/`sc:1566-1569`, I-15's `sc:2519`/`sc:2578`, I-14's `sc:2617-2634`/`sc:2652-2661`, I-12's `sc:2672-2676` — and the change ledger covers the whole permitted diff. |
| 3 | Reuse correctness | PASS | Every reused symbol exists and behaves as claimed — `_filter_rules`'s keep-if-no-`rule_set` branch (`sc:879-881`), `route.rules[0]`'s `process_name` rule (`sc:1121`), `remote_dns`'s server-level `domain_resolver` (`sc:1084`), `_saved_clash_port()`'s shape (`sc:304-317`), `cmd_lang`/`cmd_mode`'s persist pattern (`sc:2469-2478`, `sc:2219-2228`), `reload_or_restart()` (`sc:1616-1620`) — and both "nothing exists" claims (no IPv6 code in `bin/sc`; no DNS failover primitive) hold on inspection. |
| 4 | Risk coverage | WARN | R-1 … R-12 name the risks that have actually burned this project, the fixture traps and the over-claim risk included, but the "witness that cannot fail" family is covered only in its `grep`-prefix instance (R-10) and not in the `SYSTEMD = OPENRC = False` instance that makes three verification steps vacuous (F-5). |
| 5 | Migration safety | PASS | Nothing migrates: the settings key is additive and never seeded (Q-8/I-2), the document is regenerated rather than patched, every edit step carries a stated revert, downgrade is safe because the old build ignores the key, and `sc ipv6 on` is a standing user-level escape from every effect this task ships. |
| 6 | Boundary handling | WARN | Absent/malformed/unreadable settings, empty and malformed address sources, zero nodes, both node-failure shapes, both rule-set states, all three routing modes, concurrency and non-TTY output are each designed and each has a step — but the user-fault attribution path loses its provenance at `sc:1574-1576` (F-3), and BC-14's stated mechanism is contradicted by `route.default_domain_resolver` (`sc:1118`) and observed by nothing (F-9). |
| 7 | Test feasibility | FAIL | Three criteria cannot be verified as written — AC-B10's `direct` control (F-2), the AAAA probe that makes AC-B1/B2/B3/B9's HEAD controls contradict their own classification (F-1), and AC-12 read as a claim about the command (F-4) — and AC-10's source-level half is mapped to a step that cannot observe it (F-8); each correction is text-level, so C-1/C-2/C-4/C-8 carry them rather than a rollback. |
| 8 | Out-of-scope clarity | PASS | Fifteen numbered out-of-scope items, a design out-of-scope list, a frozen set with a reason per row, and K-13/K-16/K-17 make the one over-build the measurements invite — a fallback resolver or a configured wait — both forbidden and mechanically checkable (V-21, V-11). |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-1 | major | `02_SOLUTION_DESIGN.md` § Verification plan, V-26 (inherited by V-27, V-28, V-34) | V-26's third probe is "AAAA of a name matched by no rule", which at HEAD reaches `remote_dns` through `dns.final` and stalls ≈10 s, so the HEAD control cannot "produce the candidate's outcome" as AC-B10 requires of the agreement controls AC-B1/AC-B2/AC-B3/AC-B9. |
| F-2 | major | `01_REQUIREMENT_ANALYSIS.md` § Acceptance criteria, AC-B10 (`direct` clause); carried by `02` RS-10 | Verified in the file: at HEAD an AAAA query under clash mode `direct` matches `{"server": "direct_dns", "clash_mode": "Direct"}` (`bin/sc:1100`) and is answered by a resolver reached without a node, so a ≈10 s stall is impossible there, while the `global` half is sound because `{"server": "remote_dns", "clash_mode": "Global"}` (`bin/sc:1099`) carries `detour: proxy` (`bin/sc:1085`). |
| F-3 | major | `02_SOLUTION_DESIGN.md` K-7 / I-11 / I-12 (against `01` FR-14, AC-22) | `generate_config()`'s three-key array guard (`bin/sc:1574-1576`) raises `OverrideError` *after* the override merge site and can only fire when a user override is present, so under I-12's `path = None` default it would render against `CFG_PATH` — a genuinely user-caused fault would stop naming `override.json`, which K-7's "exactly two sites, and nowhere else" does not catch. |
| F-4 | major | `01_REQUIREMENT_ANALYSIS.md` BC-11 / AC-12 / NFR-5 (and `02` V-13) | `main()` runs `_init_files()` and `_resolve_clash_port()` for every command except `doctor` (`bin/sc:2646-2651`), and those create `/etc/sing-box`, seed `nodes.json`/`settings.json` (`bin/sc:418-429`) and may bind a probe socket and persist a port (`bin/sc:328-344`), so "`sc ipv6 show` writes no file, issues no network request" is true of `cmd_ipv6()` but false of the command on a fresh host — and NFR-6 forbids the fixture from driving the path that would expose it. |
| F-5 | minor | `02_SOLUTION_DESIGN.md` § Verification plan, V-13 / V-14 / V-15 | The declared service witness ("shimmed `systemctl`/`rc-service` record no invocation") is vacuous under NFR-6's mandatory `SYSTEMD = OPENRC = False`, where `restart_service()` (`bin/sc:1609-1613`) invokes nothing at all, and V-15's positive expectation "the service is restarted once" is unobservable for the same reason. |
| F-6 | minor | `02_SOLUTION_DESIGN.md` § Verification plan preamble, derivation (2) | The preamble preserves `dns.servers` "with every tag, the array order, `detour: proxy`" but never states whether `remote_dns` keeps `type: https` (`bin/sc:1085`), which needs a DoH stub sing-box will trust, or is derived to a plain transport — and the latter is a further derivation the same sentence forbids ("three derivations of the emitted document, and no fourth"); the staged shape of the `proxy` outbound is likewise unstated. |
| F-7 | minor | `02_SOLUTION_DESIGN.md` § Verification plan, V-32 | Its added expectation "in the degraded state the three rule-set names reach the **proxied** stub in both runs" holds in `rule` and `global` modes but not in `direct`, where `{"server": "direct_dns", "clash_mode": "Direct"}` sends them to the non-proxied stub; AC-B7's own criterion (the same stub in both runs) is unaffected. |
| F-8 | minor | `02_SOLUTION_DESIGN.md` § Verification plan, V-3's `AC` column | AC-10's source-level half — no configuration literal in `generate_config()`, no fourth key in its three-key array guard — is mapped to V-3, an emitted-document differential that cannot observe source; no step reads the diff for it. |
| F-9 | observation | `01_REQUIREMENT_ANALYSIS.md` BC-14 (against `02` RS-3) | BC-14 states as fact that a node whose address resolves only to AAAA becomes unreachable under suppression, while `route.default_domain_resolver` (`bin/sc:1118`) pins dial-side lookups to `direct_dns` outside `dns.rules` and RS-3 records the mechanism as unverified; no criterion observes it. |
| F-10 | observation | `02_SOLUTION_DESIGN.md` § Verification plan preamble | Every behavioural criterion is measured on a derived document — TUN inbound replaced, both DNS servers repointed at local stubs, the `proxy` selector replaced by a staged outbound — so the shipped document's own DNS path (TUN capture plus `route.rules[0]`'s `process_name` rule, a real DoH transport, T-15's auto-select group) is evidenced only by `sing-box check` (V-7) and the differential (V-3). |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| C-1 | V-26's third probe — and V-27, V-28, V-34 which reuse it — must use a query type HEAD already suppresses (64 or 65), so that AC-B1/AC-B2/AC-B3/AC-B9's HEAD runs are genuinely agreement controls; the type-28 observation stays only where AC-B10 classifies it as defect-reproducing (V-29, V-31, V-36). Fixes F-1. | 4 (fixture), re-checked at 6 | V-26 … V-28 and V-34 recorded with the probe type stated, each HEAD control producing the candidate's outcome |
| C-2 | AC-B10's `direct`-mode clause is corrected **now**, by this gate, and stages 4 and 6 verify against the corrected text: *in clash mode `direct` the HEAD control must exhibit the defect as the absence of suppression — HEAD issues the AAAA query to the non-proxied resolver and answers it — because `bin/sc:1100` makes a stall impossible there; the `global` clause stands unchanged.* AC-B10's substance (every behavioural run has a control; a defect control must exhibit its defect) is preserved. The PM files the same amendment into `01_REQUIREMENT_ANALYSIS.md` at delivery, RS-10's second addressee. Fixes F-2. | 4, 6, PM at delivery | V-31 recorded verbatim in `06_TEST_REPORT.md` against the corrected text, plus the amendment filed |
| C-3 | No user-facing surface may claim a fallback resolver or a configured wait (K-16), and the READMEs' statement of the node-independent class (L-12/L-13, I-17) is **not final** until V-26 … V-28, V-32 and V-34 have observed it; if measurement contradicts I-17, the class text changes and the divergence is reported as a finding, never reconciled silently. | 4, verified at 6 | V-21's grep plus V-26/V-32 recorded before the README text is frozen |
| C-4 | AC-12, BC-11 and NFR-5's "writes no file / no network request / one local read" are scoped to `cmd_ipv6()`; `04` and `06` must state that `main()`'s startup path (`_init_files()` + `_resolve_clash_port()`, `bin/sc:2646-2651`) still runs for `sc ipv6` exactly as for every non-`doctor` command and can write on a fresh host, and no text may claim `sc ipv6 show` is write-free *as a command*. K-10 stands: the read-only opt-out arm is not touched. Fixes F-4. | 4, 6, PM at delivery | V-13 recorded with its scope stated, a sentence to that effect in `04`/`06`, and the scoping filed into `01` |
| C-5 | A failure caused by the user's document must name `OVERRIDE_PATH` in **every** case, the three-key array guard (`bin/sc:1574-1576`) included; K-7's "exactly two sites, and nowhere else" is incomplete as written. V-23 must add a run in which an `override.json` turns `dns.rules` into a scalar and assert the rendered message names `override.json`. Fixes F-3. | 4 | V-23 extended and recorded, against AC-22 and FR-14 |
| C-6 | V-13/V-14/V-15's service witness must be one that can actually fire under `SYSTEMD = OPENRC = False` — an init-command shim alone is not evidence, because `restart_service()` (`bin/sc:1609-1613`) invokes nothing in that configuration — and V-15 is its non-vacuity control. Fixes F-5. | 4, 6 | V-14 and V-13 recorded with the witness silent and V-15 with the same witness firing exactly once |
| C-7 | The fixture's transport derivation must be declared explicitly — whether `remote_dns` keeps `type: https` and what makes its stub acceptable, or is derived to a plain transport — counted as a declared derivation rather than smuggled under "three derivations, and no fourth", together with the staged shape of the `proxy` outbound for each of the three node states. `06` records that M-5 leaves DoH-versus-UDP behaviour unestablished, so no claim rests on it. Fixes F-6. | 4 | the complete fixture recipe pasted verbatim into `04_DEVELOPMENT.md` and rebuilt independently at stage 6 |
| C-8 | AC-10's source-level half needs a step that reads the source; V-3's document differential cannot see it. Fixes F-8. | 4 | an added or extended step recorded against AC-10 |
| C-9 | V-32's degraded-state expectation is corrected in place to: *the three rule-set names reach the proxied stub in `rule` and `global` modes and the non-proxied stub in `direct` mode, identically in both runs*; AC-B7's criterion is unchanged. Fixes F-7. | 4, 6 | V-32 recorded per mode |
| C-10 | BC-14's mechanism stays unverified (RS-3): no README, changelog, help text or stage document may state more than "a node reachable only over IPv6 needs `sc ipv6 on`", and `06` records the claim as unobserved. Fixes F-9. | 4, 6 | V-21's grep plus an explicit line in `06_TEST_REPORT.md` |
| C-11 | `06_TEST_REPORT.md` states the limits of the behavioural evidence: the shipped document's TUN capture path, `route.rules[0]`'s `process_name` rule, the real DoH transport and T-15's selector/auto-select group are exercised by no behavioural run, and the shipped document itself is evidenced only by V-3 and V-7. Fixes F-10. | 6 | a stated limits paragraph in `06`, carried into `07_DELIVERY.md` beside RS-9 |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| D-1 | `dns.rules[1]` (`hosts_dns`, `bin/sc:1098`) carries no matcher — does my new rule at index 0 plus that one leave rules `[2]` … `[7]` dead? | No, and do not settle it by reading sing-box's source. Two pieces of field evidence already exclude it: the shipped document's `clash_mode` and rule-set rules demonstrably work in production, and this host's `sed` patch to the **index-3** `query_type` rule measurably took an AAAA query from 10.0 s to 0.016 s, which is unreachable if the chain terminated at index 0 or 1. V-26 and V-32 observe the class directly, per name and per mode; C-3 forbids freezing the README's class text before they run. |
| D-2 | May I just add `28` to the existing rule at `bin/sc:1101` instead of deleting it and prepending a new one? | No (K-3, I-7). At index 3 that rule sits *after* `{"clash_mode": "Global"}` (`bin/sc:1099`) and `{"clash_mode": "Direct"}` (`bin/sc:1100`), so suppression would disappear in exactly the two modes users switch to when something is broken (FR-2, Q-4). Index 0 is the only position satisfying AC-3, and the base copy must go in the same edit so the document never carries two `query_type` rules. |
| D-3 | Could the IPv6 decision ride inside `_runtime_overlay()` rather than a second overlay function? | No. `_runtime_overlay()` is in the frozen set: AC-8, V-3's differential and T-15's baseline all rest on it being untouched, and the two overlays answer different questions from different inputs with different failure modes. `_dns_overlay()` is a sibling, not an extension. |
| D-4 | Do I need a `timeout`, a retry, or a second server anywhere in the emitted document? | No, and emitting one fails outright. `timeout` is rejected by the real `sing-box check` on a DNS server, at the `dns` block level and on a DNS rule, with a bogus-key control proving the decoder rejects unknown fields (M-1); the rule chain never falls through on failure, `NXDOMAIN` or `SERVFAIL` (M-2); `dns.final` is the no-rule-matched routing default only (M-4). K-17, K-13 and I-16 bind this, and V-11 checks both halves. |
| D-5 | `sc ipv6 show` must not write — do I add it to `main()`'s read-only opt-out arm? | No (K-10). The opt-out names `doctor` positively so that a *new* command inherits initialising behaviour by default, and `doctor` stays the one member. The no-write property is a property of `cmd_ipv6()`, not of the command; C-4 scopes every claim about it accordingly. |
| D-6 | Which user-facing strings may I add, and in which languages? | Exactly Q-15's ten, verbatim in both languages, plus reuse of the existing `Reload failed` (`bin/sc:132`) and `Cannot use {path}: {problem}` (`bin/sc:252`). `TRANSLATIONS` has no `en` table, so the key **is** the English output and must read as prose; no new `zh` string may contain `失败：` (`bin/sc:160`, a load-bearing diagnostic grep); no key may take the `ls.*` namespaced shape. |

## Verdict

**APPROVED WITH CONDITIONS** — stage 4 may begin as designed; conditions C-1 … C-11 are binding on stages 4 and 6, dimension 7's FAIL is discharged by C-1, C-2, C-4 and C-8 (all text-level corrections that change no interface and no code), and no rollback to stage 1 or stage 2 is required.
