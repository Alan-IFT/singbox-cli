> Rationale portion for 03_GATE_REVIEW.md. Non-binding.

## Running B-3's two checks myself — how F-1 was discharged

I read the 425-line template end to end again rather than trusting the round-2 summary, and then did what a reader of rule 80 would do: took B-3's bytes, built each check's fixture on paper against that text, and recorded the verdict and the action.

**Metric row.** The check is "archive a fixture whose index is at the cap with ≥1 harvested insight, then `wc -l` the resulting index". The AC-1 fixture is 8 non-bullet lines + 22 single-line entries + 3 harvested bullets. In 0.47.0 the index scan classifies the 8 leading lines as header (`:107-135`; the HTML comment on the real index's lines 7-8 opens and closes inside the header, so `cs` returns to 0 and the entry set is the 22 bullets), giving `idx_entries=22`; the section scan gives `h_entries=3`; `total_after=25` at `:333` is not `> 30`, so `:337-341` sets no rotation and the run appends at `:397-401`. Result: 33 lines. Verdict **lost**, exactly as the design claims, and the number is the same 33 the vendored script produces for the same reason — the entries-vs-lines gap.

**Join row.** The check is "archive a fixture whose `## Insight` bullet wraps over three lines with the tag on the last, then read what it wrote". `RE_HEADING` does not match a continuation line, `in_entry` is 1, so lines 2 and 3 are `C` and `SCAN_E_HI` is extended (`:161-165`); the harvest loop copies every line of the entry (`:299-303`). Continuation text and the `· evidence:` tag both arrive. Verdict **already provided**, action *change nothing*.

**Then the observables.** AC-13's first is the AC-1 fixture at ≤30 lines after the *lost* action. Applying the action to that text — decision on `wc -l`, rotate until the written file is ≤30 — gives `30 + 3 − 30 = 3` entries rotated, and the rewrite emits `printf '%s\n'` per line with the header taken from the scanned range (`:387-393`), so the file lands at exactly 30, not 31: the two shapes that cost the vendored script a line are already fixed upstream. AC-13's second observable is the tag reaching the index, which the *already provided* verdict is. Neither step opens an in-file note and neither re-diagnoses a defect. F-1 is discharged.

**BC-13, read literally.** B-3 names no version, states no verdict as standing fact, and its `git checkout -- <path>` is gone; `git log -p -- <path>` survives as reference material for the join row's *lost* action, which needs the prior awk to restore a join. That is a source, not a mechanism, and it earns its line.

**What the drill does not cover, and why it is a MINOR.** The metric check's `≤30 → change nothing` is a property of the file, not of the run. 0.47.0 refuses at `:353-357` on any unclassifiable line and writes nothing, which leaves the index at 30 and reads as *already provided*. Today's fixtures do not trip it, so AC-13 is discriminating as written; C-12 closes it for the version that eventually arrives, and it does not need B-3's bytes to change.

## RS-10, re-derived rather than confirmed

The architect asked the gate to confirm a self-scored call, so I routed the seven kinds myself before reading its table.

The test is one question with a priority order: *does a later stage have to satisfy, implement or verify this unit?* Yes wins; the No branch's verbs (explains, justifies, measures, compares) only decide what the Yes branch did not take. Applied:

- `## Smaller alternative rejected` — rule 85:42 says "stage 3 must test that answer", so a later stage verifies it → contract. Matches its witnesses (`doctor-rows-establish-their-fact/02_SOLUTION_DESIGN.md:216`, `ruleset-staleness-visibility/02_SOLUTION_DESIGN.md:150`). Note that this routing comes from the **test**, not from precedence: no rule and no agent contract names the section (F-14).
- Coverage table → contract; the gate discharges it row by row (dimension 2 above is exactly that work). Witness `ruleset-staleness-visibility/02_SOLUTION_DESIGN.md:237`.
- Per-edit size table → contract by the most-recently-delivered rule (`doctor-rows-establish-their-fact/02_SOLUTION_DESIGN.md:253` over `ruleset-staleness-visibility/02_RATIONALE.md:119`) — and worth recording that T-27 itself splits it, keeping per-row `Est.` in the ledger cells and the aggregate in `02_RATIONALE.md ## Size, against the bar`. A shipped row reading "→ contract" would have been half-wrong for the very next task, which is a second, independent argument for deleting the list.
- Evidence, re-verification record, rejected reading → rationale, on the No branch, matching all five cited witnesses.
- **Measurement obligation → contract.** Here I part company with `02_RATIONALE.md`. Its witness, `doctor-rows-establish-their-fact/01_REQUIREMENT_ANALYSIS.md:215`, is T-26's OQ-11 — a schema-gap row **in the contract** saying where the *record* of inherited claims went. The obligations themselves are BC-10, OQ-8 and AC-1/5/9/10, all in the contract (`:212`), and T-26's `01_RATIONALE.md:5-10` says so in its own words: "routed to a later stage (contract OQ-8, BC-10, AC-1/AC-5/AC-9/AC-10)". Nor does precedence reach it: the analyst contract's rationale portion names "the evidence narrative and measurements" (0.47.0 `agents/requirement-analyst.md:34-39`) — a taken measurement, not an obligation to take one — while `## Boundary conditions` and `## Acceptance criteria` are precisely the declared shapes for an obligation on a later stage.

So: seven of seven route correctly under the test, the deletion stands, and rule 85's preferred outcome is the right one — but round 1's table row was **right** where the round-2 narrative calls it wrong, and RS-10's stated fallback (`measurement obligation → rationale`) is the one reading that contradicts practice. C-11 binds the corrected routing so V-9(b) does not re-open this at stage 6 and reinstate a false row under RS-2.

## The third divergence shape (F-13)

I tested I-1's corrected sentence rather than accepting it. Let H be non-bullet lines, B bullets. The decision counts `wc -l = H + B`; the rewrite emits `echo "$header"` + remaining + harvested. Command substitution strips **trailing** newlines, so if the file's last line is a non-bullet line — a trailing blank is the ordinary case — `header` holds `H − k` lines and the rewrite emits `k` fewer lines than the decision counted, deleting those `k` lines from the index. That file ends with a newline and has ≥1 non-bullet line, so it satisfies I-1's stated `iff` and still diverges.

Direction matters for severity: this one lands the file **under** the cap, so F.4 PASSes and no criterion mis-certifies; what it costs is content, against FR-2's header clause. It is not present in this repository (`.harness/insight-index.md:30` is a bullet, and the file ends with a newline), so AC-15's delivery run is unaffected — the same argument the architect used for the first two shapes, and it holds here for the same reason. That is why C-3 adds a fixture rather than sending the design back: the repair still lives inside the frozen rewrite, still costs an algorithm change under AC-16, and still belongs to the OQ-11 adoption row, where `printf '%s\n'` and the scanned header range already fix all three.

I also checked the fourth variant — a non-bullet line **between** two bullets. `grep -v` hoists it to the top, so the line count is preserved and only the order changes; V-2's byte comparison of the header would surface it if a fixture carried one. No extra fixture is needed for it.

## Rule 85, re-tested on the round-2 text

The round-2 diff is ≈ +59/−6 with 8 executable added lines, three H2 sections deleted from `02` and 12 lines removed from the policy texts. Testing the answers rather than accepting them:

- **B-1 at 18 lines.** The ten-line table is gone and I re-derived the authorisation for its deletion independently (above). What remains is four parts FR-6(a)-(d) each mandate. One line inside it still does not earn itself — clause (d)'s home enumeration, which names two homes and omits the two that stages 1 and 3 are actually required to use (F-16). Deleting it makes the clause both shorter and true, which is the outcome this rule ranks first. That is my one recommended deletion.
- **B-2 at 13 lines** against a ≤15 budget, with F-7's redundant bullet gone. I re-derived the list against the tree: `docs/batches/{_template,default,followups}` all exist and are covered by the glob; `.harness/operator-obligations.md`, `.harness/rejected-decisions.md`, `CONTEXT.md` exist. Two paths are outside it and outside the product half — `evals/golden-tasks.md` and `.harness/decision-rubric.md` — but neither is written by every delivery, and V-10/RS-3 is the mechanism for adding one if a real delivery commit carries it. No finding.
- **B-3 at 14 lines, unbudgeted, and honestly declared as such.** I tried to cut it. The mechanism sentence tells the reader what event they are standing in; "keep what arrives" is the whole F-1 fix; the version-naming sentence is BC-13; the `git log -p` line is what the join row's *lost* action needs. The five-column table is two rows of the smallest record that can hand a reader a verdict and an action. It earns its lines. What it does not earn is the **universal quantifier** in its first sentence (F-11) — the one place where the round-2 text still asserts something the code contradicts.
- **The document itself.** Three invented sections are gone, and I verified the re-homing lost nothing: all 40 requirement ids reach the verification plan's fourth column, and the per-row `Est.` figures carry the size mapping. One undeclared H2 remains, on a false precedence claim (F-14) — one line of E-5, not a design position.

## OQ-11 tested, not overturned

The four reasons hold against the artifacts. (1) 0.47.0 still decides on entries (`:333`) against an F.4 that caps lines, and there an entry may span several lines, so the divergence is genuinely wider — the metric edit still has to be made, on 425 unfamiliar lines. (2) The report strings, the exit-3 refusal path and the multi-line entry model would force AC-1…AC-7 and AC-16 to be re-derived; AC-16's "metric, not algorithm" bar could not survive it. (3) `refresh_set` at `upgrade-project.sh:186-194` carries `archive-task.ps1` — frozen by OQ-4 — and, understated by 01, also `guard-rm.{sh,ps1}` which OQ-1 froze, plus `harness-sync`, `install-hooks`, `migrate-scripts-layout` and the ambient pair; the mechanism really cannot be taken selectively. (4) It is an owner/PM action mid-pipeline. I record one consequence for the pool row: whoever runs it inherits a `guard-rm` refresh as well, which is a second frozen decision to re-open.

## Verified good — checked this round, not carried over

- All 40 requirement ids (FR-1…11, BC-1…13, AC-1…16) reach `## Verification plan`'s fourth column; I partitioned them one by one, so the deletion of `## Requirement coverage` cost nothing binding.
- The size arithmetic is real: B-1 is 18 lines, B-2 13, B-3 14 as counted in the fenced blocks; rule 70 is 91 lines today → ~111 against AC-10's ≤130; rule 80 is 53 → ~83 against F.2's 200. The `## Adversarial check` and `### Rule 4` anchors E-2 places between both exist (`70-doc-size.md:64`, `:79`), as does rule 80's `## Reporting` (`:49`) and `## The policy` (`:12-24`).
- The E-1 arithmetic on the real index: 30 lines today, so this task's own delivery run rotates exactly as many entries as it harvests and lands back at 30 — AC-15 is reachable, not aspirational.
- AC-7's `Rotated 3` is emitted on the dry-run path because `rotated` is filled at `:97-99`, outside the `[[ "$DRY_RUN" == false ]]` guard at `:105`; the criterion measures a string the script actually prints.
- The insight index contradicts nothing here; its one methodological entry (2026-08-15, `verify_all` false red from a subdirectory, line 17) is carried as BC-10, K-10 and C-8.
- No credential byte, no share URL, no live-host action, no `bin/sc` import appears anywhere in 01 or 02; `.claude/**`, `CLAUDE.md` and `.github/copilot-instructions.md` are in the frozen set; B.2's scope is frozen; F.4 is byte-identical by K-6 and AC-8.

## Rejected readings

- *"F-11 is pedantry — the table is about `archive-task.sh`."* The table is; the sentence is not. It lands in a rule fragment as a general statement about `/harness-upgrade`, and its "no marker preservation, no backup" clause is exactly the fact a reader would rely on when deciding whether their custom `B.*` checks survive an upgrade. That reader would be wrong, in a file this task is adding to close the class of harness artifacts that state something untrue.
- *"F-13 should be a rollback, because I-1 is still false."* The direction decides it. The two shapes stage 2 named leave the file over the cap and F.4 WARNing; this one leaves it under. Nothing mis-certifies, nothing in this repository's index has the shape, and the repair is inside a range frozen by AC-16 for reasons that did not change. A measured fixture and a corrected residual statement is the whole remedy.
- *"F-12 means the deletion was wrong and the table must come back."* The opposite: the corrected routing is one the bare test produces unaided, so it is one more kind that needs no row. The finding is against the derivation and against RS-10's fallback instruction, not against the deletion.
