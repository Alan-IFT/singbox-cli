> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | PASS | FR-1…FR-11, BC-1…BC-13 and AC-1…AC-16 each name an observable I could measure or compute from the shipped artifacts, and the four rewritten criteria now separate HEAD from the candidate by construction rather than by reference to their own run — AC-7 on an absolute `Rotated 3` against HEAD's `Rotated 0` (`archive-task.sh:139` prints `${#rotated[@]}`, populated outside the dry-run guard at `:97-99`), AC-5 on the arithmetic identity `report == wc -l − 30` which I verified closes on both of its fixtures, AC-9 on a two-clause test priced against archived practice, and AC-13 on two observables a record can actually deliver. |
| 2 | Design completeness | PASS | Every one of the 40 requirement ids reaches a verification step: I partitioned FR-1…11, BC-1…13 and AC-1…16 against the fourth column of `02_SOLUTION_DESIGN.md` `## Verification plan` (V-1…V-16) and found zero unreached ids, so the coverage mapping survived the deletion of `## Requirement coverage` intact; each edit id carries a one-command rollback and each landing in `## Migration & edit sequence` is gated on a measurement taken first. |
| 3 | Reuse correctness | WARN | Every code citation resolves as claimed — `archive-task.sh:27/:44-77/:82/:89-94/:105-132`, `verify_all.sh:213-219`, `upgrade-project.sh:56/:186-194/:210-226`, `85-design-discipline.md:41`, `70-doc-size.md:26/:64/:79`, `80-delivery-policy.md:6-9/:12-24/:49`, `AI-GUIDE.md:30`, `.gitignore:19` — and the 425-line template's five cited line ranges are exact, but B-3's opening sentence generalises `/harness-upgrade`'s replacement to "every vendored script … no marker preservation, no backup", which `upgrade-project.sh:136-141` and `:548-556` contradict for `verify_all.{sh,ps1}` (F-11). |
| 4 | Risk coverage | WARN | R-1…R-10 name the risks that actually bite here — the `set -u` clamp death, the fixture run against the real repository, the refresh that replaces rather than reverts, the vacuous drill, the flipped verdict, the deleted-table regression — and each mitigation is a step in the plan rather than an intention; the one unnamed risk is that B-3's metric check reads a **refusing** run (0.47.0 exits 3 writing nothing, `:353-357`) as *already provided* (F-15). |
| 5 | Migration safety | PASS | No data migration, no format change, no flag, no user action: each edit id has a `git checkout -- <path>` rollback, the whole task is one revertible commit, and an index already at the cap (today exactly 30 lines, `\.harness/insight-index.md:1-30`) is brought to the cap by the next run — I computed that run: 30 lines + h harvested rotates exactly h entries and lands back at 30. |
| 6 | Boundary handling | WARN | BC-1…BC-13 cover absent, empty, at-cap, over-cap, unrotatable, clamp-to-zero, hostile-character, dry-run and version-drift shapes, and V-16 adds the two shapes where the frozen rewrite out-counts the decision — but a third shape satisfies I-1's stated `iff` condition and still diverges, in the opposite direction and with silent content loss (F-13). |
| 7 | Test feasibility | PASS | All eight declared-discriminating criteria separate the two texts, each verified at this gate rather than transcribed (see PQ-1), and AC-13 — unsatisfiable in round 1 — is now satisfiable: I ran B-3's two checks against harness-kit 0.47.0's template myself and they return *lost* and *already provided*, after which both AC-13 observables are reachable (PQ-5). |
| 8 | Out-of-scope clarity | PASS | Ten exclusions in 01 and eight in 02 draw the boundary by name, the frozen set names files *and* line ranges, K-1…K-14 are prohibitions with actors, and OQ-11's four reasons for excluding the 0.47.0 adoption each hold against the artifacts — reason (3) is in fact understated, since `refresh_set` (`upgrade-project.sh:186-194`) also refreshes `guard-rm.{sh,ps1}` that OQ-1 froze, plus five further pairs. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| F-11 | MAJOR | `02_SOLUTION_DESIGN.md` B-3, first sentence (lands in `.harness/rules/80-delivery-policy.md`) · owner: stage 4 under C-10 | The claim "`/harness-upgrade` **replaces** every vendored script that differs from the plugin's current template with that template … no marker preservation, no backup" is false of `verify_all.{sh,ps1}`, which `upgrade-project.sh:136-141` and `:182-185` deliberately exclude from `refresh_set` and which S5 handles by marker-preserving splice, a HALT on custom `B.*` checks and a timestamped `.bak` (`:548-556`) — true of `archive-task.sh`, over-general as a rule-80 sentence, and this task's own subject is harness artifacts that state something untrue. |
| F-12 | MAJOR | `02_RATIONALE.md` `## AC-9(b) — every FR-6(c) kind against its witness`, row 7 · `02_SOLUTION_DESIGN.md` RS-10 · owner: stage 4/6 under C-11 | The measurement-obligation witness `doctor-rows-establish-their-fact/01_REQUIREMENT_ANALYSIS.md:215` is a schema-gap row **in the contract** recording where a *record* went, not an instance of the kind; T-26's actual measurement obligations are BC-10, OQ-8 and AC-1/5/9/10 — all contract, and `01_RATIONALE.md:5-10` states that routing explicitly — so the kind routes **contract** under FR-6(a)'s bare test with no precedence needed, and RS-10's stated fallback row `measurement obligation → rationale` would install a routing this project's practice contradicts. |
| F-13 | MAJOR | `02_SOLUTION_DESIGN.md` I-1 invariant, `## Out of scope` 8, RS-9, V-16 · owner: stage 4/6 under C-3 | I-1's `iff` is not exhaustive: an index whose last line is a **non-bullet** line (a trailing blank) ends with a newline and holds ≥1 non-bullet line, yet `header=$(grep -vE …)` at `archive-task.sh:114` loses trailing blank lines to command substitution, so the rewrite emits **fewer** lines than the decision counted and deletes those lines from the index — the opposite direction from the two named shapes (F.4 still PASSes) but the only one that silently drops content, against FR-2's "the index's header lines survive unchanged". |
| F-14 | MINOR | `02_SOLUTION_DESIGN.md` E-5 and `## Smaller alternative rejected` header note (`:276-277`) · owner: stage 5 under C-6 | The claim that `.harness/rules/85-design-discipline.md:41` "names it in `02_SOLUTION_DESIGN.md` by name" is false — `:41` names no section and no document, `:79` names `02_SOLUTION_DESIGN.md` only for a consolidation/split decision, and harness-kit 0.47.0 `agents/solution-architect.md:20-31` does not declare the section — so the one remaining undeclared H2 in `02` is admitted on a wrong basis, though it is recorded in E-5's gap row and routes to the contract anyway under B-1's test (rule 85:42 obliges stage 3 to verify it). |
| F-15 | MINOR | `02_SOLUTION_DESIGN.md` B-3, metric row's check column · owner: stage 4/6 under C-12 | The check "archive a fixture …, then `wc -l` the resulting index; ≤30 → change nothing" has no completion clause, so a text that refuses and writes nothing — 0.47.0 exits 3 on any unclassifiable line in either document (`:336`, `:353-357`) — leaves the index at ≤30 and reads as *already provided*, which is the one way the record can hand a reader a wrong verdict. |
| F-16 | MINOR | `02_SOLUTION_DESIGN.md` B-1 clause (d) (lands in `.harness/rules/70-doc-size.md`) · owner: stage 4 under C-14 | The home enumeration "in stage 2's change ledger, or in the stage's own findings/residuals table" names no home for a stage-1 author, whose own contract prescribes a `## Resolved questions` row (0.47.0 `agents/requirement-analyst.md:29-32`), nor for stage 3, whose contract prescribes a `## Findings` row — the precedence sentence two paragraphs above rescues both, which is precisely why the enumeration does not earn its line. |
| F-17 | MINOR | `02_SOLUTION_DESIGN.md` E-2 (the `.harness/insight-index.md` caps-table cell) · owner: stage 4 under C-13 | E-2 rewrites one cell of `70-doc-size.md:26` — a rule fragment's bytes — but no byte-form row specifies its text (B-1/B-2/B-3 cover only the three new sections) while K-12 forbids the implementer improvising policy wording, so the one edited rule sentence in this task with no designated author is the one that must point a reader at the durability record. |
| F-10 | MINOR | `03_GATE_REVIEW.md` schema (gate-reviewer contract) · owner: none — recorded, not fixed | The dispatch requires a per-criterion discriminating verdict, an RS-10 ruling and a rule-85 re-test, none of which fits a declared section of this document, and `.harness/rules/70-doc-size.md` still declares no `## Stage-doc boundary rule` at HEAD — the nineteenth recorded confirmation of R-37, filed here as a schema-gap row; the units were given the destinations PQ-1 (verdict table), PQ-9 (RS-10 ruling), C-11 (its binding half) and `03_RATIONALE.md` (rule-85 re-test), and no section was invented. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| C-3 | **Three** divergence fixtures are built and measured, all over the cap, `wc -l` quoted before and after each: (i) final line with no trailing newline; (ii) zero non-bullet lines; (iii) **final line a non-bullet line** (F-13). Each result is reported as a residual against FR-1 — never repaired by a hand edit and never by touching `archive-task.sh:105-132` — and RS-9 carries all three, with (iii) stated as a **content** loss (the trailing line disappears) rather than only a count divergence. If (iii) also reorders a mid-file non-bullet line, that observation is recorded in the same row. | developer (04), qa-tester (06) | `04_DEVELOPMENT.md` / `06_TEST_REPORT.md`, three fixtures with both counts quoted; `07_DELIVERY.md` pool row |
| C-6 | Every section of every stage document this task writes from here on is either a shape its own agent contract declares or is recorded in a schema-gap row whose stated basis is **true**; specifically `## Smaller alternative rejected` in `02_SOLUTION_DESIGN.md` is treated as a gap-row unit, not as a section named by precedence (F-14). Stage 5 confirms that no unit of stages 4-7 has two destinations or none under B-1's shipped text (BC-12). | code-reviewer (05) | `05_CODE_REVIEW.md`, explicit per-section check |
| C-7 | This task's delivery-time archive run is executed **once**; if it leaves `.harness/insight-index.md` over 30 lines the failure is reported against AC-1/AC-15. No hand-rotation, no second run, no edit to the index or to `insight-history.md` between the script's exit and `git add` — the two sha256 pairs are the observation. | PM (07) | `07_DELIVERY.md`, both digest pairs quoted |
| C-8 | Every reported `verify_all` count comes from `bash .harness/scripts/verify_all.sh` executed in `/home/alan/Programs/singbox-cli`; the expected result is **PASS 17 / WARN 0 / FAIL 0 / SKIP 1**, and `git diff -- .harness/scripts/verify_all.sh` is empty at every stage that reports a count. | developer (04), qa-tester (06) | `04_DEVELOPMENT.md`, `06_TEST_REPORT.md`, command line quoted |
| C-9 | Every fixture lives under `test/t27/` (ignored by `.gitignore:19`) and every run executes the **fixture's own copy** of the script; no stage runs `.harness/scripts/archive-task.sh` against this repository's `docs/features/**` or `.harness/insight-index.md` before C-7's single delivery run; `HARNESS_ALLOW_OUTSIDE_RM` is never set; the 0.47.0 template is copied **read-only** into `test/t27/refresh/` and no byte of it enters `.harness/` (K-14). | developer (04), qa-tester (06) | `04_DEVELOPMENT.md` fixture note; `06_TEST_REPORT.md`; `git status --porcelain` for `test/` |
| C-10 | B-3's opening sentence lands **true of `upgrade-project.sh`**: the wholesale-replacement claim (and its "no marker preservation, no backup" clause) is scoped to the scripts `refresh_set` names (`:186-194`), and the sentence does not assert it of `verify_all.{sh,ps1}`, which `:136-141` excludes and `:548-556` protects by splice, HALT and timestamped `.bak` (F-11). This condition authorises that bounded wording change and overrides K-12 for this clause only; nothing else of B-3 is re-worded, and the landed bytes are quoted verbatim in `04_DEVELOPMENT.md`. | developer (04), code-reviewer (05) | `04_DEVELOPMENT.md` quoting the landed section; `05_CODE_REVIEW.md` re-reading it against `upgrade-project.sh:136-141,186-194,548-556` |
| C-11 | RS-10 is ruled: **B-1 ships without the per-kind list** (see PQ-9). V-9(b) is run against the corrected routing — `measurement obligation → **contract**`, witnessed by T-26's BC-10/OQ-8 at `doctor-rows-establish-their-fact/01_REQUIREMENT_ANALYSIS.md:212` and confirmed by that task's `01_RATIONALE.md:5-10` (F-12) — and if any contradiction reinstates rows under RS-2/BC-12, that is the row that ships. No stage records `measurement obligation → rationale`. | developer (04), qa-tester (06) | `04_DEVELOPMENT.md` V-9(b) table; `06_TEST_REPORT.md` |
| C-12 | Every execution of a B-3 check records the command, the **exit status** and the number it produced; an exit status other than 0 is reported as "the check did not complete" and never read as a verdict (F-15). V-12 states both exit statuses alongside both `wc -l` values and the grep result. | developer (04), qa-tester (06) | `04_DEVELOPMENT.md` / `06_TEST_REPORT.md`, exit statuses quoted |
| C-13 | The rewritten `.harness/insight-index.md` cell of `70-doc-size.md:26` is quoted verbatim in `04_DEVELOPMENT.md` and satisfies three properties: it names the cap's measurement (the file's line count), it points at rule 80's `## Local fixes to plugin-vendored scripts` for the case where F.4 still WARNs after an archive run, and it asserts no verdict about any template version (BC-13). The cell adds no line and the caps table is otherwise untouched (F-17). | developer (04), code-reviewer (05) | `04_DEVELOPMENT.md` quoting the cell; `05_CODE_REVIEW.md` checking the three properties |
| C-14 | B-1's clause (d) does not ship naming a set of homes that excludes the home a stage's own contract prescribes (F-16). The gate's recommended discharge is deleting the em-dash enumeration "— in stage 2's change ledger, or in the stage's own findings/residuals table —", leaving the precedence sentence to decide the shape and clause (d) to decide the duty; if the enumeration is kept instead, stage 5 verifies that each of stages 1-7 has a home in it. Either way B-1 stays ≤35 lines. | developer (04), code-reviewer (05) | `04_DEVELOPMENT.md` quoting the landed clause; `05_CODE_REVIEW.md` |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| PQ-1 | Which of the eight declared-discriminating criteria actually separate HEAD from the candidate under the round-2 wording? | All eight, none NOT-DISCRIMINATING. **AC-1** — HEAD counts 22 + 3 = 25 bullets ≤ 30 (`archive-task.sh:89-94`), rotates nothing, ends at 33 lines; candidate rotates 3 and ends at 30. **AC-5** — HEAD has no residual `echo` anywhere in the file and rotates nothing; the identity closes on both fixtures (header 30 + 2 entries + 1 harvested → rotate 2, file 31, residual 1; header-only 32 + 1 harvested → clamp 0, file 33, residual 3). **AC-7** — candidate reports `Rotated 3` because `rotated` is populated at `:97-99` outside the `DRY_RUN` guard at `:105`; HEAD reports `Rotated 0`. **AC-9** — rule 70 has no such section at HEAD (91 lines: `## What this is`, `## When to read this`, `## Caps`, `## Process discipline`, `## Adversarial check`), and clause (b) now also separates the candidate from a degenerate "everything → contract" rule, which would mis-route the evidence section and the rejected reading. **AC-11** — `ruleset-staleness-visibility/01_REQUIREMENT_ANALYSIS.md:149` omits `docs/batches/**`, `CONTEXT.md` and `.harness/operator-obligations.md`, all three of which exist. **AC-12** — `80-delivery-policy.md:6-9` and `AI-GUIDE.md:30` are both delivery-time only. **AC-13** — see PQ-5. **AC-15** — the index stands at exactly 30 lines today (8 non-bullet + 22 bullets), so at HEAD one harvested insight gives 31 lines at 23 bullets, no rotation, F.4 WARN, hand-rotation. |
| PQ-2 | Does the residual over-cap line still print when the clamp reduces `rotate_count` to zero? | Yes, and the number is `total_after − 30 − rotate_count` **after** clamping, which is the same digit as `wc -l` of the resulting index minus 30 in both AC-5 fixtures (checked above). Print it on the clamp condition, not inside the `rotate_count > 0` branch — when the clamp reaches zero, K-3 routes the run through the existing `elif` and the line is the run's only signal that the file is over the cap. |
| PQ-3 | Where does the `wc -l` go relative to the missing-index `touch` at `:82`? | Before the write phase, behind `[[ -f "$insight_index" ]]`, yielding 0 when the file is absent, never after a `touch` the dry-run path must not perform. Use `wc -l < "$insight_index"` — byte-for-byte the form F.4 uses at `verify_all.sh:215`. With E-1b landed (unconditional) the dry-run path reaches this point instead of aborting under `set -e`. |
| PQ-4 | Can I run the repository's own `archive-task.sh` with a fixture `--task`? | No. The script derives its root from its **own** location (`:27`), so `$insight_index` resolves to this repository's index whatever `--task` says, and one wet run consumes the single observation AC-15 exists for. Copy the candidate into `test/t27/<case>/.harness/scripts/` and run that copy (K-9, C-9). |
| PQ-5 | I ran B-3's two checks against the arriving text — what do they return, and does the drill then reach AC-13's two observables? | Measured at this gate against harness-kit **0.47.0**'s `skills/harness-init/templates/common/.harness/scripts/archive-task.sh` (425 lines, resolved through `upgrade-project.sh:56`). **Metric row → *lost***: the AC-1 fixture gives `idx_entries` 22 + `h_entries` 3 = 25, not > 30 (`:333`, `:337`), so it takes the append path at `:397-401` and the index ends at **33 lines**. **Join row → *already provided***: the wrapped bullet's continuation lines are classified `C` and the entry range extended (`:161-165`), and every line of the entry is harvested (`:299-303`), so the continuation text and the trailing `· evidence:` tag both reach the index. After the metric row's action — make the decision read `wc -l` and rotate until the written file is ≤30 — the AC-1 fixture lands at exactly 30 lines (that text emits with `printf '%s\n'` and re-emits the header from its scanned range, `:387-393`, so there is no off-by-one), and the AC-6 fixture's tag arrives. Both AC-13 observables hold. Record the version and both exit statuses (C-12). |
| PQ-6 | B-3's metric check says "a fixture whose index is at the cap" — at the cap in lines or in entries? | In **lines**, by `wc -l`: that is how the cap is stated in every document that defines it (`70-doc-size.md:26`, `05-insight-index.md:5,29,48`) and what F.4 measures. Use the AC-1 fixture unchanged. Against 0.47.0 both readings happen to give the same verdict, so the drill is not sensitive to it today. |
| PQ-7 | What does the *lost* action mean on a program whose entries span several lines — am I supposed to transcribe E-1's patch into it? | No. The action is stated as a metric, not a patch (FR-5, OQ-6): make the rotation decision read `wc -l` of the index and rotate until the file it writes is ≤30 lines. In a multi-line-entry program that means rotating **entries until the emitted line count** falls to the cap, not reusing `total_after = entries + harvested`. That is a bounded edit onto the arriving text; nothing about the defect is re-diagnosed and nothing arriving is discarded. |
| PQ-8 | Do BC-6's hostile characters survive the rotation rewrite? | Through `echo`, yes — bash's builtin performs no escape interpretation without `-e` or `xpg_echo`, so `- a\b`, `- $x` and `- -n foo` round-trip as bytes. The hazard on that path is the **header**, not the entry: `header=$(grep -vE …)` hoists every non-bullet line to the top and drops trailing blank ones (F-13, C-3). |
| PQ-9 | RS-10 asks the gate to confirm that B-1 can ship without the per-kind list — does it? | **Yes — the list does not ship.** Re-derived independently against the seven witnesses: `## Smaller alternative rejected` → contract (a later stage must verify it: `85-design-discipline.md:42`), coverage table → contract, per-edit size table → contract by the most-recent rule, evidence → rationale, re-verification record → rationale, rejected reading → rationale, and **measurement obligation → contract** (F-12), which the bare test also gives. Seven of seven route correctly on FR-6(a)'s test with FR-6(b) applied first, so AC-9(b)'s final clause fires and the ten lines stay deleted — but note the routing that authorises the deletion is not the one `02_RATIONALE.md` row 7 states, and C-11 binds the corrected one for V-9(b). |

## Verdict

APPROVED FOR DEVELOPMENT — with binding conditions C-3, C-6…C-14; zero blockers remain, F-1's ruling having been re-derived by stage 2 and B-3's two checks reproducing their claimed verdicts (*lost*, *already provided*) when run first-hand at this gate against harness-kit 0.47.0's template.
