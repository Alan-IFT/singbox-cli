# Delivery Summary

## Summary

- Task: **T-27 / `harness-self-maintenance`** — fix the three harness defects that tax every future task: `archive-task.sh`'s rotation counting bullets where `verify_all` F.4 counts lines, rule 70 declaring no stage-doc boundary rule, and the committed-diff AC template omitting `docs/batches/**`.
- Mode: **full** (7 stages), pool `followups`, dispatched by `/harness-batch` under deferred-human mode.
- Stages traversed: 1 (2026-08-15, rounds 1-2) → 2 (rounds 1-2) → 3 (rounds 1-2) → 4 (2026-08-16, rounds 1-3) → 5 (rounds 1-2) → 6 (round 1) → 7. **Twelve stage transitions**, all recorded in `PM_LOG.md`.
- Rollbacks: **1** — stage 3 → stage 2 (with four clauses routed to stage 1 first, since downstream may not edit upstream). Cause: the gate found that the durability ruling had been derived **without opening the artifact `/harness-upgrade` actually copies**. `upgrade-project.sh:56` resolves it to harness-kit 0.47.0's **425-line rewrite**, not a near-copy of the vendored 151-line script — so `git checkout -- <path>` as the restore path would have discarded four upstream fixes, and one of the two prescribed re-applications was for a defect the arriving text does not reintroduce. Shipping that would have put two false instructions into a rule fragment, which is the exact defect class this task exists to close. Plus **two changes-requested rounds at stage 5** (not rollbacks): CR-1 (a completion clause B-3 needed in its own bytes) and RES-10 (stale figures in stage-4 documents).
- Final `verify_all` result: **PASS 17 / WARN 0 / FAIL 0 / SKIP 1** — identical to the baseline measured at `d849234`. Run from the repository root (`bash .harness/scripts/verify_all.sh`); a run from any subdirectory self-reports a false red.
- Baseline changes: **none**. `baseline.json` is untouched — its `test_count` has read 0 for seventeen deliveries, and raising it would invent a convention and add a fifth path to a diff pinned at four (R-4/R-9, T-28's scope).
- Outstanding risks: three measured index shapes on which the rewrite's `grep -v` header filter still diverges from the decision, **reported as residuals and deliberately not repaired** (the repair lies inside a range AC-16 froze as an algorithm change). One of them, R-89, loses a line and reorders another **while F.4 passes over it**. None occurs in this repository's index, whose 8 non-bullet lines all lead and whose final line is a newline-terminated bullet — a property that is now load-bearing and unguarded. Filed as R-89/R-90/R-92 for whoever next opens `archive-task.sh:109-136`.
- Files changed: **4 product files, +65 / −6**, of which **8 added lines are executable** — `.harness/scripts/archive-task.sh` `8/4`, `.harness/rules/70-doc-size.md` `20/1`, `.harness/rules/80-delivery-policy.md` `36/0`, `AI-GUIDE.md` `1/1`. Plus the delivery-time ledger writes. **`bin/sc` has no diff**, as required; nor do `install.sh`, `uninstall.sh`, both READMEs, `CHANGELOG.md`, `verify_all.{sh,ps1}`, `guard-rm.{sh,ps1}`, `archive-task.ps1` or anything under `.claude/`.
- Rows closed: **R-18, R-36, R-37** — see below. Rows filed: **R-86 … R-92**.
- Next steps for user: nothing is required. Two items are **yours, not any agent's** — **R-87** (adopting the upstream `archive-task.sh` refresh, which fixes four things this copy still gets wrong but *does not* fix R-18, and which cannot be taken selectively because `refresh_set` also re-lands the frozen `archive-task.ps1` and `guard-rm.*`), and **R-88** (`task-state.js` / `entropy-cadence` are absent on this host, so the delivery-time entropy watch resolves NOT-DUE here indefinitely).

### The three rows, and what closing them actually took

**R-18 — CLOSED, and this delivery's own archive run is the proof.** Confirmed sixteen times, once per delivery, each paying a hand rotation. The cause was exact as filed and survived first-hand re-verification: `archive-task.sh:89-94` counted **bullets** against 30 while F.4 (`verify_all.sh:213-219`) counts **lines**, so on any index with a header the branch could never fire before F.4 warned. The fix makes the script take **the same measurement F.4 takes** — `wc -l < "$insight_index"`, the identical form at `verify_all.sh:215` — rather than tuning a threshold. **F.4 is byte-identical**: stage 1 ruled (OQ-5) that the script is the deviant, since both documents defining the cap state it in lines, and changing F.4 to match the script would have been weakening a check to make a defect pass. The smaller alternative — add the header size as a constant (`+ 8`, zero added lines) — was rejected at stage 2 and the rejection **tested** at stage 3: the count is a whole-file `grep -v`, so any stray non-bullet line anywhere shifts it, not just a header edit. Upstream reached the same conclusion independently.

**R-37 — CLOSED after nineteen recorded confirmations.** `.harness/rules/70-doc-size.md:80-96` now declares `## Stage-doc boundary rule`. The notable part is what is **not** in it: a per-kind routing table was designed, then priced by a strengthened AC-9(b) requiring each recurring unit kind to route to the portion an archived instance of that kind actually occupied — all seven did so under the bare test plus the precedence clause, so the ten lines earned nothing and were deleted. The gate re-derived that ruling independently rather than accepting it, and **corrected a row while doing so** (`measurement obligation → contract`, witnessed by T-26's own BC-10/OQ-8, not → rationale as the design's narrative had it).

**R-36 — CLOSED at the level the row asked for.** The "template" was prose instantiated per task inside archived stage documents, so correcting T-19's instance would have discharged nothing. The half that is identical in every instance — the ledger path list — moved into `.harness/rules/80-delivery-policy.md` `## Process paths` **carrying `docs/batches/**`**, with one sentence binding a criterion to *cite* it rather than re-transcribe it, and one clause added to the fragment's "when to read" trigger (mirrored word for word at `AI-GUIDE.md:30`) because the criterion is written at stage 1 while the fragment was read only at stage 7. Two further paths T-19's instance also missed were found by derivation, not transcription: `CONTEXT.md` and `.harness/operator-obligations.md`. Validated by partitioning **75 paths across the last three delivery commits** — none in neither list.

### The durability ruling, and what happens at the next `/harness-upgrade`

Option **(a)** — local fix plus a record outside the vendored file — but with the mechanism inverted after the rollback. The record in `.harness/rules/80-delivery-policy.md` `## Local fixes to plugin-vendored scripts` is a **check per fix**, not a restore path, and its standing instruction is **keep what arrives**. `git checkout -- <path>` was dropped; `git log -p -- <path>` survives only as a source of prior bytes. Run against the text sitting in the cache today, the two checks return *lost* (it still decides rotation on entries) and *already provided* (it harvests wrapped bullets whole) — so the next refresh costs one bounded metric edit, keeps four upstream fixes, and discards nothing. **Neither verdict is written into the rule**: BC-13 forbids version-frozen prose, which is the same defect one level up. Stage 5 added the clause that makes it hold on a *refusing* arrival too — a run that wrote nothing is never *already provided*, which matters because 0.47.0 exits 3 without writing on any unclassifiable line.

### The archive run — AC-15, measured

Pre-flight (RES-2, the one input nobody had measured): `wc -l .harness/insight-index.md` = **30**, `tail -c 1 | xxd` = **`0a`**. The file ends with a newline, so the failure mode where `wc -l` reads 29 and the run lands at 31 is absent.

The script was run **once** (C-7). No hand rotation, no second run, no edit to `.harness/insight-index.md` or `insight-history.md` between the script's exit and `git add`.

| observation | value |
|---|---|
| index before | 30 lines · `e148278e434dfff70daae9db20284a8172c1199d58d90f352ccea6ea7b2953b8` |
| `insight-history.md` before | `9eefd99cc5b0ea45f88e572cb2fb81f736ea20370a5b91c3731ffd5921630aeb` |
| run, exit status | `bash .harness/scripts/archive-task.sh --task harness-self-maintenance` → **exit 0**, executed **once** |
| run output | `Harvested 4 insight(s) from 07_DELIVERY.md` · **`Rotating 4 old insight(s) to insight-history.md`** · `Insights: +4` · `Rotated: 4` |
| index after | **30 lines** · `7a63d28fb254ed5ed8186ba698b58bb7bc6788f346039a973c136e2fc18c310f` · **F.4 PASS** |
| `insight-history.md` after | `e544d390363ba1700dc6f4040cef54b78942ae28d3c84eae0ce1b7a1018ee406` |
| digests immediately after exit vs at `git add` | **equal** for both files — nothing was touched in between |
| hand rotation needed | **NO — the first time in seventeen deliveries** |

**That `Rotating 4` line is the acceptance evidence R-18 never had.** At HEAD the same run would have counted 22 + 4 = 26 bullets against 30, rotated nothing, appended four entries, and left the index at **34 lines** with F.4 WARNing — the sixteen-times-repeated outcome. The branch fired, the index came back to exactly 30, and F.4 passes with no hand edit. AC-15 is satisfied on all three of its clauses (≥1 harvested, ≤30 lines, digest equality).

## Insight

- 2026-08-16 · The file `/harness-upgrade` copies over a plugin-vendored script is **not** a newer copy of that script: `upgrade-project.sh:56` resolves the source to the plugin's `skills/harness-init/templates/common/…`, which for `archive-task.sh` is a **425-line rewrite** of this project's 151-line file, so "revert my two hunks" and `git checkout -- <path>` are both wrong models — the refresh replaces `refresh_set`'s seven script pairs wholesale with no backup and no marker preservation (`:186-194`, `:195-227`), while `verify_all.{sh,ps1}` alone is excluded and gets a splice, a HALT and a timestamped `.bak` (`:136-141`, `:535-573`) · evidence: harness-self-maintenance
- 2026-08-16 · `archive-task.sh`'s `## Insight` harvest matches the heading exactly (`^##\s+Insights?\s*$`, `:58`), so a delivery writing `## Insight to surface` — the heading `04_DEVELOPMENT.md` itself uses — harvests **zero entries, exits 0 and prints nothing**, which is indistinguishable from a task that surfaced no insight and silently loses the one artifact of a task that outlives it · evidence: harness-self-maintenance
- 2026-08-16 · `archive-task.sh`'s index rewrite reconstructs the file as `echo "$header"` + remaining + harvested where `header=$(grep -vE '^\s*-\s')` (`:114-123`), so it **hoists every non-bullet line to the top and loses trailing ones to command substitution**: an index whose last line is a non-bullet line comes back one line shorter with a mid-file marker moved (measured 33 → 29, marker line 19 → 9) — and because that lands *under* the cap, **F.4 passes over the loss**, while the two shapes that land over it (unterminated final line; zero non-bullet lines) merely WARN · evidence: harness-self-maintenance
- 2026-08-16 · Bash's `errexit` exempts a failing command inside an `&&` list unless it is the command **after the final `&&`**, so `[[ cond ]] && cmd` as an ordinary statement under `set -e` does **not** abort when the test fails — measured exit 0 on `archive-task.sh:82`'s HEAD form, refuting a claim two of this task's own upstream documents had asserted and that a reviewer, a developer and a gate had each carried forward before it was run · evidence: harness-self-maintenance

## Verdict

DELIVERED
