> Contract portion. Rationale: 05_RATIONALE.md (absent = none written).

## Files reviewed

- `/home/alan/Programs/singbox-cli/.harness/scripts/check-sc-contracts.py`
- `/home/alan/Programs/singbox-cli/.harness/scripts/verify_all.sh`
- `/home/alan/Programs/singbox-cli/.harness/scripts/baseline.json`
- `/home/alan/Programs/singbox-cli/.harness/rejected-decisions.md`
- `/home/alan/Programs/singbox-cli/docs/dev-map.md`
- `/home/alan/Programs/singbox-cli/bin/sc` (read only, as the subject the claims are about: `:10-17`, `:76-84`, `:120-126`, `:2165-2182`, `:2498-2507`, `:2590-2669`, `:2723-2734`, `:2795-2864`, `:3400-3410`, `:3595-3610`)
- `/usr/lib/python3.12/os.py` (read only, to re-derive half (a)'s enumeration claim first-hand)
- `/home/alan/Programs/singbox-cli/docs/features/suite-guarantee-boundaries/04_DEVELOPMENT.md`, `04_RATIONALE.md` (T5.2: adjudicating recorded drifts D-4 / D-5; T5.3: the reuse-correctness ruling on the private-helper decline)
- `/home/alan/Programs/singbox-cli/docs/features/suite-guarantee-boundaries/06_TEST_REPORT.md` (DEF-1 … DEF-5, the 20-mutant sweep)
- `/home/alan/Programs/singbox-cli/docs/features/suite-guarantee-boundaries/05_CODE_REVIEW.md`, `05_RATIONALE.md` (round 2, the list re-reviewed against)
- `/home/alan/Programs/singbox-cli/.harness/rules/70-doc-size.md` (`## Stage-doc boundary rule`, present)

## Findings

| id | severity | axis | file:line | finding |
|---|---|---|---|---|
| CR-10 | MINOR | Standards-conformance | `docs/dev-map.md:204-214` | **Carried unchanged from round 1 by my own round-1 ruling; re-anchored at its new line numbers.** The fenced recipe block — labelled "**use this one, do not re-invent it**" — carries neither half (a) nor half (b) of the capability denial, so a task that copies the block gets the *predicate* defeat only, while the prose beside it (`:132-202`) now describes a two-half denial, four open route families and an assertion-phase warning. Frozen by `02_SOLUTION_DESIGN.md`'s `## Frozen set` ("the denial has always lived in the prose beside it"), so **not this delivery's defect**. Round 4 widened the gap a third time (`docs/dev-map.md` +91/−4 this round; the bullet's prose is now ~70 lines against an 11-line block) without touching the block's bytes — which is the correct behaviour under the freeze and the reason the hazard is worth carrying rather than absorbing. Travels as RES-7. |
| CR-13 | NIT | Standards-conformance | `.harness/scripts/check-sc-contracts.py:58`, `docs/dev-map.md:172-173` | **New, introduced by the round-4 rewrite, in the safe direction.** "Adding the two names would deny those two spellings **and buy nothing**" states two things in one clause, and the second is true only of *capability*, not of *spelling* — adding `_execvpe` / `_spawnvef` to the tuple would in fact close the exact escape DEF-1 measured, for that one spelling. The clause names that effect itself ("would deny those two spellings"), the surrounding sentences give the real ground (a copied function's `__globals__` is the real `os` dict, and `os.path.os._execvpe` hands the capability straight back through family (iv), measured), and `rejected-decisions.md:766-769` states it exactly ("a name denies a spelling, not the capability"). So no reader is misled and nothing false is asserted; the phrasing simply invites a first reading in which the private spellings are undeniable. A precision NIT on a claim surface whose whole subject is precision — the exact form is "denies two spellings, closes no capability". |

**Round-2 findings, closed on first-hand evidence this round.** `CR-11` (MINOR, Spec/design-fidelity) — both false clauses are gone from both texts: `check-sc-contracts.py:85-87` now reads "empties the `if SYSTEMD:` / `elif OPENRC:` pairs **and short-circuits `_doctor_service` at `:2816`**, so of those three only `_doctor_tun`'s `:2853` survives", and `docs/dev-map.md:193-195` says the same with "(`:2816`, taking `:2827` and `:2831` with it)"; `rc-update show` is no longer named as a route the fixture *enters*, and the word "only" is gone. Verified against `bin/sc:2816-2819` (the `if not SYSTEMD and not OPENRC:` early return), `:2826-2831`, `:2846-2853`, `:2499-2504`, `:3405-3406` and `cmd_log`'s `else`-arm at `:3607` / `:3609`. `CR-12` (NIT) — `baseline.json:7`'s `test_count`-reader clause is now its own sentence *before* the "every other number" sentence, not a parenthesis inside it. Round-1 `CR-1` … `CR-9` remain closed. Disposition evidence for all of these is in `05_RATIONALE.md`.

## Requirement coverage check

| criterion | implementation | status |
|---|---|---|
| FR-1 (every claim-surface sentence enforced or marked) | header `:12-137`, `docs/dev-map.md:132-267`, `baseline.json:7`, `rejected-decisions.md:724-777` | ✅ — the assertion phase is stated in **both** texts and explicitly marked "enforced by nothing in this file" (`:93`); CR-11's accuracy defect inside that marked boundary is closed |
| FR-2 (non-`os` process route: refuse or name) | `check-sc-contracts.py:222`, `:233`, `:234`; four residual families `:44-75` | ✅ both branches used; the seven documented `subprocess` entry points refuse (QA 14/14 across both `_USE_POSIX_SPAWN` settings), the rest are named |
| FR-3 (no name added to the enumeration) | prefix tuple `:216` byte-identical; POSIX **and public** scoping `:19-28`; `rejected-decisions.md:761-773` declines `os.startfile`, `os._execvpe`, `os._spawnvef` and any `dir(os)` meta-assertion | ✅ — the decline is now grounded on a measurement (`os.path.os._execvpe` escapes with both halves in force), not on an assertion |
| FR-4 (`verify_all` FAILs on a lowered floor) | `verify_all.sh:116-134`, floor read `:90-93` | ✅ |
| FR-5 (language boundary stated, no second pass) | header `:107-114`, `dev-map:244-250` | ✅ ground verifiable: expected and observed both come from `sc.t()`; QA's zh mutant survives in both languages, as the sentence predicts |
| FR-6 (output-layer boundary, C-6 scoping) | header `:115-126`, `dev-map:251-261` | ✅ both texts scope to "**this repository's** `bin/sc`" and name `restricted-network-regression.sh:285` |
| FR-7 (one-writer: clause or recorded unenforced) | `check-sc-contracts.py:587-627`, `TESTS` row `:854`, boundary `:127-133` | ✅ clause adopted; red on QA's `os.replace` subject, green on the task-start `bin/sc` |
| FR-8 (no assertion removed; floor raised in the same change) | `TESTS` `:846-857` = 19 entries, re-counted entry by entry; `baseline.json:4-5` = 19 | ✅ — and `verify_all.sh`'s 2 total deletions this task make it structurally impossible that any other step lost a line |
| FR-9 (`bin/sc` unmodified) | — | ✅ PM's `sha256sum` = `81d65da8…b312`, unchanged; QA re-took it at both ends of its round |
| AC-1 (escape measurement before FR-2's disposition) | `04_DEVELOPMENT.md` `### C-2` / `### V-1` / `### the posix route` | ✅ re-taken by QA from scratch; all ten variants reproduce |
| AC-2 (escaping subject: no marker + non-zero, or named in both docs) | refusals `:222`; `posix` `:45-48`, `ctypes` `:48`, `_posixsubprocess.fork_exec` `:48-49`, private helpers `:51-65`, real-`os` chain `:69-75` — each mirrored in `dev-map:159-185` | ✅ |
| AC-3 (no sentence asserts coverage the artifact lacks) | half (a) scoped **twice** `:19-28` / `dev-map:137-145`; family (ii) carries the enumeration and both measurements `:51-65` / `dev-map:166-178`; "a reader may not take that list for a verified guarantee" `:98` / `dev-map:202` | ✅ **DEF-1 closed.** Re-derived independently of the developer's script: the tuple `("exec","spawn","fork","popen","posix_spawn","system")` matches exactly 22 names of `dir(os)` on POSIX (8 `exec*`, 8 `spawn*`, `fork`, `forkpty`, `popen`, `posix_spawn`, `posix_spawnp`, `system`), and no **public** `os` name that starts or replaces a process falls outside them; `os._execvpe` (`/usr/lib/python3.12/os.py:593`) and `os._spawnvef` (`:853`) do, are private, and are named **open, present-tense** in both texts. The scoped sentence is true, not merely narrower. Derivation in `05_RATIONALE.md` |
| AC-4 (lowering FAILs, naming both numbers) | `verify_all.sh:130-131` | ✅ code verified, `10#`-pinned; QA measured the detail string verbatim |
| AC-5 (raising leaves PASS) | `verify_all.sh:132-133` | ✅ discriminating instant taken pre-commit; post-commit is RES-1 |
| AC-6 (unreadable history: no FAIL, "not performed" printed) | `verify_all.sh:127-129` | ✅ one `step` SKIP + exactly one `echo`; the four BC-2 shapes all reach the same branch because `floor_of()` answers empty for every one of them |
| AC-7 (translated wording survives; boundary says so) | header `:107-114` | ✅ QA's zh mutant survives all three ways the sentence says it must |
| AC-8 (no runner / no spawn, under C-6's scoping) | header `:115-126`; `strace` over a B.4 run: `execve` 1, `clone/clone3/fork/vfork` 0 | ✅ |
| AC-9 (clause red on `os.replace`, green on task-start `bin/sc`) | `:615-627` | ✅ |
| AC-10 (`bin/sc` byte-identical) | PM's re-take, digest unchanged | ✅ |
| AC-11 (`verify_all` PASS / FAIL 0 / WARN 0 / B.3 the only SKIP) | PM: PASS 20 · WARN 0 · FAIL 0 · SKIP 1, exit 0, B.4 and B.6 both PASS; B.3 is `verify_all.sh:77` | ✅ |
| AC-12 (non-discriminating criteria reported, not rounded up) | C-8 clause, BC-4, RES-3, RES-4 all declared | ✅ QA corrected two of them by measurement rather than copying the list |
| AC-13 (no new file/dir/dependency; net executable within NFR-2) | 5 modified files, 0 added; imports `:139-153` unchanged | ✅ |
| NFR-2 (≤ 40 net executable) | E-1 **3** (`:161`, `:222`, `:233`) + E-2 **14** (`def :587` + body `:615-627`) + E-4 **14** (`floor_of :90-93` = 4, B.6 `:125-134` = 10) = **31** | ✅ re-counted line by line at the delivered line numbers. The `+1` over round 3 is `floor_of()`'s shape test (`:92`); E-4's 14 sits inside the gate's 12–15 allowance; the leak-check clause `:234`, `_no_new_process`'s message `:182-184`, B.4's read `:99` and its `10#` comparison `:106` are rewrites at 0 net |
| NFR-3 (3.6 syntax, stdlib, no new import, 0755) | no f-string, no walrus, no 3.7+ keyword in any added line; import block `:139-153` unchanged; PM's `stat` = 755 | ✅ |

## Design fidelity check

| design item | implementation | status |
|---|---|---|
| I-1 / K-1 — capture, displace, `try:` with nothing that can raise between | capture at **module scope** `:161`; `:218` `mod = …`, `:222` `subprocess.Popen = _no_new_process`, `:223` `sys.modules["os"] = shim`, `:224` `try:` | ✅ strictly narrowing; the window is still exactly the `exec()`, and the two displacement statements are still adjacent to the `try:` with nothing raisable between them |
| I-1 / K-2 — one `finally` restores both; leak check covers both | `:231-233` (LIFO: `os` then `Popen`, both in the **same** `finally`); `:234` `… or subprocess.Popen is not REAL_POPEN`; `:235` raise | ✅ BC-5 intact **and strengthened**: the clause now compares against a binding made at import, so no statement order inside `load()` can make the restoration assert itself (QA's M4b) |
| BC-6 — the denial does not reach the assertion phase | restore precedes `return mod` (`:236`); `_CheckerStub` bound to `sc.subprocess` at `:798-799`, restored `:803`, all after `load()` returned | ✅ unchanged by round 4 |
| I-2 — message names the attempt, offers two possible causes | `:182-184` | ✅ |
| I-3 / BC-8 / C-7 — bounded to `generate_config()`, `--source`-driven, no statement order | `:615-627`; three residuals in the docstring `:602-613` with "re-aim, never delete" | ✅ |
| I-4 / K-4 — new name appended after `every_file_read_and_write_names_utf8`, no reorder | `:854` | ✅ `len(TESTS)` = 19 |
| I-5 / K-13 — `floor_of()` one definition, inside the B-CUSTOM markers | `:90-93`, markers `:48` / `:135`; both callers `:99`, `:125-126` | ✅ still one reader, one answer |
| I-6 / K-14 — B.6 after B.5, FAIL names both numbers and the file, SKIP + one line, never runs the suite | `:116-134` | ✅ |
| I-7 / C-5 — `notes` marks `passing_count` / `warnings_baseline` as read by nothing | `baseline.json:7` | ✅ and the `notes` now also states B.4's new FAIL condition ("its `test_count` is not one readable number"), which is true of the delivered `floor_of()` |
| I-8 / I-9 / K-6 … K-11 — both halves, POSIX scope, four families, three "do not reach" limits | header `:12-137`, `dev-map:132-267` | ✅ residual list complete and identical in both texts: `posix` (i), `ctypes` (i), `_posixsubprocess.fork_exec` (i), the private `os` helpers (ii), the pre-import line (iii), the real-`os` attribute chain (iv) |
| I-10 / K-12 / C-11 — the same six claims in the recipe bullet; T-32's sentences byte-identical | `dev-map:132-267`; fenced block `:204-214`, R-77 `:236`, R-78 `:239`, R-84 `:240`, "nine path constants" `:216` | ✅ anchors re-verified at their new line numbers; byte-identity of the block is derivable from the PM's `--numstat` (4 deletions total, all four located inside the two falsified sentences), not only from the developer's set difference |
| K-15 — mirror untouched; B.6's comment states the Windows consequence | `verify_all.sh:122-124` | ✅ citation still exact after the reflow |
| K-16 / C-4 — record amended, G-2's ground absent | `rejected-decisions.md:724-777` | ✅ and the new "also declined" clause `:761-773` is correct: it names both private helpers, states the measured escape, and declines on "the name is not the mechanism" — the ground that survives contact with family (iv) |
| K-3 / K-17 / K-18 — no name added, no import added, `bin/sc` and red-line files untouched | tuple `:216`; imports `:139-153`; PM's `--numstat` (5 files, none of them `.claude/`, `CLAUDE.md`, `.github/copilot-instructions.md`, `archive-task.sh`, `guard-rm.sh`, `verify_all.ps1`, `bin/sc`) | ✅ |
| D-1 / D-2 / D-3 (drifts recorded at round 1) | `:235`; `:222`/`:233`; `:587`/`:615-627`/`:854` | ✅ all three re-counted at the new line numbers, unchanged |
| **D-4 (new)** — I-1 places the `Popen` capture *inside* `load()`; delivered at module scope (`REAL_POPEN`, `:161`) | `:155-161`, `:233`, `:234` | ✅ **ACCEPTED.** It closes a measured defect (DEF-2 / M4b) at 0 net lines, does not widen what the denial permits — it changes only which object the restore reads — and leaves BC-5 and BC-6 intact. The one thing it does not cover, a deliberate **rebinding** of `REAL_POPEN`, is named in the code at the binding (`:159-160`) and filed; naming is the right disposition, because that is an edit no reorder can produce and a mechanism against it would cost more than the hazard |
| **D-5 (new)** — I-9 / K-13 spell `floor_of()` as the one `sed`; delivered, it also decides what a floor **is** (`:91-92`), with both comparisons `10#`-pinned (`:106`, `:130`) | `:90-93`, `:106`, `:130` | ✅ **ACCEPTED, including the extension into B.4.** The fail-open was in the *shape* of the value, so the fix belongs in the one reader; putting it at each call site would be two spellings of one judgement, which is exactly what I-5 forbids. Both callers' existing branches are untouched (unusable → B.4 FAIL, B.6 SKIP), B.4's behaviour on a valid file is unchanged, and the `10#` pinning is one token beyond QA's finding that closes B.4's identical fail-open on a leading-zero floor. A defensible widening, not scope creep |

## Axis status

- **Standards-conformance**: 2 findings (CR-10, carried by ruling and frozen by design; CR-13, NIT), worst = **MINOR**. Rule 85 re-checked at the delivered line numbers: **31 net executable** against NFR-2's cap of **40**, the single added line being `floor_of()`'s shape test, which is what makes two controls fail closed instead of open — the cheapest line in the change. No delivered element is droppable: dropping E-1 forfeits a measured capability closure for 3 lines, dropping E-2 forfeits FR-7's only enforcement, dropping `floor_of()`'s shape test restores two fail-open comparisons, dropping the C-8 clause saves 0 lines while falsifying BC-5 and I-1. No new file, directory or dependency; Python 3.6 floor and stdlib-only intact; `TESTS` = 19 and `baseline.json`'s floor = 19; B.3 still the standing SKIP (`verify_all.sh:77`); B.5 unwidened (`:112`, `--self-check` only) — and `verify_all.sh`'s **2 total deletions** across the whole task make that structural rather than asserted. `bin/sc` byte-identical (PM). No invented rule was applied: every judgement above is traceable to `01`, `02`, the frozen set, or a `.harness/` fragment.
- **Spec/design-fidelity**: **no findings — clean.** QA's AC-3 failure is closed and I re-derived the closure rather than accepting it: the delivered sentence claims exactly what the artifact does (public spellings, on POSIX), the two private helpers are named present-tense open with their measurements in both texts, and the "no more" test holds too — the enumeration is scoped to a named interpreter and the future-name case stays inside family (ii). Both new drifts (D-4, D-5) are accepted as defect closures inside their design elements. The six claims appear in both claim-surface texts, in the same order, saying the same things, and each one ends where the artifact ends.

## Residuals travelling

| id | statement | must reach |
|---|---|---|
| RES-1 | B.6's **post-commit** reading (tree 19 vs `HEAD` 19 → PASS) in the real repository is still owed; stage 4 took the pre-commit instant there and pre-exercised post-commit in a scratch clone only. | `07_DELIVERY.md` |
| RES-2 | The four open route families are re-measured, not inherited, on the delivered tree — including the two private `os` helpers added to family (ii) at round 4. | `06_TEST_REPORT.md` (V-1 / C-2) and `docs/tasks.md` R-93 |
| RES-3 | E-1's post-`finally` `Popen` clause (`check-sc-contracts.py:234`) fails on no build whose `finally` restores unconditionally (QA M3); it **does** kill a deleted restore, and after D-4 a capture-order error can no longer defeat it. Report it that way, not as a passed check. | `06_TEST_REPORT.md` AC-12 list |
| RES-4 | The redirection order at `verify_all.sh:99` and `:125` (`2>/dev/null` **before** `<`) has no automated control by construction (QA M14): no committed step creates an absent `baseline.json`. Its only record is `floor_of()`'s comment (`:86-89`). | `06_TEST_REPORT.md` AC-12 list |
| RES-5 | BC-4: B.6 cannot see a lowering already present in `HEAD`. Declared, carried in `baseline.json`'s `notes` and B.6's comment (`verify_all.sh:116-121`), measured blind by QA at floor 5. | `06_TEST_REPORT.md` (V-12) |
| RES-6 | `verify_all.ps1` has no B.6 and its B.4 is an unconditional SKIP (`:90-93`), so on Windows neither the floor nor its monotonicity is checked at all. | `docs/tasks.md` — **new row** at stage 7, owner "next task touching the mirror" |
| RES-7 | `docs/dev-map.md:204-214`'s fenced recipe carries neither denial half; a task copying the block gets the predicate defeat only, and the prose-to-block ratio grew again this round (CR-10). Frozen by design this task, so it is a filed hazard, not a defect. | `docs/tasks.md` — filed row, owner "next task touching the recipe or the mirror" |
| RES-8 | `import posix; posix.system(…)` is the cheapest open process route — cheaper than either other family-(i) exemplar and than family (iv). R-93's disposition should be narrowed to say the cheapest escape is a two-token one. | `docs/tasks.md` R-93 |
| RES-9 | `.harness/rules/50-singbox-cli.md:29-30` says "14 contract assertions" and is stale at 19. R-94's, not this task's. | `docs/tasks.md` R-94 |
| RES-10 | `01_REQUIREMENT_ANALYSIS.md` FR-6 / AC-8 / BC-11's "no committed artifact starts a child process" is still unamended and false of the tree; C-6's scoping is what was built, what the delivered sentences claim, and what QA reported AC-8 against. FR-1's claim-surface enumeration (`01:16-19`) also omits `.harness/rejected-decisions.md`, as does `CONTEXT.md:222-228`'s glossary term. | the analyst, via `docs/tasks.md` at stage 7 |
| RES-11 | **New.** `REAL_POPEN` (`check-sc-contracts.py:161`) is what the post-`finally` clause compares against; a deliberate **rebinding** of that name inside `load()` would restore the denial and assert it against itself, and nothing asserts against that. Named in the code at the binding; no mechanism exists. | `docs/tasks.md` — filed row, owner "next task editing `load()`" |
| RES-12 | **New.** The denial itself has **zero** committed controls: QA's M1 (drop half (b)), M5 (drop `"system"` from the tuple) and M6 (drop `mod.os is not shim`) all leave `verify_all` green. Closing any of them needs a committed escaping subject driven through `--source` — i.e. a new file, which AC-13 / NFR-3 forbid in this task — or a second interpreter invocation, which the output-layer sentence denies. Record the class, and price the fixture in a later task. | `06_TEST_REPORT.md` AC-12 list and `docs/tasks.md` — new row at stage 7 |
| RES-13 | **New.** With an unusable floor B.6 SKIPs, so a duplicated or non-numeric `test_count` loses the monotonicity check for that run while B.4 FAILs it. The ratchet goes silent, not violated, and no committed step produces that state — the safe direction, but it is a boundary a reader should meet stated rather than inferred. | `06_TEST_REPORT.md` (B.6 boundary list) |
| RES-14 | **New.** Family (ii)'s "**402** names in `dir(os)` on CPython 3.12.3" is the one figure on the claim surface I could not verify by reading (the 22 matched and the two unmatched process-starters I re-derived by hand and confirm). It is a denominator: wrong, it costs a MINOR correction and no guarantee. Re-take it mechanically. | `06_TEST_REPORT.md` (AC-3) |

## Verdict
APPROVED WITH MINOR FINDINGS (0 CRITICAL, 0 MAJOR, 1 MINOR, 1 NIT)
