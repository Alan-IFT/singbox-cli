# 03 — Gate Review · T-31 `suite-guarantee-boundaries`

> Contract portion. Rationale: 03_RATIONALE.md (absent = none written).

## Dimension audit

| # | dimension | verdict | reason |
|---|---|---|---|
| 1 | Requirement completeness | WARN | Every in-scope behaviour is testable and FR-1 states one rule its instances derive from, but FR-6/AC-8's "no committed artifact starts a child process" is unscoped and already false of the committed tree (`verify_all.sh:33`, `:44`, `:71`, `:87`, `:96`), so no delivery can report AC-8 passed without narrowing it (G-3). |
| 2 | Design completeness | WARN | FR-2 … FR-9 each have a named element, seam and revert, but FR-1's own sweep of the claim surface stops short of `baseline.json`'s two numbers no step reads (G-4). |
| 3 | Reuse correctness | PASS | Every reuse claim holds first-hand: the `ast`/`--source` seam (`check-sc-contracts.py:416-471`, `:432`), `_no_new_process` + `LoadRefused` (`:69-76`), the one `try/finally` and its leak check (`:112-122`), B.4's inline `sed` (`verify_all.sh:83`), `step()` printing detail only on FAIL (`:19`), and the `.git`-guarded `git` pattern (`:30-46`) — and `subprocess.run`/`call` do funnel through the module-global `Popen` (`/usr/lib/python3.12/subprocess.py:389`, `:548`). |
| 4 | Risk coverage | WARN | Risks 1-8 are the real ones and each is answered at the artifact, but the residual enumeration the entire task turns on omits the cheapest escape family — the real `os` module reachable one attribute hop from the shim itself (G-1) — and that family refutes one stated ground for declining candidate (3) (G-2). |
| 5 | Migration safety | PASS | No data format, no flag and no on-disk artifact of `sc` moves; E-2 and E-5 are bound into one commit with the silent direction named as the reason (K-5, `## Migration & edit sequence` row 5), and each element's revert leaves no sentence over-claiming. |
| 6 | Boundary handling | PASS | BC-1 … BC-12 each get an element or an explicit refusal, and BC-2's four unreadable-history cases collapse into one empty-`floor_of` branch whose "not performed" line must be a bare `echo` beside a SKIP — which `step()`'s FAIL-only detail (`verify_all.sh:19`) forces and E.3 already demonstrates (`:129`). |
| 7 | Test feasibility | WARN | AC-1 … AC-14 are each runnable on this host without root, but AC-5 discriminates an equality-written control only at the pre-commit instant (G-9) and one delivered line can fail on no build while AC-12's declared list does not name it (G-6). |
| 8 | Out-of-scope clarity | PASS | The frozen set names paths and line spans rather than intentions (`bin/sc`, `:108`'s tuple, `dev-map.md:142-152`, B.3/B.5, the `.ps1` mirror, the eleven T-32 sentences), so the three over-builds this task invites — a wider name list, a second-language pass, a child-process runner — are each refused by name in both stage documents. |

## Findings

| id | severity | owning upstream doc + section | finding |
|---|---|---|---|
| G-1 | MAJOR | `02_SOLUTION_DESIGN.md` K-8 (and I-8 / I-9 / I-10) | The real `os` module stays reachable one attribute hop from the shim and from every already-imported module — `shim.path` **is** the real `posixpath`, whose `os` attribute is the real `os` (`posixpath.py:25`), and `subprocess.os` / `shutil.os` / `tempfile.os` are the same object (`subprocess.py:47`) — so `os.path.os.execvp(...)` from a subject's import passes through neither the shim's names nor `subprocess.Popen`, and K-8's three residuals do not name that family. |
| G-2 | MAJOR | `02_RATIONALE.md` `## Why the subprocess choke point and not a wider enumeration`, candidate (3); `02_SOLUTION_DESIGN.md` K-16 | "It … buys no coverage candidate (1) does not already give" is refuted by G-1: denying the process-start names on the **real** `os` for the load window is precisely what would close the attribute-chain family. The ruling survives on its other grounds, but the record K-16 appends must not ship the refuted one. |
| G-3 | MAJOR | `01_REQUIREMENT_ANALYSIS.md` FR-6, AC-8 (and BC-11's spine wording) | "No committed artifact … starts a child process" is already false of the committed tree — `verify_all.sh` runs `git` (`:33`, `:44`), `bash` (`:71`, `:96`) and `python3` (`:87`), and B.6 adds `git show`. NFR-1 states the enforceable spine correctly (a **B.4** run: `execve` 1, `clone` 0); FR-6/AC-8/BC-11 overshoot it, and an unscoped criterion in this task of all tasks cannot be reported passed. |
| G-4 | MAJOR | `02_SOLUTION_DESIGN.md` I-7 (FR-1 instance stage 1 did not enumerate) | `baseline.json` carries `passing_count` and `warnings_baseline`, which **no** committed step reads (B.4 reads `test_count` at `verify_all.sh:83`; B.6 will read the same key), while its `notes` asserts a property of `passing_count`. FR-1 requires every guarantee-shaped sentence there to be enforced or marked; I-7 marks only `test_count`, B.6 and BC-4. |
| G-5 | MAJOR | `01_REQUIREMENT_ANALYSIS.md` BC-8 vs `02_SOLUTION_DESIGN.md` I-3 | I-3's "first positional argument is the bare name `CFG_PATH`" is the clause's **only** discriminator (the measured mutant keeps `bin/sc:2170`'s `_write_private(Path(name), text)`), so the clause necessarily pins one argument spelling — `_write_private(path=CFG_PATH, …)` or `dest = CFG_PATH; _write_private(dest, …)` redden it — and BC-8's "pins neither statement order nor **spelling**: a reshaped `generate_config()` that still installs through `_write_private()` passes it" is not satisfiable as literally written by any discriminating clause. |
| G-6 | MAJOR | `02_SOLUTION_DESIGN.md` I-1 / K-2; `01_REQUIREMENT_ANALYSIS.md` AC-12 | E-1's fourth line (post-`finally` "`Popen` still displaced" clause) can fail on no build whose `finally` restores unconditionally — on the exception path the check is never reached, on the success path the restore always ran. It is a regression control against a future edit, not evidence that the restoration works, and AC-12's declared list (BC-4, AC-3, AC-8) does not include it. |
| G-7 | MINOR | `02_RATIONALE.md` `## NFR-2 — the cap, re-derived` | The floor-control element is priced at 10; re-derived over the same artifacts it is 12-15 (`floor_of()` is +3 net — `verify_all.sh:83` is rewritten, not deleted — and B.6's branch needs two reads, three `step` calls and the BC-2 `echo`). Total re-derives to 31-34, so NFR-2's cap of 40 is upheld unamended and the design's "≈29" is optimistic rather than incredible. |
| G-8 | MINOR | `01_REQUIREMENT_ANALYSIS.md` NFR-2 / AC-13 | "Net executable addition" is undefined against docstrings and comments, while RES-4 requires I-3's docstring to carry two residuals and K-15 requires a comment beside B.6 — so `git diff --stat` will exceed the executable count by a wide margin with no stated rule for reading the gap. |
| G-9 | MINOR | `01_REQUIREMENT_ANALYSIS.md` AC-5; `02_SOLUTION_DESIGN.md` V-4 | AC-5 ("raising the floor leaves `verify_all` PASS") discriminates a control written as equality only at the **pre-commit** instant (tree 19 vs `HEAD` 18); post-commit (19 vs 19) an equality control also passes, so a delivery that records only the post-commit reading reports a non-discriminating observation as the criterion. |
| G-10 | MINOR | `01_REQUIREMENT_ANALYSIS.md` FR-1 | FR-1 enumerates the claim surface as three places; E-7 demonstrates a fourth — `.harness/rejected-decisions.md:678-679`'s "until then this record **is** the enforcement" is a guarantee-shaped sentence this delivery falsifies. The design already handles it; the enumeration, not the design, is what is incomplete. |
| G-11 | MINOR | `02_SOLUTION_DESIGN.md` `## Migration & edit sequence` row 2 | "E-1 applied, so the new assertion runs under the widened denial" is false: the denial covers the `exec()` window only and is restored in `load()`'s `finally` before any assertion runs. The ordering constraint is still right (the suite must report 19 passing with E-1 in place); its stated reason is not. |

## Binding conditions

| id | condition | owner stage | discharged by |
|---|---|---|---|
| C-1 | Both claim-surface texts (K-8 inside I-8/I-9 and I-10) name a fourth open route family in its own clause: the real `os` module stays reachable one attribute hop away — from the shim itself (`os.path.os`, because `shim.__dict__.update(os.__dict__)` copies the real `posixpath`, `check-sc-contracts.py:105`) and from every already-imported module (`subprocess.os`, `shutil.os`, `tempfile.os`) — so a subject reaching `os.path.os.execvp(...)` passes through neither half of the denial. | 4 (dev) | V-2 reading both documents; 06 quoting the clause from both files |
| C-2 | M-1 gains two variants, taken before E-1 is written and re-taken at stage 6, scratch subject only and marker target only (BC-7): (a) `import os` then `os.path.os.system("/usr/bin/touch <scratch>/escaped-realos-path")`, (b) `import subprocess` then `subprocess.os.system("/usr/bin/touch <scratch>/escaped-realos-subprocess")`, each under `if os.getuid() != 0:`. C-1's clause states the outcome the reading gives: marker present → the family is named open; marker absent → the reading is recorded and the clause follows it. | 4 (dev), re-taken 6 | 04 and 06 reporting exit code, marker state and the `os restored` line for both variants, before and after E-1 |
| C-3 | Both claim-surface texts carry one sentence making the limit unmistakable: the load-time denial is a guard against an **accidental** process start from the subject's own import, not a sandbox against a subject that seeks to escape — a reader may not take the residual list for a verified guarantee. | 4 (dev) | V-2 |
| C-4 | E-7's appended record (`denying-the-non-os-process-routes-by-a-wider-name-enumeration`) does not carry G-2's refuted ground. It states candidate (3)'s true coverage delta — it **would** close C-1's attribute-chain family, it does **not** close `_posixsubprocess.fork_exec` or `ctypes` — and declines it on the grounds that survive: it holds for `subprocess` only by one CPython's dispatch choice, it is a second mutation of a global module the harness itself uses, and FR-2 branch (b) permits the sentence at zero lines. | 4 (dev) | reading the committed record against this row |
| C-5 | `baseline.json`'s `notes` (I-7) additionally marks that `passing_count` and `warnings_baseline` are read by no committed step — B.4 reads `test_count` only (`verify_all.sh:83`) and B.6 reads the same key — so every sentence that file carries is enforced or marked (FR-1). No executable line is added for either number. | 4 (dev) | reading `notes`, plus a repo-wide search for a reader of `passing_count` / `warnings_baseline` returning none, reported in 06 |
| C-6 | FR-6 / AC-8 are read as scoped to the contract suite and to any artifact this task adds; `verify_all.sh`'s pre-existing `git` / `bash` / `python3` invocations and B.6's `git show` are outside that scope. K-10's delivered sentence states the scope it claims (no committed artifact runs `bin/sc` **as a program**, and the **suite** starts no child process), and 06 reports AC-8 against that scoping rather than against the unscoped wording. | 4 (dev), 6 (QA) | 06's AC-8 row naming the scope and citing NFR-1's `execve` 1 / `clone` 0 reading over a B.4 run |
| C-7 | I-3 ships with its docstring and RES-4 carrying a third residual: the clause pins the destination as the **bare name** `CFG_PATH` in first-positional position, so a keyword or aliased spelling reddens it although the invariant holds, and the remedy is re-aiming the clause, never deleting it. BC-8 is discharged as "pins no statement order and no spelling **other than the invariant's two nouns**", because a clause without the destination argument passes `mut-res9-os-replace` and discriminates nothing. | 4 (dev) | M-4 extended by a third case — the keyword-spelled reshape — reported as a known red of the clause, not as a defect of `bin/sc` |
| C-8 | E-1's post-`finally` `Popen` clause is either dropped (E-1 becomes 3 executable lines) or reported NOT-DISCRIMINATING at stage 6 as a regression control against a future edit that moves the restore. AC-12's list becomes BC-4, AC-3, AC-8 and — if kept — this line. | 4 (dev), 6 (QA) | 06's AC-12 row; 04 stating which of the two was taken |
| C-9 | B.6's result is recorded at **both** instants with both numbers — pre-commit (tree 19 vs `HEAD` 18 → PASS) and post-commit (19 vs 19 → PASS) — because only the pre-commit reading discriminates a control written as equality. A delivery that records only one of them reports AC-5 as NOT-DISCRIMINATING. | 4 (dev), 6 (QA) | 04 and 06 each carrying the two readings verbatim |
| C-10 | AC-13 is reported as two numbers: raw `git diff --stat` per file, and net **executable** lines with docstrings, comments and prose excluded, itemised over the element list. The gate's re-derivation is denial **4** · one-writer clause + registry row **13** · `floor_of()` + B.6 **12-15** · floor edit **2** = **31-34**; NFR-2's cap stays **40**, unamended. An executable total above 40 blocks the delivery rather than re-capping it. | 6 (QA) | 06's AC-13 row carrying both counts and the itemisation |
| C-11 | The two falsified sentences at `docs/dev-map.md:137-140` are corrected in place and every other sentence of that bullet is byte-identical in the diff — R-77 (`:172-174`), R-78 (`:175-177`), R-84 (`:177-178`), the "nine path constants" clause (`:154`) and the fenced block (`:142-152`) — per BC-12 and K-12. | 4 (dev) | `git diff docs/dev-map.md` read line by line in 06 |

## Pre-answered developer questions

| id | question | answer |
|---|---|---|
| Q-A | Can the `subprocess.Popen` denial break the assertion phase, where a `subprocess`-shaped stub legitimately lives? | No. The stub is bound at `check-sc-contracts.py:642-643` as `sc.subprocess` — the **loaded module's** attribute, a different object — and only after `load()` has returned and its `finally` (`:119-120`) has restored the real `Popen`. A leak would also be loud rather than silent: any assertion path reaching `sc.subprocess.run` would raise `LoadRefused` where the code expects `OSError`. |
| Q-B | Where exactly do E-1's two pre-`try` lines go, and can either raise? | Between `mod = types.ModuleType("sc")` (`:110`) and `sys.modules["os"] = shim` (`:111`): one attribute read to capture the original, one attribute write to displace it. Neither can raise, which is the property `bin/sc:2161-2164` states in its own words ("nothing that can raise may ever go between the sentinel and the `try:`"). |
| Q-C | Can this delivery fail its own new control when it raises the floor 18 → 19? | No. B.6 compares the working tree against `git show HEAD:…`: pre-commit 19 vs 18 is higher → PASS (BC-3), post-commit 19 vs 19 is equal → PASS. B.4 is the step that binds the other direction — E-5 without E-2 makes it FAIL loudly with fewer passing than the floor. |
| Q-D | `step()` prints no detail on SKIP — how does BC-2's "the comparison was not performed" reach the output? | As a bare indented `echo` immediately after the `step "B.6" … "SKIP"` call. That is confirmed against the source (`verify_all.sh:19` prints detail on FAIL only) and the shape already exists at `:129`, where E.3 prints its WARN explanation the same way. |
| Q-E | Does the new assertion need a fixture, a new import, or a run of `bin/sc`? | None of the three. `ast` is already imported (`:39`), the clause reads `sc.generate_config.__code__.co_filename` exactly as `every_file_read_and_write_names_utf8` does (`:432`) so `--source` drives it, and its only I/O is reading that one file. At task start the call it looks for is `bin/sc:2202`, inside `generate_config` (`def` at `:2057`); `bin/sc:2170`'s `_write_private(Path(name), text)` is the candidate write and is what the destination argument separates it from. |
| Q-F | B.6 guards on `[[ -d .git ]]` like A.1/A.2 — what about a worktree or submodule checkout, where `.git` is a **file**? | It SKIPs, exactly as A.1 and A.2 already do there, and prints the "not performed" line. That is inside BC-2's intent (the comparison is not performed, nothing FAILs) and is not a new inconsistency; it belongs in the delivery's residual list rather than in a widened condition. |

## Verdict

APPROVED WITH CONDITIONS
