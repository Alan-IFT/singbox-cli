# PM Log — T-31 / suite-guarantee-boundaries

Mode: **full** (7 stages). Pool: `closeout` (`docs/batches/closeout/BATCH_PLAN.md`).
Decision authority: standing («你来决策就行»). Deferred-human: NOT set — this is a batch run but
the operator granted standing authority; `BLOCKED: NEEDS-HUMAN` is reserved for the one red line
named in the brief (a design that could let `verify_all` elevate).

## Task-start state (measured by PM, 2026-08-16)

- HEAD `2a6b6e8`; working tree clean except `docs/batches/closeout/{BATCH_PLAN,BATCH_LOG}.md`.
- `bash .harness/scripts/verify_all.sh` → **PASS 19 / WARN 0 / FAIL 0 / SKIP 1, exit 0**.
  B.3 is the standing SKIP. B.4 `bin/sc contract assertions` PASS.
- `baseline.json`: `test_count: 18`, `passing_count: 18`.
- Live host witness (`systemctl show`, never `is-active`): `MainPID=2566751`, `NRestarts=0`,
  `ActiveEnterTimestamp=Tue 2026-08-11 12:13:57 CST` — matches the brief.
- `.harness/intervention.md`: **absent** at task start (checked before stage 1 dispatch).
- `.harness/agents/dev-*.md`: **none** → single-Developer mode (`harness-kit:developer`).
- `.harness/scripts/task-state.js`: **absent** on this host — durable state is kept in this log.
- `.harness/scripts/entropy-cadence`: **absent** (R-88) → delivery-time entropy watch resolves
  NOT-DUE fail-open, as in T-28/T-29/T-30.
- `docs/tasks.md`: **298** lines against the F.5 cap of 300 → rotation into
  `docs/tasks-archive.md` (563 lines) is required at delivery.

## Insight-index entries surfaced to stage 1

Lines 18, 27, 29 of `.harness/insight-index.md` (the R-93, R-102 and R-104 subjects respectively),
plus lines 12, 9, 20, 22 (the fixture/locale traps that bear on R-95 and R-96). Passed whole into
the stage-1 dispatch.

## Routing decisions

| # | Time | Stage | Decision |
|---|---|---|---|
| 1 | 2026-08-16 | — | Folder created, baseline measured, no intervention pending. Dispatch stage 1 (requirement-analyst). |
| 2 | 2026-08-16 | 1 → M-1 | Stage 1 returned `READY` (`01_REQUIREMENT_ANALYSIS.md` 182 lines + `01_RATIONALE.md` 208 lines; it also added one glossary term, **claim surface**, to `CONTEXT.md`). Intervention re-checked: absent. Stage 1 assigned **M-1** (the `subprocess`-escape measurement) to stage 2 with an exact probe, but `harness-kit:solution-architect` holds no shell. Routed M-1 to an independent **measurement runner** (general-purpose agent) constrained to stage 1's probe verbatim, observations only, no design opinion, scratch-only writes. This is evidence collection under stage 1's own specification — the runner proposed nothing and the architect owns the disposition. AC-1 still requires stage 6 to re-take it. |
| 3 | 2026-08-16 | M-1 result | **Measured, and stage 1's expectation held.** `subprocess.call`, `subprocess.Popen`, `subprocess.run` and `ctypes.CDLL(None).system` each **started a process and left its marker** while the suite ran on to its assertion phase (exit 1 only because a scratch subject defines none of `sc`'s functions). The `os.posix_spawn` control was **refused** (`LoadRefused`, `os restored True`, `18 defined, 0 run, 0 passed`, exit 2, no marker) — so the probe harness discriminates. Host: CPython **3.12.3**, `subprocess._USE_POSIX_SPAWN` **True**. Service witness and `git status` unchanged (nothing written under the repo). FR-2 branch **(a)** is therefore live. Dispatch stage 2 with the raw readings. |
| 4 | 2026-08-16 | 2 → 3 | Stage 2 returned `READY` (`02_SOLUTION_DESIGN.md` 186 lines + `02_RATIONALE.md` 200 lines), no measurement outstanding. ≈**29** executable lines against NFR-2's 40: E-1 `subprocess.Popen` denial for the load (4), B.6 floor monotonicity + `floor_of()` (10), FR-7 source clause + registry row (13), floor edit (2); R-95/R-96/FR-3 close at **zero code** as written boundaries in both claim-surface documents. FR-7 **adopted** on stage 2's own ground (K-11 does not transfer). Candidate (3) rejected *because* it appears to work under `_USE_POSIX_SPAWN=True` — a spelling standing in for a capability. Intervention re-checked: absent. **AC-10 witness taken by the PM** (stage 2 has no shell): `bin/sc` task-start `sha256 = 81d65da83ba23808c1f09ce81c94e067449eac698db7c625d67b775dbd31b312`. Advance to stage 3 (gate-reviewer). Stage 2 offered an optional pre-implementation confirmation of the E-1 flip; the gate may request it — the PM holds the shell. |
| 5 | 2026-08-16 | 3 → 4 | Stage 3 returned **`APPROVED WITH CONDITIONS`** — 0 BLOCKER / 6 MAJOR / 5 MINOR, **11 binding conditions**. The gate holds no write capability; the PM **transcribed both returned portions verbatim** to `03_GATE_REVIEW.md` and `03_RATIONALE.md` (checked before writing: each body begins with its own opening line, the contract ends at its `## Verdict` line, both header-named paths carried a portion, no partial return reported). Nothing was added, completed or reformatted. **G-1 is the round's sharpest finding**: the real `os` is reachable one attribute hop from the shim itself (`shim.path` **is** the real `posixpath`, whose `.os` is the real `os`), so the residual list the whole task turns on omitted the *easiest* escape family while naming the two hardest. The gate closed it with **sentences plus a measurement (C-1/C-2/C-3), not code** — the rule-85 call this task was set up to make. G-3 and G-5 are findings against **stage 1's** FR-6/AC-8 and BC-8; the gate **scoped them by condition (C-6, C-7) rather than rolling back**, which is its call to make, so `01_REQUIREMENT_ANALYSIS.md` stands unedited and QA reports those two criteria against the gate's scoping. Cap re-derived per R-61 over the gate's own element list: **31–34** executable lines; NFR-2's **40** upheld **unamended**. C-2's probe is owned by stage 4 (the developer holds a shell), so no PM measurement round is needed. No rollback. Intervention re-checked: absent. `.harness/agents/dev-*.md` absent → dispatch `harness-kit:developer` (single-Developer mode). |
| 6 | 2026-08-16 | 4 → 5 | Stage 4 returned `READY FOR REVIEW` with **`verify_all` PASSED**, re-measured independently by the PM: **PASS 20 / WARN 0 / FAIL 0 / SKIP 1, exit 0** (B.3 the only SKIP; the +1 is the new B.6). Stage-5 gate satisfied. **C-2 measured before E-1 and again after: both real-`os` attribute-chain variants left their marker both times** — the gate's G-1 expectation held, so the family is named **open** in both claim-surface texts rather than closed. **V-1 flip taken: all three `subprocess` variants moved from marker-present/exit-1 to `LoadRefused` / no marker / `os restored True` / `19 defined, 0 run, 0 passed` / exit 2**, while `ctypes` still escapes and is written as a residual. **30 net executable lines** against the gate's 31–34 and NFR-2's 40. **C-8: kept** — on the finding that it is a `-1/+1` rewrite of the pre-existing leak check, so dropping it saves **zero** lines and falsifies BC-5; QA reports it NOT-DISCRIMINATING. `bin/sc` digest re-checked **identical** (AC-10 holds). Working tree: `.harness/rejected-decisions.md`, `.harness/scripts/{baseline.json,check-sc-contracts.py,verify_all.sh}`, `docs/dev-map.md` (+`CONTEXT.md` from stage 1); `docs/batches/**` untouched by the pipeline and stays unstaged. Intervention re-checked: absent. **Three upstream defects reported, none fixed downstream** — (1) C-2's evidence column asks for an `os restored` line that does not exist on the *escape* path (gate-owned, recorded not routed: it does not change any condition's outcome and the developer recorded the reading plus the reason); (2) G-3's FR-6/AC-8 wording stays unamended, standing scoped by C-6 (analyst-owned; the gate already ruled scoping over rollback at stage 3, so re-opening it now would re-litigate a stage-3 decision on no new evidence); (3) `.harness/rules/50-singbox-cli.md:29-30`'s "14 contract assertions" was **already** stale before this task, so it is **T-32's** (R-94), not a sentence this change falsified — carried to delivery as a note. Advance to stage 5 (code-reviewer). |
| 7 | 2026-08-16 | 5 → 4 (**rollback 1**) | Stage 5 returned **`ROLLBACK TO DEVELOPER`** — 0 CRITICAL / **1 MAJOR** / 8 MINOR / 1 NIT. The reviewer holds no write capability; the PM **transcribed both portions verbatim** to `05_CODE_REVIEW.md` and `05_RATIONALE.md`. Transcription check before writing: the contract body begins with **its** declared opening line — `> Contract portion. Rationale: 05_RATIONALE.md (absent = none written).`, which is the line the `harness-kit:code-reviewer` contract declares (no H1, unlike stage 3's) — and ends at its `## Verdict` line; both header-named paths carried a portion; no partial return. Nothing added or repaired. **CR-1 (MAJOR) is the R-74 defect this project repeats most**: a *new* claim-surface sentence claims `SB_BIN`'s non-existence neutralises "a `subprocess.run` reached from an assertion", true only of the checker call — `cmd_status`'s `ip`, `cmd_sysproxy`'s `sudo -u` and `cmd_log`'s `tail -f` are unguarded, and `fixture()`'s `SYSTEMD = OPENRC = False` *routes into* the last one. Prose-only fix, zero executable lines. Routed to the **developer**, correctly: the sentence is the developer's own new text, not a design or requirement gap. **Rollback streak at stage 4: 1** (limit 3). |
| 8 | 2026-08-16 | measurements | The reviewer asked the PM for four readings it had no shell for. Taken: **CR-3 confirmed** — `bash -c 'cat < /nonexistent 2>/dev/null'` prints `No such file or directory` on the inherited stderr, and the same holds for `floor_of < missing 2>/dev/null` (`value=[]` with the leak line printed), so CR-3 stands and is **not** struck. `sha256sum bin/sc` = `81d65da8…b312` (**AC-10 holds**, `bin/sc` byte-identical). `stat` = **755** on `check-sc-contracts.py` (NFR-3 holds). `git status --short` and `--numstat`: only the five intended files + `CONTEXT.md` + the two unstaged `docs/batches/**` files; **`docs/tasks.md` is NOT modified** — the environment snapshot the reviewer flagged as stale was indeed stale, and its `04` counterpart was right. Handed to round 2. |
| 9 | 2026-08-16 | 4 (round 2) → 5 (round 2) | **Round record, written here per the no-changelog rule:** `round 2 · corrected the assertion-phase claim in both claim-surface texts (scoped to what SB_BIN and SYSTEMD=OPENRC=False actually do, site inventory read first-hand in bin/sc) and mirrored it into docs/dev-map.md, which carried no equivalent sentence; qualified the COVERED: clause; named import posix as family (i)'s cheapest open member after measuring it; scoped the "no committed artifact runs bin/sc as a program" universal; restored B.4/B.6 off-path quietness by reordering 2>/dev/null before the <; replaced baseline.json's decayed line citation with a function+step reference; struck a false ground from candidate (2) of the committed rejected-decision; repaired citations in 04_DEVELOPMENT.md · why: CR-1's sentence claimed more than the code enforces (the R-74 defect class) and the eight MINOR/NIT findings were precision defects on permanent records · findings: CR-1 … CR-9 (CR-10 refused by design, travels as RES-7)`. The developer **improved on the reviewer's suggested sentence rather than transcribing it** — it found a fifth unguarded site the review missed (`_doctor_service`'s `else`-arm, `bin/sc:2831`) and established that `SB_BIN` neutralises an **argument**, not a site (two of the three `[SB_BIN, …]` calls sit behind `shutil.which()` and never run at all), then named what actually holds the phase: which functions `TESTS` names, evidenced by `strace`. It also **measured** CR-6's `posix` route rather than inheriting it (marker present → open) and **narrowed the reviewer's CR-7 premise** with a measurement (a `__dict__`-copying shim still has `shim.os is os`). Net executable **unchanged at 30** against the cap of 40; every round-2 edit is prose, comment or a two-token move. `verify_all` re-measured by the PM: **PASS 20 / WARN 0 / FAIL 0 / SKIP 1, exit 0**. Intervention re-checked: absent. Re-dispatch stage 5 for round 2. |
| 10 | 2026-08-16 | 5 (round 2) → 4 (scoped correction) | Stage 5 round 2 returned **`APPROVED WITH MINOR FINDINGS`** — 0 CRITICAL / 0 MAJOR / **2 MINOR / 1 NIT**. **Round record (reviewer's, written here):** `round 2 · re-reviewed the full current tree against round 1's CR-1…CR-10; closed 9 findings on first-hand evidence, re-counted 30 net executable at the shifted line numbers, retired RES-2 on the developer's set-difference reading, raised CR-11 (MINOR, new) + CR-12 (NIT, new); verdict moved ROLLBACK TO DEVELOPER → APPROVED WITH MINOR FINDINGS · why: the MAJOR is discharged with a better sentence than the one proposed and all eight MINOR/NIT are fixed or correctly re-argued · finding ids: CR-1…CR-9 closed, CR-10 open by ruling → RES-7, CR-11 → RES-11, CR-12`. Both portions **transcribed verbatim**, replacing round 1's content at each path (opening lines and `## Verdict` line checked first; the reviewer's own transcription header and its restated verdict sit outside the bodies and were **not** written into the files). Two closures are worth the record: the reviewer found **its own round-1 CR-1 sentence was wrong** where the developer corrected it (`shutil.which` returns `None` for the absent `SB_BIN`, so two of the three sites never run), and it **accepted the developer's alternative discharge of CR-4** (drop the decaying line citation, name the function and the steps) as the better fix. **CR-11 is the rework's own new defect** — the "fifth site" the developer added is not reachable under the fixture (`_doctor_service` short-circuits at `bin/sc:2816`), i.e. a false *warning* rather than a false assurance. The reviewer rates it MINOR, prose-only, and states it **does not need a stage-5 re-review**. Routed to the developer as a **scoped correction round (not a rollback — the verdict is approving)**: CR-11 + CR-12 only, zero executable lines, then stage 6. Rollback streak at stage 4 remains **1**. Intervention re-checked: absent. |
| 11 | 2026-08-16 | 4 (round 3) → 6 | **Round record:** `round 3 · CR-11's two false clauses rewritten in both claim-surface texts (check-sc-contracts.py:68-78, docs/dev-map.md:173-179) and in the two stage-4 docs that repeated them; CR-12's test_count-readers clause moved out of the no-readers sentence in baseline.json:7 · why: the round-2 "fifth site" was unreachable under the fixture, and a paragraph whose whole authority is citation accuracy cannot carry a citation a reader can falsify · findings CR-11 (MINOR), CR-12 (NIT). 0 executable lines.` The developer **confirmed CR-11 against `bin/sc:2805-2860` first-hand** rather than accepting it, and added two details the review did not have (the inner pair is `if SYSTEMD:` / `else:`, not `elif OPENRC:` — which is precisely why `_doctor_service` goes dead while `cmd_log` is routed into). It again **improved on the reviewer's suggested sentence** (resolving the dangling three-tool list instead of leaving a reader to work out which tool is live) and **preserved both files' line counts exactly, so zero citations moved**. Net executable **still exactly 30**; `--numstat` byte-for-byte the round-2 profile. `verify_all` re-measured by the PM: **PASS 20 / WARN 0 / FAIL 0 / SKIP 1**, with **B.6 `Assertion floor never below its last committed value` PASS** and B.4 `19 defined, 19 run, 19 passed`. Stage-6 gate satisfied (stage 5 approving, corrections in). Intervention re-checked: absent. Advance to stage 6 (qa-tester). |
| 12 | 2026-08-16 | 6 → 4 (**rollback 2**) | Stage 6 returned **`CHANGES REQUIRED`** — **1 CRITICAL / 3 MINOR / 1 NIT**; **13 AC PASS / 1 FAIL / 0 BLOCKED**; **20 mutants run, 11 killed, 9 surviving, reported rather than rounded up**. **DEF-1 (CRITICAL) is this task's own thesis turned on its own artifact, and QA measured it rather than arguing it**: `os._execvpe` and `os._spawnvef` are process-start names present in `dir(os)` **today** on POSIX and match **no** prefix in the tuple (every tuple spelling is public), so half (a)'s completeness sentence is false in **both** claim-surface texts — a scratch subject calling `os._execvpe` **replaced the loading interpreter with `touch`**, marker PRESENT, **exit 0, no summary, no refusal**, and `os._spawnvef` forked (`execve=1 clone=1`) before its exec was refused. Family (ii) compounds it by framing the gap as a **future** CPython name when a present one exists. **This is T-28's CR-1 one level down** — the exact defect the task exists to stop, found in the artifact meant to stop it. Routed to the **developer**: the fix is prose at **0 executable lines** (scope the claim to public spellings, name the private-helper route open) because FR-3 / K-3 / Q-2 already refuse adding names — a longer list is the defect repeating. DEF-2 (leak check compares against the captured value, so a capture-after-displace reorder leaves `Popen` permanently displaced with the suite green), DEF-3 (**B.6 fails open**: a multi-valued `floor_of` result is a bash arithmetic error that falls to the PASS arm) and DEF-4 (a `_doctor_run` citation naming the runner's `subprocess.run` line rather than its `def`) travel with it. QA's own `verify_all`: **PASS 20 / WARN 0 / FAIL 0 / SKIP 1, exit 0**, three runs byte-identical; host witness identical at start, middle and end; `bin/sc` digest unchanged; nothing left behind. **Rollback streak at stage 4: 2** (limit 3 — the next stage-4 rollback stops the task and escalates). Intervention re-checked: absent. |
| 13 | 2026-08-16 | 4 (round 4) → 5 (round 3) | **Round record:** `round 4 · half (a)'s completeness claim scoped to the PUBLIC spellings and family (ii) re-written from prospective to present in both claim-surface texts and in rejected-decisions.md (0 executable lines); the real Popen bound once at import (REAL_POPEN) so load() has no capture left to reorder (0 net); floor_of() now also decides what a floor is (one run of digits) with both comparisons 10#-pinned, so no unparseable reading reaches (( )) (+1); _doctor_run cited at its def · why: DEF-1 was the task's own defect one level down, DEF-2 was a future-edit hazard with a 0-line removal available, DEF-3 was a control that failed OPEN · findings DEF-1 … DEF-4.` **The developer derived the correction from its own enumeration rather than from QA's two examples**: 402 names in `dir(os)` on CPython 3.12.3, 22 matched by the tuple, and of the 380 unmatched **exactly two** start or replace a process (`_execvpe`, `_spawnvef`) — **no public spelling is unmatched**, which is what makes the scoped sentence true rather than merely narrower. It also **measured the refusal ground instead of asserting it**: `os.path.os._execvpe(...)` escapes identically, so a name denies a spelling, not a capability — the tuple stays unchanged (FR-3/K-3/Q-2 upheld) and **no capability closure is proposed to the architect**. DEF-3's fix is proven non-vacuous in six states (duplicate key, `018` vs `19`, `17` vs `19`, and BC-2 ×4 each SKIPping with **exactly one** line and no FAIL), and it closed **B.4's identical fail-open** as a scoped extension. Net executable **31 / 40** (E-4 now 14, inside the gate's 12–15 allowance), counted twice by two methods. PM re-measured: `verify_all` **PASS 20 / WARN 0 / FAIL 0 / SKIP 1**, B.4 and B.6 both PASS, `bin/sc` digest unchanged. **`guard-rm.sh` blocked an `rm`-free command a fifteenth time** (an `ls /etc/sing-box` inside a compound); bypass **not** set. Because round 4 changed **executable** lines after stage 5's approving verdict, dispatch a **scoped stage-5 re-review** before returning to QA. Intervention re-checked: absent. |
| 14 | 2026-08-16 | 5 (round 3) → 6 (round 2) | Stage 5 round 3 returned **`APPROVED WITH MINOR FINDINGS`** — 0 CRITICAL / 0 MAJOR / **1 MINOR (CR-10, carried by its own round-1 ruling and frozen by design) / 1 NIT (CR-13)**; spec/design-fidelity **clean**. **Round record:** `round 3 · scoped re-review after round 4 · CR-11 and CR-12 closed on first-hand evidence, CR-10 carried and re-anchored, CR-13 raised (NIT), RES-11 retired and RES-11…RES-14 filed anew, D-4 and D-5 adjudicated ACCEPTED · why: round 4 changed executable lines after an approving verdict · findings CR-10, CR-13`. Both portions **transcribed verbatim** (opening lines and `## Verdict` checked; the reviewer's transcription header sits outside the bodies and was not written). **The reviewer re-derived DEF-1's closure from `/usr/lib/python3.12/os.py` rather than accepting the developer's own audit script** — the tuple matches exactly **22** names and **no public process-start spelling is unmatched**, which is what makes the scoped sentence *true* rather than merely narrower — and it explained the mechanism (a copied function's `__globals__` is the **real** `os` dict, so the private helpers call the real `execv`/`fork` whatever the shim says). It **accepted both new drifts**: D-4 (`REAL_POPEN` at module scope) and D-5 (`floor_of()` deciding what a floor *is*, `10#`-pinned, extended into B.4). **All four QA defects confirmed closed.** It ruled on QA's **9 survivors**: record all, close none here, and it filed the *class* as RES-12 (**the denial has zero committed controls**) rather than letting three mutant rows speak for it. **RES-14 measured by the PM immediately: `3.12.3 402 22` — the denominator on the claim surface is exact, no correction owed.** Net executable **31 / 40**. Return to stage 6 for a re-run. Intervention re-checked: absent. |
| 15 | 2026-08-16 | 6 (round 2) → 7 | Stage 6 round 2 returned **`APPROVED FOR DELIVERY`** — **14/14 AC PASS**, 0 FAIL, 0 BLOCKED, 0 NOT-DISCRIMINATING as a verdict (four items reported non-discriminating **inside** the AC-12 list, each re-measured rather than copied). **AC-3 moved FAIL → PASS on a re-measured artifact, never on a re-worded criterion**: the shim's denied set read directly off the object is 22, set-equal to the tuple-matched set, and an `ast` scan of `/usr/lib/python3.12/os.py` yields exactly `['_execvpe', '_spawnvef']` among the 380 unmatched. **Round record:** `round 2 · re-verified the round-4 tree from scratch and replaced both stage-6 documents with a current-state report · because round 1's CHANGES REQUIRED needed re-measurement, not amendment, and round 4 moved 175 lines so every round-1 mutant file was stale · findings DEF-1…DEF-5 plus CR-13 and RES-3/4/12/13/14`. **23 mutants run (18 mutating a delivered artifact), 6 killed, 12 surviving — every survivor named**, three of them newly found (M21 `REAL_POPEN` rebinding, M22/M23 the two lines that closed DEF-3). DEF-5 was QA's **own** schema gap and it resolved it as a named unit per rule 70 rather than inventing a section. `strace` over a B.4 run: `execve` **1**, `clone`/`clone3`/`fork`/`vfork` **0**. Nothing BLOCKED; no operator obligation created. Both stage gates for stage 7 are satisfied (stage 5 approving, stage 6 approving). Intervention re-checked: absent. |
| 16 | 2026-08-16 | 7 delivery | `07_DELIVERY.md` composed. **Entropy watch: `.harness/scripts/entropy-cadence` does not exist on this host (R-88), so the cadence resolved NOT-DUE fail-open** — no scan dispatched, no `## Entropy watch` section written, exactly as at T-28/T-29/T-30. `docs/tasks.md` was at **299/300** after the update: **2 completed rows (T-29, T-30) and 9 already-closed row lines were rotated to `docs/tasks-archive.md` in preference to displacing any open row**, R-95/R-96/R-104 were rotated **at closure** with their closure text, and **R-93 and R-102 were amended in place** because each keeps an open half. 5 rows filed: **R-107 … R-111**. **`archive-task.sh` rotation: FIRED** — `Harvested 4 insight(s)` and `Rotating 4 old insight(s) to insight-history.md`, leaving `.harness/insight-index.md` at **30/30** with no hand edit. That is the **fifth independent confirmation** of T-27's R-18 fix (T-28, T-29, T-30, and now this delivery), and the rotation branch was again on the critical path. Commit + push per rule 80 (durably authorized), `docs/batches/**` left unstaged, message written to a file and applied with `git commit -F` (R-86 fired a fifteenth time during this task). **RES-1 (B.6's post-commit reading, tree 19 vs `HEAD` 19) is taken after the delivery commit and recorded in the archived `07_DELIVERY.md` by a second commit** — never faked, and never by rewriting the first. |
