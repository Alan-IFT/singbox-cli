# Changelog

## [Unreleased]

## [0.6.0] - 2026-08-28

次版本号而非补丁号，按 `0.2.0` 立下的同一条规矩——**行为变更，脚本看得见**：`sc doctor` 从八节变为**九节**（新增「策略路由」，且它可以让一台此前退出 0 的机器现在退出 1）；`sing-box.service` 的 `ExecReload=` 被**删除**（`systemctl reload sing-box` 从「返回成功但什么都不做」变成明确失败）；安装脚本新增一个**会写入 `/etc/systemd/networkd.conf.d/` 的步骤**。项目仍在 `0.x`，所以这些计入次版本号。

主题只有一个：**TUN 接口存在不代表策略路由仍然完整**——以及随之而来的、按「预防 > 检测 > 兜底」排序的三层处理。

### 根因

- **规则是被 `systemd-networkd` 删的，而这一版从源头堵住了它。**

  `systemd-networkd` 的 `ManageForeignRoutingPolicyRules=` **默认为 `yes`**，语义是「接管**别的程序**创建的策略路由规则，并删掉它不认识的那些」—— sing-box `auto_route` 建的正好属于这一类。于是任何一次 networkd 重载（网络变化、挂起恢复、`systemctl restart systemd-networkd`）都可能把它们抹掉，而 sing-box 进程、`sb-tun` 设备、表 2022 的默认路由全都完好无损 —— 这就是「服务在跑却静默直连」的由来。同类受害者不止我们：[netbirdio/netbird#4578](https://github.com/netbirdio/netbird/issues/4578)。

  **为什么 sing-box 不自愈**：从 1.13.18 二进制的 Go 符号表可以直接读出，`sing-tun` 对两套内核状态的一致性策略是**不对称**的 —— nftables 那条路径有 `reconcileRedirectRoutesLocked` / `calculateRedirectRouteChanges`（有对账），而 `NativeTun` 的 `ip rule` 只有 `setRules`（`Start` 时一次）与 `unsetRules`（`Close` 时一次），**没有任何重装入口**。规则是 fire-and-forget，装完就再也不复查。

  **业界已有答案，而且就编译在同一个二进制里**：sing-box 静态链接了 Tailscale，后者撞上过一模一样的问题并已解决 —— 订阅 netlink 的规则删除事件后立即重装（符号 `netmon.RuleDeleted`、`osrouter.(*linuxRouter).onIPRuleDeleted`；上游 commit [b3af74e「restore Linux ip rules when systemd deletes them」](https://github.com/tailscale/tailscale/commit/b3af74e4ff334c37427522119c77d59f2c737be9)、先行的[只记日志版本](https://github.com/tailscale/tailscale/commit/2ba36c294b51b672073a36a599a61c758d0ffafa)、以及讨论重装顺序的 [#10857](https://github.com/tailscale/tailscale/issues/10857)）。

  据此，本版的修复顺序是**预防 > 检测 > 兜底**，而不是只做兜底。

### 新增

- **`install.sh` 安装 `/etc/systemd/networkd.conf.d/singbox-cli-keep-foreign-rules.conf`（`ManageForeignRoutingPolicyRules=no`），从源头消除删除行为。** 这是这一版真正的修复：代价为零、不重启任何东西、不掉一个连接。只在**装有 `systemd-networkd`** 的机器上安装（判断依据是 `list-unit-files` 而非 `is-active` —— 保护必须在 networkd 下次启动**之前**就位）；只用 `try-reload-or-restart`，因此**绝不会把一个没在运行的 networkd 启动起来**；写入用 `install -m 644`，重跑写入同样的字节，天然幂等。`uninstall.sh` 会删除它并重载，让本机行为回到发行版默认。

- **`sc doctor` 第 6 节新增「规则保护」一行**，报告这层保护在不在。它是三态的，而中间那态才是重点：networkd 未安装 → 正常（这个删除者根本不存在）；drop-in 已就位 → 正常；**networkd 已安装但 drop-in 缺失 → 异常**。注意最后一种在**当前规则完好**时也报异常 —— 因为这样的机器在下一次 networkd 重载时必然再丢一次，等到规则真的没了才报，就永远只能事后诸葛亮。

### 变更

- **`sc watchdog` 降级为「兜底」，不再是推荐解法。** 它只能在事后发现损坏，并靠**重启 sing-box** 复原（会中断所有活动连接），而已知删除者已被上面那份配置在源头挡掉。保留它是因为那份配置挡的是**一个**删除者而非所有：手工 `ip rule flush`、别的 VPN 的清理、将来才出现的管理器。帮助文本与两份 README 都改成明说这一点，并指出**若经常需要它，说明还有别的东西在删规则**，该从「规则保护」那一行开始查。

- **删除 `sing-box.service` 里的 `ExecReload=/bin/kill -HUP $MAINPID`。** 它是双重死配置：`sc` 从不调用 `systemctl reload`（所有改动文档的路径都走 `restart_service()`），而且实测 1.13.18 —— **sing-box 收到 SIGHUP 既不重载也不退出**。留着它唯一的作用是让管理员以为 `systemctl reload sing-box` 有效，而那条命令返回成功、什么也没做。`Restart=on-failure` / `RestartSec=5` 原样保留，并由测试守着不被这次删除顺手带走。

- **`sc doctor` 第 6 项：策略路由。以及一句必须写下来的话——TUN 接口存在不代表策略路由仍然完整。**

  实测故障（0.5.3 / sing-box 1.13.18 / systemd）：`sing-box.service` 是 `active`、`sb-tun` 在、表 2022 里 `default via 172.19.0.2 dev sb-tun` 也在，而 `ip rule` 只剩 `local` / `main` / `default` 外加一条 `9002: from all iif sb-tun goto 9010 [unresolved]`。9000 / 9001 / 9003 / 9010 全部消失，于是没有任何规则把本机流量送进那张表，全部走物理网卡。Global 模式下 `icanhazip.com` 返回的是本机直连 IP，而**此前的八项检查每一行都是 `[正常]`** —— 服务在跑、TUN 在、配置校验通过，没有一项在看内核到底怎么选路。`sc reload` 之后规则齐全，`ip route get 1.1.1.1` 回到 `via 172.19.0.2 dev sb-tun table 2022`。

  新的一节检查五个条件，每个条件对应这次故障的一种呈现方式：路由表里有经该 TUN 的默认路由；`ip rule` 里没有 `[unresolved]`；每个 `goto` 都有目标规则（换一个不打印 `[unresolved]` 的内核，同一个截断仍然抓得到）；**除 TUN 自己的 `iif` 规则之外**还有规则把流量送进那张表（这条排除是关键：本次故障剩下的恰好就是那条 `iif` 规则，只问「表被引用了吗」会把泄漏判成健康）；`ip route get 1.1.1.1` 确实经过该 TUN。任一不满足即 `[异常]`、退出码 1，并附一行说明存在直连泄漏风险、可运行 `sc reload`。

  接口名与路由表号**从 `config.json` 读出**（`interface_name` / `iproute2_table_index`），常量只是文档没写这两个字段时的回退值 —— 在探测点写死数字，会让改过 override 的机器被误报为损坏。位置在 TUN 之后、Clash API 之前：设备存在是这一节的前提，而规则截断正是「出口 IP 是本机地址」的解释。

  **全程只读**：三条 `ip` 查询，没有 `ip rule add`。`ip route get` 是从内核路由表里选一条路由打印出来，**不发送任何报文**，所以这项检查不触网、代理已断的机器上照样能跑。设备不存在时只报这一个事实，不再往下派生四条关于「一个不存在的设备的路由表」的异常。

- **`sc watchdog on|off|status`：可选的策略路由自动修复，默认关闭。**

  只修一种故障 —— 上面那种。诊断复用 `_route_policy_probe()`，与 doctor 第 6 项是同一个判断，不存在第二套「什么叫健康」。systemd `oneshot` + timer（`OnBootSec=2min`、`OnUnitActiveSec=1min`）；非 systemd 主机明确拒绝并说明，而不是静默失败。

  状态机与防抖：健康 → 什么都不做、不写日志（每分钟一次的东西一旦成功也打日志，日志就没人看了）；失败 → 等约 5 秒复查 → 仍失败才动手。这一步是必需的，`sc reload` 期间 sing-box 正在自己重写规则，单次采样与真故障长得一模一样。修复用 `systemctl restart sing-box`，**不用 `sc reload`**：配置没变，需要重建的是内核状态而不是文档。重启后**再查一次内核**来判断是否恢复 —— systemctl 返回 0 只说明进程起来了，不说明规则回来了。

  三条不做的事，都是结构上不做而非约定不做：**不访问任何网站 / DNS / 节点**（输入全是本地内核查询，所以某站不可达、DNS 抖动、节点测速失败都不可能触发重启 —— 节点可用性是 `sc ping` 的问题）；**不启动被 `sc off` 禁用的服务**（`is-active` 与 `is-enabled` 都为否即视为用户的决定，记一条「按兵不动」后返回）；**5 分钟内不重启第二次**（重启循环比泄漏更糟，它每次还要掐断全部连接）。并发由 `flock` 挡住：定时器在上一次的 5 秒确认窗口里再次触发时会直接退出，否则它会自己再采两次样、绕过冷却重启第二次。

  `sc watchdog status` 显示四件事：timer 是否启用、最近检查时间、最近检查结果、最近一次自动修复时间；状态存在 `/var/lib/sing-box/watchdog.json`。

### 变更

- **`install.sh` 只刷新「已经开启」的 watchdog，绝不替用户开启。** 判断依据是 systemd 对 timer 的 `is-enabled`，不是单元文件是否存在 —— `sc watchdog off` 留下的文件不是同意。它委托 `sc watchdog on` 完成，而后者写的是常量字节、并对已启用的 timer 重复 enable，所以刷新的幂等性来自被委托的操作本身，而不是又一层判断。`uninstall.sh` 增加这两个单元的 disable 与删除，从未开启过的机器上两条都是空操作。

### 测试

- 新增 8 个测试。策略路由的判定被拆成一个**纯函数**（三段 `ip` 输出进、判定出），所以五种故障各是一组字符串，全程不跑 `ip`、不碰本机路由：健康基线、`goto ... [unresolved]`、缺 9010 nop、表在但没有规则送流量进去、表里默认路由走了别的设备、以及 `ip route get` 仍走物理网卡。另有一条断言：同一份字节在「按文档读到的名字」下健康、在「写死的默认名」下异常 —— 这是**唯一**能证明接口名和表号真的来自 `config.json` 的形状。

  watchdog 的时间由测试自己持有（`_Clock` 替换 `time.time` / `time.sleep`），健康与否由脚本化的判定序列给出，所以「第一次失败不重启」「连续两次才重启」「冷却期内不重启」「重启后恢复」「重启后仍失败则记录并非 0 退出」「`sc off` 时不启动」六条都是确定性的，没有真的 sleep、没有真的 systemctl。单元文件内容、on/off/status 的重复执行幂等、以及 `install.sh` 那段升级逻辑（`sed` 抽出来对着桩跑，两个分支各断言一次）同样覆盖。doctor 只读由「断言每一条子进程 argv 都是 ip 查询、且临时目录内容前后一致」来守。

- 根因修复另有 3 个测试：出厂的 networkd 配置内容（**必须是 `[Network]` 节** —— 写错节名会让文件解析通过却静默失效）、安装器那段逻辑（`sed` 抽出来对着桩跑：无 networkd 不写、有 networkd 才写、重复执行幂等、只 `try-reload-or-restart` 绝不 `start`/`enable`）、以及 `sing-box.service` 不再含 `ExecReload`。还有一条对拍：**安装器写入的路径与 doctor 查找的路径必须逐字相同** —— 两者分属不同程序，没有这条断言，改一处就会让 doctor 把已保护的机器报成有风险。另 7 个变异全部报红（doctor 恒判已保护、doctor 查错路径、安装器不装、安装器擅自 `start networkd`、删掉存在性守卫、死 `ExecReload` 复活、配置节名写错）。

- 变异测试 12 个，3 个逃逸，**每一个逃逸都是测试的真实漏洞而不是等价变异**：删掉 `iif` 排除条件后仍然全绿（样本里那条 `iif` 规则没写 `lookup 2022`，于是排除逻辑从未被执行到 —— 而它恰恰是本次故障剩下的那条规则）；默认路由检查里删掉接口名后仍然全绿（缺一个「表里有默认路由但走 eth0」的用例）；`ip route get` 只看有没有 `dev` 同理。补齐样本与用例后 12 个变异全部报红。

## [0.5.3] - 2026-08-22

把「本构建不做 IPv6」这句话补完整。0.4.0 移除 IPv6 检测子系统时，这个立场只在 DNS 层落了地（AAAA 一律本地返回空 NOERROR）——绕过系统 DNS 拿到 v6 地址的软件不在其内：应用内置 DoH 的应答、写死的 v6 字面量、P2P 交换来的地址。TUN 只有 v4 地址，这类流量在双栈宿主上**整个绕过代理直连**，要么泄漏真实地址，要么对直连不通的目标悄悄挂死，用户无从归因。

### 变更

- **IPv6 流量现在被收进 TUN 并立即拒绝，而不是绕行。** 两行，各自一半，缺一不可：TUN 挂一个 ULA 地址（`fdfe:dcba:9876::1/126`），不提供任何连通性，只为让 `auto_route` 装上 v6 路由、流量**进来**；`route.rules` 加一条 `{"ip_version": 6, "action": "reject"}` 回以 RST / ICMPv6。

  为什么是「拒绝」而不是「转成 v4」：连接层不存在后者（那是 NAT64，一整套基础设施）。能选的只有失败得多快——毫秒级的主动拒绝正是让应用自身的双栈回退（Happy Eyeballs）立刻改走 IPv4 的机制。**拒绝是实现「自动转 v4」的手段，不是它的对立面**；DNS 层的空 NOERROR 是同一件事在另一层的同一做法。绝不用 `drop`：静默丢弃就是本条规则要消灭的那种挂死。

  位置与 AAAA 规则同一套推理：在 `hijack-dns` **之后**，发往 v6 解析器的 53 端口明文 DNS 仍被劫持并作答（继续吃 AAAA 抑制），而不是被一个 RST 逼着解析器重试；在两条 `clash_mode` **之前**，切到 global / direct 也切不掉它——出问题的时候人切的正是这两个模式。

  代价直说：v6-only 的目标从「可能靠绕行直连成功」变成「干脆利落地不可达」——DNS 层早已让 v6-only 域名不可达，这只是把字面量地址那扇门也关上。无 v6 的宿主行为几乎不变（内核的 ENETUNREACH 换成 sing-box 的 RST，同样快）；链路本地流量（fe80::、组播）不走默认路由、进不了 TUN，不受影响。想要 IPv6 的宿主：override.json 里 `$replace` 掉 `dns.rules`、`route.rules` 和 `inbounds`，或者换个工具。两个 README 的「三条默认值」段落同步更新。

  实测（1.13.18，发布当日在真机升级后补上）：`sing-box check` 接受该文档；`ip -6 route get` 显示 v6 目标经 `sb-tun`（策略路由表 2022）——机制本身与宿主有无原生 v6 无关；对 v6 字面量地址的 `curl` 在 **33 毫秒**内被终止（`SSL_ERROR_SYSCALL`：sniff 先于 reject，TCP 先被接受、随后被路由判决重置——该顺序是必要的，`hijack-dns` 的协议匹配依赖 sniff），远快于 5 秒超时；强制 AAAA 的域名请求则在 DNS 层就立即失败。仍未实测的只剩双栈宿主上应用回落的顺滑度，那来自协议行为的推理。

### 修复

- **0.4.0 删掉 doctor 第 4 节时留下的一串化石。** README 退出码表格声称 doctor 会检查「`dns.rules` 第一条不是 AAAA 决策对应的规则」——**这个检查从 0.4.0 起就不存在**（对 config.json 的一切手改由漂移检查覆盖，按 doctor 自己的规矩也不该有第二个意见）；doctor 头注释的因果排序还在给这个不存在的段落排位，且仍说「九项」，而 0.4.0 的 CHANGELOG 自己写着「从九节变为八节」；`sc help` 两种语言也仍说「九项」；段落 docstring 编号 S1–S9 中间空着 S4，还让 `TUN_IFACE` 注释里「doctor 的 S5」指到了错误的段。表格行删掉那个从句，九改八，编号补齐——`TUN_IFACE` 那处引用随之自动变对，一个字都没改它。

### 测试

- `t_generate_config` 新增：TUN 携带 ULA v6 地址；reject 规则位于 `hijack-dns` 之后、第一条 `clash_mode` 之前（**位置就是契约**）；文档整体照旧过 `sing-box check`。变异验证：删规则、去地址、把规则挪到 `clash_mode` 之后，三者各自当场报红。

## [0.5.2] - 2026-08-22

一次全量代码审查的结果，没有新功能。0.5.1 立下的规矩——凡是 `sc` 没写的文本，上屏前必须过 `_plain()`——自己漏掉了两处，外加中文帮助里一行没翻的注释。

### 修复

- **`sc add` 的成功行打印地址时没有中和。** 0.5.1 说地址「在唯一打印地址的地方过 `_plain()`」——数错了：`sc ls` 之外，`Added: {tag} ({type} → {server}:{port})` 也打印它，而 vmess 链接的 base64 正文可以把任意字节放进 `add` 字段，粘贴链接的人从头到尾看不见。地址在 `nodes.json` 里仍然逐字节保持原样（sing-box 需要它），两处打印现在过同一个中和器；`sc ls` 处那句「唯一打印地址的地方」的注释也改成不再声称唯一。
- **`sc ls` 自动选择行的「→ 当前节点」没有中和。** 这个名字是 Clash API 的应答，即外来文本——`sc doctor` 的 node delays 行对同一个值早就过 `_plain()`，列表却直接打印。同一事实两个屏幕两种处理，正是本项目自己禁止的形状。
- **中文帮助里 `sc sub add` 示例的注释是英文。** 「# wrap it in single quotes」→「# 整个链接用单引号包起来」。占位符对拍、帮助与命令表对拍、README 双语结构对拍此外全部通过。

### 测试

- `t_foreign_text_on_screen`：`t_tag_minter` 的端到端断言够不到的两个屏幕——`sc add` 收一条 `add` 字段带转义序列的 vmess 链接后，成功行里不含 `\x1b`；`sc ls` 的自动选择行拿到一个带转义序列的 API 应答后同样如此。变异验证：分别撤掉两处 `_plain()`，各自的断言当场报红。

## [0.5.1] - 2026-08-22

一次代码审查的结果，没有新功能。三处修复，起因是同一件事：**`sc sub` 让节点的名字第一次变成了远端提供的内容**，而此前的代码是按「名字来自用户自己粘贴的链接」写的。

### 修复

- **机场可以用节点名改写你终端上看到的东西。** 分享链接的 fragment 会原样变成 tag，而 `sc ls` / `sc ping` 把它直接打印到终端。一个名字里带 `ESC [ 2K ESC [ 1A`（清除本行 + 光标上移）的节点，在列表里会**把上一行擦掉并覆盖**——足以让某个节点隐身，或让一行伪装成另一个节点。订阅之前这需要用户自己攻击自己；订阅之后它是机场说了算。

  修在 `_unique_tag()` ——所有入库节点的**唯一铸造者**（`sc add`、`sc import`、订阅刷新三条路径都经过它）。中和走 `_plain()`，本文件已有的那个中和器，所以这里没有第二套 CSI 语法；这也是结果**可读**而不只是无害的原因：只删 ESC 会在名字里留下 `[2K[1A` 这样的残渣，正是 `_plain()` 当初被写成能识别整段序列的理由。剩下的散落 C0 / DEL / C1 再过一道过滤。

- **vmess 的 `ps` 不一定是字符串。** `"ps": 5` 会让 tag 变成整数，而 `load_nodes()` 要求非空字符串——文档写进去之后才被拒（会回滚，但报错指向 `nodes.json` 而不是那条链接）。同一处修：铸造者的**后置条件现在正好等于读取者的前置条件**，两个契约合成一个。

- **`sc ls` 打印的地址没有中和。** tag 现在从构造上就是安全的，但 `server` 必须对 sing-box 保持逐字节不变，所以它在**唯一打印地址的地方**过 `_plain()`。同时补齐了本项目自己那条规矩（"凡是 sc 没写的文本都要过 `_plain()`"）在 `cmd_add` 错误路径和 `sc sub` 五处 URL 插值上的遗漏。

### 变更

- `sc sub add` / `rm` 在**动手之前**读 settings.json。这**不是**安全修复，我一开始判重了：`generate_config()` 本身就会拒绝不可用的 settings.json，而 `_apply_subscription()` 捕获后会还原 nodes.json，所以旧顺序不会留下孤儿节点。它买到的只是——一条注定要拒绝的命令，不必先取回一次 URL、再写两次 nodes.json 才给出同一个答案。注释与测试都已改成陈述这一点：测试断言的是**没有发出任何请求**，而不是文件没变（后者两种顺序都成立，所以它抓不住任何东西，事实上也没抓住）。

### 已知、未修，以及为什么

- **订阅长期取回失败只在 journal 和单元状态里看得见**，`sc doctor` 没有订阅一节。加一行的话，由于没有记录"上次成功时间"，它能说的只有"N 个订阅、M 个节点"——那正是 `sc sub show` 已经说的，一个不带判定的重复。要做就得先记时间戳，那是另一件事。
- **urllib 会跟随 http → ftp 的重定向**：`_fetch_text()` 只检查了原始 URL 的 scheme。不涉及本地文件读取，而 `sc` 本来就以 root 运行，所以这里没有可得的权限；记录在案。

### 测试

- `t_tag_minter`：铸造者的四条性质，并且**与读取者对拍**——它铸出来的，读取者必须接受。外加一条端到端断言：一个名字和地址都带转义序列的节点，`sc ls` 的输出里不含任何 `\x1b`。
- 变异测试 6 个，5 个当场报红；第 6 个逃逸让我发现上面那处**判重**——测试没抓住，是因为那里本来就没有可抓的东西。

## [0.5.0] - 2026-08-22

订阅落地，README 路线图上最后一个未打勾的框合上了。没有破坏性变更。

### 新增

- **`sc sub [show|add <url>|rm <url>|update]`：订阅，以及它名下的节点。**

  **模型只有一条：节点记着自己来自哪个订阅**（`source` 字段）。下面所有行为都是它的推论，而不是各自实现的规则：刷新是**按来源整组替换**，所以机场下线的节点会消失——合并语义永远表达不了这件事；手工 `sc add` 的节点不带 source，于是订阅操作根本看不见它；取回失败的订阅保留它上次给的节点，**网络故障不可能清空 `nodes.json`**；名字撞车由 `_unique_tag()` 处理，与 `sc add` 同一个铸造者；新列表**先校验再保留**，拒绝就一字节不差还原——`sc add` / `sc import` 已有的契约，这里第三次复用而不是第三次实现。

  `settings.json` 在 `add` / `rm` 里都是**最后**才写的：配置被拒时两份文件都原样不动，因此不存在「两个文档的回滚要写对」这个问题。

  sing-box 拒绝带未知字段的 outbound（在 1.13.18 上实测：`outbounds[0].source: json: unknown field "source"`），所以来源虽然写在节点上，**被过滤的是发出去的文档**——过滤只发生在唯一把节点放进文档的地方（`_emitted_node()`）。

  订阅格式两种都读：纯文本，以及整份 base64（多数机场提供的形式）。空行与 `#` 注释跳过；无法识别的行**计数**而不是失败——四十个节点里有一个协议不支持，不该让你损失另外三十九个。URL 只接受 http / https：`sc` 以 root 运行，`file://` 不会带来任何提权（调用者本来就有 root），但这个功能也不需要它。

- **「自动」没有新增任何单元。** `Type=oneshot` 的 `ExecStart` 可以有多条并按顺序执行，所以订阅刷新搭在规则集**已有的**定时器上：零新单元、零新定时器、零安装脚本改动，而且只有一个频率要配（`sc update-interval` 同时管两件事——这是更好的 UX，不只是更少的代码）。

  `-` 前缀加在**第一条**上，这是这里唯一真正的排序决定：systemd 会在第一条失败的 `ExecStart` 处停下，而 `sc update-rules` 因为一个镜像不通就会非 0 退出——不加 `-` 的话，一个长期不通的镜像会悄无声息地让订阅永远不再刷新。规则集的失败仍然看得见（journal、`sc doctor` 的规则集一节），而单元自身的状态留给订阅刷新，因为那件事没有别的地方会报。OpenRC 的 periodic 脚本是同一个形状（`|| true` 加在第一条上）。

### 移除

- **`_b64dec()` 里一行没有作用的代码。** 本次开发中途我给它加了 `+/` → `-_` 的转换，理由是「一种字母表的解码器会直接拒绝另一种」——**这句话是假的**：`urlsafe_b64decode` 只把 `-_` 映射过去，标准字母表的 `+/` 原样通过，本来就两种都收。一行代码加一段论证，什么都没买到，已撤回；事实留在注释里，并由测试守着（把解码器换成严格的标准字母表解码器会报红）。

### 变更

- `OVERRIDE_MAX_BYTES` 更名为 `DOC_MAX_BYTES`：一个概念（sc 从外部读入的最大文档），两个消费者（override.json 与订阅正文），而不是两个同值常量。

### 测试

- 新增 `t_subscription`：全程一个服务器一个 URL（`bodies` 每次请求都重读，所以「机场改了内容」是一次字典赋值，而不是换一个端口——换端口就等于换了被测的那个身份）。断言的都是无人值守的周任务可能搞砸的事：手工节点被删、网络故障清空列表、每次刷新累积一份副本、`source` 泄进文档、拒绝时 `settings.json` 已经写了、无变化却重启。以及 `t_scheduled_run_covers_both_jobs` 直接对着出厂单元断言两条 `ExecStart` 及 `-` 的位置。

- 变异测试 10 个：9 个报红。逃掉的那个正是上面撤回的空操作——**变异测试发现的不是测试的漏洞，而是代码里一行不该存在的东西**。

## [0.4.0] - 2026-08-22

次版本号：**有破坏性变更**。`sc ipv6` 被删除（见下方「升级须知」），`sc doctor` 从九节变为八节。项目仍在 `0.x`，按约定计入次版本号。

### 移除

- **IPv6 支持整体删除，AAAA 一律在本地返回空结果。** 不是把默认值从 `auto` 改成 `off` —— 那样要继续养着整套检测设施去伺候一个不支持的配置：`/proc/net/if_inet6` 的读取与解析、三值设置、决策函数、`sc ipv6` 命令、`sc doctor` 的一整节、十条译文，外加一个只在那里用到的路径常量。删掉之后，替代它们的是**一条规则**：`dns.rules` 开头一条 `predefined` / `NOERROR`，query_type `[28, 64, 65]`，无条件发出。净减 277 行。

  **直说后果**：只能通过 AAAA 解析到地址的节点在这个构建上不可达，`sc` 里没有任何东西能解除它。确实需要的机器只能在 `override.json` 里 `$replace` 掉 `dns.rules`，或者换一个工具。两份 README 的 `override.json` 一节把这条与另外两条硬编码默认值（拒绝 UDP 443、国内优先 DNS）并列写明。

  **升级须知**：`sc ipv6` 这条命令没有了，敲它会得到 argparse 的「invalid choice」。旧机器 `settings.json` 里遗留的 `"ipv6"` 键不需要清理 —— 没有任何东西再读它，就像 `sc sysproxy` 时代留下的 `"mode"` 键一样被忽略。**如果你此前用 `sc ipv6 on` 明确开启过 IPv6，升级后它不再生效**：下一次 `sc reload` 生成的文档会抑制 AAAA。

### 新增

- **`sc ping [名称|序号]`：实时测量节点延迟。** `sc ls` 的延迟列是历史值 —— 自动选择组探测时留下的记录，所以没被探测过的节点是 `-`。`sc ping` 让正在运行的 sing-box 通过 Clash API 现测一次，而且用的是 `PROBE_URL`：探测端点从此只有一处定义，自动选择组发进文档的和这条命令测的是同一个 URL，两者不可能对「可达」给出不同判断。API 会记录结果，所以 `sc ls` 随后显示的就是这次的数字。

  顺序执行、逐行流式输出（与 `sc update-rules` 同一种风格）：慢节点在**等待它的时候**就看得见，而不是等全部测完才出现。序号列就是 `sc use` 接受的号码，单节点形式也保留该节点的真实序号。仅当没有任何节点响应时才非 0 退出。为它 `clash_api()` 多了一个 `timeout` 参数，且只有这一个调用者：API 被要求为一个节点花掉最多 `PING_TIMEOUT_MS`，那么 socket 就必须给得更宽，否则回答这个问题的会是传输层而不是被测的节点。

- **`sc export <路径>` / `sc import <路径>`：节点备份与迁移。** `export` 通过唯一的状态写入者落盘，因此备份是**原子写入、权限 600、root 所有** —— 手工 `cp nodes.json ~/backup.json` 经常留下的正是一份 0644 的凭据文件。它**绝不写到标准输出**，也没有开关能改：`/etc/sudoers.d/sc` 让安装用户免密运行 `sc`，把凭据打印到那个用户控制的流里，等于凿穿 `sc config` 无条件打码所堵的那个洞。

  `import` 是**合并**：本机已有的一个都不删；除名字外完全相同的节点会被跳过（所以同一份备份导入两次是空操作，而不是把每个节点再复制一份）；名字与另一个节点冲突时由 `_unique_tag()` 另起一个，与 `sc add` 一致。然后**先校验再保留**：生成不出可用配置就把 `nodes.json` 一字节不差地还原并非 0 退出 —— 与 `sc add` 同一条契约。

### 修复

- **一个既存的隐患：手改坏的 `nodes.json` 会抛 traceback。** `generate_config()` 里的 `[n["tag"] for n in nodes]` 在受保护区**之外**，所以数组里放一个标量、或一个没有 `tag` 的对象，会以 traceback 结束运行 —— 这个文件其他每一种坏法都早有一句话。修法不是在那里加一层 try：`load_nodes()` 是节点文档的唯一读取者，把「每个元素必须是带非空 `tag` 的对象」这条要求写在读取者身上，手改和导入就同时有了一句话。这也是 `sc import` 不需要任何导入专用校验的原因 —— 它读的是同一个读取者，只是换了个路径参数，报错自动指名那个文件。

### 测试

- 新增 `t_ping`（对着 loopback 上的 Clash API 替身，断言的是**发出的请求**——带空格的 tag 必须百分号编码，否则那个节点会被静默判死——以及 socket 预算必须大于探测预算）、`t_export_import`（备份权限、重复导入是空操作、同名不同节点改名、校验不过则还原、坏文档指名自己）、以及节点文档元素契约的四种坏形状。`t_doctor_smoke` 钉住的段落清单从九节改为八节 —— 这一行正是让 IPv6 的移除**必须显式声明**而不是悄悄通过的东西。

- 又跑了一轮变异测试：9 个针对新代码的缺陷全部报红（不编码 tag、无响应却退出 0、忽略探测预算、export 写到别处、import 变成替换、import 不还原、import 丢掉去重、`load_nodes` 不再检查元素、AAAA 规则从文档里掉出去）。

## [0.3.0] - 2026-08-22

次版本号而非补丁号，按 `0.2.0` 立下的同一条规矩——**行为变更，脚本看得见**：`sc rm` 在配置生成失败时改为**非 0 退出**（此前打印「已删除」并以 0 退出）；`sc update-interval` 对 systemd 不接受的表达式改为**在写入之前**拒绝并非 0 退出；`/etc/sing-box/settings.json` 的权限从 0644 收紧为 0600（升级时由安装脚本收紧一次）。项目仍在 `0.x`，所以这些计入次版本号。

本次没有新的用户可见功能。六处改动都是**用一个更小的设计取消掉一类问题**，而不是在原地打补丁——其中三处的净效果是代码变少；另外新增了一个单文件自检套件，以及它所需要的那一个常量，让上面这些性质从此有东西守着。

### 新增

- **`selftest.py`：一个文件的自检套件，普通用户即可运行。** 项目此前没有任何测试（随 harness-kit 一起撤掉了），能验证的只有 `py_compile` 与 `bash -n`。这一份刻意不是一个 `test/` 目录：没有框架、没有 fixture 目录、没有配置、没有插件——十几个 `t_*` 函数，加二十行 runner（按名字收集、各给一个临时目录、捕获输出、只在失败时打印）。它把 `bin/sc` 当**模块**加载，把每个路径常量重指到临时目录，所以既不需要 root，也碰不到 `/etc`、`/var` 或任何服务；`t_import_is_inert` 直接对模块体的 AST 断言「导入这个文件不做任何事」，这条性质一旦回退，套件本身就会先被 sudo 掉，所以它必须由结构而不是由运行结果来保证。

  覆盖的是有契约的地方：分享链接六种协议（含 userinfo 的三条规则，其中「以最后一个 `@` 为界」用一个密码里带**未编码** `@` 的链接来分辨）、状态文件的单一写入者与拒绝语义、完整合成链路、`sing-box check` 的判决具有约束力（被拒的文档一个字节都到不了 `config.json`，漂移记录也不动）、规则集降级后无悬空引用、override 的指令与六种拒绝形状、选择判定的全域性、脱敏的 fail-closed、`_apply_or_exit()` 的非零退出、`sc add` 校验不过时把 `nodes.json` 原样放回、i18n 平价（160 个 `t()` 字面量键必须都有 zh 译文）、`sc doctor` 的九节及其因果顺序、`sc config` 的「文档进 stdout、说明进 stderr」、`update-interval` 对非法表达式的「写之前就拒绝」，以及从 `install.sh` 里抽出来实跑的凭据权限清扫——`CRED_FILES` / `CRED_MODE` 是从 `install.sh` 里读出来的，不是测试自己声明的，否则测的就是测试自己的清单。

  规则集下载器（~90 行，此前零覆盖）由一个临时的 loopback HTTP 源驱动：镜像回退、死源在同一次运行里不再重试、坏 magic / 文件过小 / 与 Content-Length 不符三种坏响应各自被拒、全部源失效时的非零退出，以及**那条 apply 判定**——字节没变就不碰服务（无条件重启正是当年掐断远程管理员 SSH 的那个回归），字节变了只重启、只有规则集从不可用变回可用时才重新生成文档。这里用合成字节是成立的：下载器的校验器是 `srs_reject_reason`，不是 sing-box，全程没有任何一份伪造的 `.srs` 被交给 sing-box 加载。

  主机上有 `sing-box` 和真实 `.srs` 时会用上（用来断言生成的文档确实被 `sing-box check` 接受，本轮是 1.13.18），没有时逐条报告跳过了什么。**不伪造规则集**：合成出来的 `.srs` 不是 sing-box 能加载的东西，拿它断言等于在断言伪造物。

  **套件本身经过变异测试**，而且这是它成型的方式：先后向副本注射了二十多个缺陷，每一个逃逸要么变成了一个新测试（`update-interval` 当时完全没覆盖；清扫测试原本自己声明了文件清单；`_userinfo` 的用例把 `@` 编码了因而分不出两种实现；apply 判定因为测试里没有 `config.json` 而是死代码；doctor 的段落断言原本是照着被测的那张表迭代的，删一段就少断言一段），要么经核对是等价变异——最后只剩两个：`os.fchmod` 与 `mkstemp` 的 0600 重复，以及一处 `_record_generated()` 摘要的是同一份旧文件。

- **`TIMER_DROPIN_DIR`**：`sc update-interval` 的 systemd drop-in 目录从函数体里的字面量提为常量，加入本文件既有的可重指路径常量集合（「只在函数体内引用」正是为此）。这让本轮最新的那条代码路径可以在 fixture 里验证，且验证的是**拒绝**路径——它在第一个子进程之前就返回，所以测试永远不会碰到宿主机的 systemd。

### 修复

- **`sc rm` 不再在配置生成失败时报告成功。** 它此前丢掉了 `reload_or_restart()` 的返回值：节点已经从 `nodes.json` 消失，而 `config.json` 仍然带着它、服务仍然按老配置走它，命令却打印「已删除」并以 0 退出。修法不是给 `rm` 补上那两行判断——那两行已经在 `use` / `ipv6` / `telemetry` / `reload` 里各抄了一遍，`rm` 正是第五处忘了抄的地方。现在这个判断只有一个家 `_apply_or_exit()`，五条命令各调用一次；「一条会动服务的命令不可能悄悄地成功」从此是这个函数的性质，而不是每条新命令都要记住的规矩。`sc add` 刻意不是它的调用者：它必须先把 `nodes.json` 放回去才能开口，所以自己读那个布尔值。

- **`sc update-interval` 先验证，再写入。** 它此前是本文件里唯一一处「先安装、后验证」：把用户给的 OnCalendar 原样写进 systemd drop-in、`daemon-reload`、然后才让 `restart` 去否决它——失败时那份坏的 drop-in 留在 `/etc`，而原来的频率已经没了，自动更新从此静默停摆。而且 `val` 只去掉首尾空白，一个带换行的值可以往 unit 文件里追加它自己的指令，偏偏 `/etc/sudoers.d/sc` 让安装用户可以免密通过 `sc` 写这个文件。现在判决由 systemd 自己的解析器给出（`systemd-analyze calendar`），并且**在写任何东西之前**——这与 `config.json` 的「候选文档 → check → 落位」和每一次规则集下载的顺序一致。坏值到不了 `/etc`，所以没有半应用状态要回滚，也没有残留要清理；两个问题由同一个更早的判决同时消失，没有新增任何一条译文。验证器**跑不起来**时是拒绝而不是警告（与 `generate_config()` 的「无法检查不等于无效」相反，理由也正是那里的理由）：一次没生效的频率只损失这一条命令，一个坏掉的 timer 损失的是此后每一次无人值守的规则集更新。

- **`settings.json` 与 `nodes.json` 从此是同一个写入者。** `save_nodes()` 走原子的 `_write_private()`，`save_settings()` 却是裸的 `write_text()`——本项目唯一一个会截断的写入者。磁盘写满或断电写到一半，这份文档就解析不出任何东西，`_settings_or_empty()` 随即把整个工具降级到默认值：语言，以及**正在运行的 sing-box 实际监听的那个 Clash API 端口**——后者会让 `sc mode` 直接失败、让 `sc use` 退化成重启整个服务。两个函数现在都调用 `_write_state()`，一种字节形状、一个权限、一句失败话。

### 变更

- **auto-elevate 从模块顶层移进了 `main()`。** 这一行 `os.execvp("sudo", ...)` 此前在 `import` 时执行，于是任何以普通用户身份 import 这个文件的进程都会把**自己**重新 exec 成 `sudo`。这是整份文件唯一的 import 副作用，而文件其余部分恰恰是按可测试性写的——八个路径常量刻意只在函数体内引用，就是为了让 fixture 能在没有 root 的情况下重指它们。现在 `import` 什么也不做，`bin/sc` 可以作为模块被加载：配置合成、降级、override 指令、脱敏、漂移判定都能在一个临时目录里以普通用户身份验证。行为完全没变：非 root 运行 `sc` 仍然在第一条语句就 exec 成 `sudo`。

- **install.sh 不再自带第二个 `settings.json` 写入者。** 第 3 步那段内联 Python（17 行）唯一的用途是在首次 `sc reload` 之前把选定的语言写进去——而 `sc lang` 就是这个操作，`/usr/local/bin/sc` 就装在它上面一行。那段代码在每一个要紧的点上都与 `bin/sc` 的写入者不一致：它只捕获 `JSONDecodeError`（于是一份读不出来或顶层不是对象的文档会让安装在第 3 步以 traceback 结束）、它按 locale 编码读写、它把文件留在 0644，还得复述一遍 `_init_files()` 的种子才不至于把已有的键抹掉。现在是一行 `sc lang "$LANG_CHOICE"`。

- **`sc doctor` 的权限检查不再豁免 `settings.json`，install.sh 的收紧清单则把它加了进来。** 那条豁免存在的唯一理由是它自己的写入者把它留在 0644；随着第二个写入者消失，豁免也跟着消失，而不是作为一个长期的空洞留在检查里。检查现在覆盖目录下每一个普通文件；从旧版本升级上来的 0644 文档由安装脚本收紧一次。

### 移除

- **`ruleset_status()`**：零调用者。单一可用性判断由 `srs_reject_reason()` 与 `ruleset_state()` 承担，两者都有真实调用者；一个「保留着以备有人只想问一个文件的状态」的适配器，是把「以后可能有人要」写成了今天的代码。

### 文档

- **`docs/faq.md` 不再把用户引向 `config.json` 的漂移警告。** 三条回答教的是「改 `bin/sc` 里的 `generate_config()` 然后 `sc reload`」——`override.json` 整套机制在 `docs/` 里出现过 0 次，而 README 有完整一节。现在这三条改成指向 `override.json`：换 DNS / 路由模板、注册自己的 `.srs` tag（`route.rule_set` 与 `route.rules` 各 `$append` 一次）、给 Clash API 挂 Web Dashboard（`experimental.clash_api.external_ui` 是一次深度合并，只加这一个键）。合并语义仍然只在 README 里讲一遍，FAQ 不复述第二份。「我想贡献代码」那条把指向说细了（清单在 README 的「贡献」一节），并要求附上 `sc doctor` 的输出；出口 IP 那条改为先跑 `sc doctor`。
- **`docs/architecture.md`**：数据流图里补上 `override.json` 这一层输入；规则集一节改为镜像顺序 + 三重校验 + 原子替换的实际行为（此前写的是「从 GitHub raw 下载」）；安全考量表加上 `settings.json` 一行。
- **两份 README**：系统要求里写明 **sing-box 1.12+**，并说清 `install.sh` 第 2 步在 `PATH` 上已有 `sing-box` 时会跳过下载——旧内核会在安装最后一步的配置校验处失败，而此前没有任何一处文档说过这件事。`override.json` 一节前面加了两条默认值的说明：生成的文档**对所有目标拒绝 UDP 443**（把 QUIC 逼回 TCP，对直连的国内流量同样生效），以及 DNS 是**国内优先**的——两条都没在任何地方写过。文件位置表里 `settings.json` 标上 mode 600。

## [0.2.0] - 2026-08-17

次版本号而非补丁号：本次有**破坏性变更**。`sc sysproxy` 被删除（见下方「升级须知」），`sc mode` 在服务未运行时改为拒绝执行并非 0 退出，`sc add` 在配置校验不通过时改为非 0 退出且不再保留节点。项目仍在 `0.x`，所以按约定这些变更计入次版本号。

### 新增

- **`sc version`，以及 `sc doctor` 报告开头的 `sc 版本` 一行**：此前整个仓库**没有任何地方写着版本号** —— `bin/sc` 里没有，也从没打过 git tag（`0.1.0` 同样没有）。对一个靠 `curl | bash` 分发、并且用 `sc doctor` 的输出来收 bug 报告的工具来说，这意味着每一份报告都说不清是哪个构建产生的，用户也无法确认一次升级到底生效了没有。现在版本号只写在 `SC_VERSION` 一处常量里，三个读者（`sc version`、`sc doctor`、`sc help` 的标题）都不重复拼字面量；由于这个文件是**整份拷贝**安装的，常量必然随代码一起走，不可能与代码不一致 —— 需要与这一行保持同步的是发布 tag，而不是反过来。`sc version` 的输出是不带标签、不翻译的一行 `sc <版本>`，因为它是要被粘进 bug 报告、被脚本读取的。`sc doctor` 里它是**第一行**，而且是全报告唯一一个永远不会是「未知」的事实：它是编译进正在执行的这个文件里的常量，不需要任何查询，也没有失败模式 —— 一份不说明是哪个构建产出的报告，是一份关于未知程序的报告。它加入的是既有的第 1 节，所以**九项的节数没有变化**。`sc version` 与 `sc doctor` / `sc config` 一样被列入 `main()` 的只读豁免名单：它打印的是一个编译进文件的常量，不存在需要读取的主机状态，为它创建 `/etc/sing-box` 并探测端口纯属副作用 —— 何况它正是用户在决定要不要安装或升级**之前**会跑的那条命令。

### 移除

- **`sc sysproxy` 已删除**：这条命令把 GNOME 的 HTTP / HTTPS / SOCKS 代理全部指向 `127.0.0.1:7890` —— 而这个项目从来没有在这个端口上监听过任何东西：生成的文档里唯一的入站就是 TUN 设备。也就是说，开启它的唯一效果是让所有走系统代理的桌面应用立刻断网，直到用户自己想起来执行 `sc sysproxy off`；两份 README 里也从没写过它。要让它成立，就得在每台机器上都额外发出一个 `mixed` 入站，即在一台「无头 + TUN 已经承载全部流量」的机器上再开一个本地代理端口 —— 这与项目的设计前提相反。因此是删除而不是修复，没有任何东西接替它。同时删掉的还有它专用的两条译文（`GNOME system proxy → {val}`、`Error: cannot determine target user`）。

  **升级须知**：如果你在旧版本上执行过 `sc sysproxy on`，那份 GNOME 设置**留在你的桌面会话里**，而升级后已经没有 `sc sysproxy off` 可以撤销它了 —— 症状正是「浏览器等应用连不上，但 `curl` 正常」。以你自己的普通用户身份（不要用 root）跑一次即可复原：

  ```bash
  gsettings set org.gnome.system.proxy mode none
  ```

  从没用过这条命令的机器不受影响：它只有被显式开启过才会写入任何东西。

### 新增

- **sing-box 二进制在安装前核对 sha256，且摘要与二进制必定来自不同来源**：此前 `install.sh` 从 GitHub 下载一个 tar.gz、解开、`install -m 755` 到 `/usr/local/bin/sing-box`，**全程不校验任何哈希** —— 这个二进制之后是以 root 身份、持有 TUN 和全量流量运行的。只有官方源时风险还算有限，但既然新增了两个第三方反代作为回退，供应链面就变宽了，这项检查必须补上。SagerNet **不发布** checksums.txt、签名或 attestation 资产（实测当前 release 共 154 个资产，没有一个是摘要），所以唯一可比对的公布值是 GitHub 自己在 release 资产上给出的 `digest` 字段。它的取法是这项检查成立的关键：**摘要只从 `api.github.com` 取，绝不经过 GH_PREFIXES**。`api.github.com` 恰好是 GitHub 反代唯一不承载的主机（这也正是 `gh_latest_tag()` 存在的原因），所以二进制可以来自镜像、摘要永远不来自镜像，没有任何一方能同时提供两者 —— 把摘要也走同一份前缀列表，就会把真实性校验降级成自洽性校验。请求的是 `releases/tags/v<版本>` 而非 `releases/latest`，所以无论版本号来自 `SB_VERSION`、JSON API 还是重定向，都由同一次请求覆盖；只接受 `sha256:` + 64 位十六进制这一种形状，错误页和未来的其他摘要算法都满足不了它。**不一致就删文件并终止**，不解压、不安装任何东西 —— 与 `config.json` 早已贯彻的「先验证、再落盘」同一条顺序。**取不到摘要则是另一种结果：大声说明，然后继续** —— 与 `bin/sc` 在「`sing-box check` 跑不起来」时画的是同一条界线（无法验证不等于验证不通过）。在这里拒绝执行，会把「能连上镜像、连不上 `api.github.com`」的机器 —— 也就是镜像列表本来要救的那批 —— 为了一项此前任何版本都没做过的检查全部挡在门外。这种机器可以用 `SB_SHA256=<摘要>` 自行传入，该值优先于 API。哈希用 `python3` 计算（安装脚本第 1 步刚装过、第 3 步已经在用的硬依赖），因此不需要探测 `sha256sum` 是否存在，也就没有第二条代码路径。

- **安装路径不再只有 GitHub 一条路，受限网络下可以装得上**：装好之后的一切本来就不需要外网 —— `sc add` 解析链接、生成配置、跑 `sing-box check`、重启服务全在本机完成，不发一个请求 —— 但**引导阶段**过不去：安装脚本自己的五个文件走 `raw.githubusercontent.com`，sing-box 的版本号走 `api.github.com`，二进制走 `github.com/releases/download`，**三处一个镜像回退都没有，也没有任何环境变量能覆盖**（对比规则集那边早就有 `--mirror` 和 `SB_RULES_BASE`）。现在这三处走同一套机制：一份**前缀列表**依次套在每个规范 URL 前面，第一个取到文件的就算数。**官方源排在第一位并且保持第一位** —— 这里没有任何环节校验安装物的哈希，所以能连上 GitHub 的机器一次都不会碰第三方反代；连不上的机器靠新加的 `--connect-timeout 10` 把每个端点的等待从内核默认的约 130 秒压到 10 秒，然后继续。两个公共反代（`ghfast.top`、`gh-proxy.com`）都实测过能对 raw 文件和 release 资源返回逐字节正确的内容。`SB_GH_MIRROR` 可以整份替换这个列表（空格分隔），供有内网镜像的机器使用。**版本号从单点故障变成三条独立途径**：`SB_VERSION` 环境变量 → `api.github.com` 的 JSON → `releases/latest` 的重定向（终点 URL 里就带着 tag，而这个地址每个反代都承载）—— 最后这条的推进条件是**解析成功**而不是请求成功，因为实测其中一个反代会以 200 应答该地址却**不跟随重定向**，只看请求状态会把它误判成可用源。另外，回退过程中被下一个前缀救回来的那次失败**不再打印 curl 的报错**：先报一句「无法解析主机」然后又装成功了，会让一次正常安装读起来像坏掉了 —— 而这恰恰是每一台连不上官方源的机器都会看到的画面；最后一个前缀仍然保留 stderr，所以真正失败的运行照样能看到原因（DNS、拒绝、超时、404）。规则集镜像列表也按同样的理由**按可达性重排**（`testingcf.jsdelivr.net` → `ghfast.top` → `cdn.jsdelivr.net` → `raw.githubusercontent.com`）—— 之所以可以按可达性而不是按可信度排序，是因为每一个响应体都要过同一道校验（SRS 魔数、字节下限、与 Content-Length 相等）才会被安装，源的先后只决定等多久，决定不了装进去什么。两份 README 新增「受限网络下的安装」一节，写明镜像一行命令、`SB_GH_MIRROR`、`SB_VERSION`，以及**完全离线**的路径：`PATH` 上已有 sing-box 时安装脚本会跳过它唯一的大文件下载，所以自己先放一个二进制进去，对 GitHub 的依赖就只剩五个小文件。同时写明规则集下载失败并不阻断安装 —— 降级为「全部走代理」，加完节点确认能上网后再 `sc update-rules`，这一次是经由你自己的代理下载的，分流随下次重新生成恢复。

- **`sc doctor` 补齐到九项：规则集年龄、配置漂移、AAAA 一致性、节点延迟、DNS 解析、文件权限**：此前的七项体检停在了「它自己落地之后才做的那些功能」的边界上 —— 规则集躺了几个月、`config.json` 被手改过、AAAA 决策和磁盘上的文档已经对不上、所有节点从没被探测过、域名根本解析不出来、凭据文档对同组/其他用户开着，这些机器每一行都读作 `[正常]`。现在报告新增 **5 行**（健康的机器上一项一行、不会更多）：**规则集**那几行在状态和字节数旁边多了「多久以前写入的」，可用但**超过 60 天**的会被报为异常并给出 `sc update-rules`（60 天严格大于 `sc update-interval` 最长的预设周期，所以自动更新正常的机器永远不会被误报）；**配置改动**一行说明磁盘上的 `config.json` 还是不是 `sc` 最近一次生成的那份，被改过时告诉你把改动写进 `override.json` 再 `sc reload`，没有记录则是「未知」而不是「已改动」；**IPv6（AAAA）** 一行说明本机实际生效的 AAAA 决策，并检查磁盘上的文档是否与之一致；**节点延迟**一行说明有多少个节点带有**已记录的**延迟（历史值，不是实时测量）以及自动选择当前走哪个出站，一个都没有时给出 `sc ls`；**DNS 解析**一行让正在运行的 sing-box 真的解析一次域名，报告实测耗时 —— 这是整份报告里唯一能抓出「节点连得上、但什么都解析不出来」的一行；**文件权限**一行点名 `/etc/sing-box` 下任何对同组或其他用户开放的凭据文件（包括手工留下的 `config.json.bak-*` 这种任何枚举式清理都看不见的备份；`settings.json` 除外 —— 它不含凭据，安装脚本也从不改动它的权限）以及目录本身是否可被同组/其他用户写入，每条都给出具体权限位和收紧它的命令。报告的顺序仍然是**因果顺序**（改动排在配置检查之前、AAAA 排在 DNS 之前、DNS 与节点延迟排在出口 IP 之前、权限永远最后），仍然由同一张顺序表决定。每一项新事实都直接建立在它的功能主人已经提供的那次调用之上，不产生第二种说法。**依然全程只读**：不写、不建、不删任何路径，也不碰服务 —— 唯一向外界发起的动作是那次域名解析，而它是由正在运行的 sing-box 完成的。副作用是**退出码**：以前读作 `0` 的过期 / 漂移 / AAAA 不一致 / 权限过宽的机器，现在会按原有映射退出 `1`。
- **新增 `sc config`：打印磁盘上的那份配置，节点凭据一律隐去**：以前想看 `sc` 到底生成了什么配置只有一条路 —— 手动去读 `/etc/sing-box/config.json`，而那个文件是 0600、只有 root 读得到，读出来又是每个节点的密码 / UUID / Reality 公钥全部明文摊在终端里。现在 `sc config` 把这份文档原样打印出来，同时把凭据换成固定的 `******`：`outbounds` 之内的任意深度上，凡是不在**可见键集合**里的键（可见的是 `type` / `tag` / `server` / `server_port` / `detour`，以及传输层、TLS、Reality、obfs 和各协议调优、自动选择组的设置），整个值都会被替换 —— 因此 `uuid`、`password`、`public_key`、`short_id` 会被隐去，你通过 `override.json` 添加的出站里那些 `sc` 从来不会写出的键（也包括将来 sing-box 新增的字段）同样会被隐去而不是被打印出来；此外整份文档的任意位置上，`password`、`uuid`、`secret`、`token`、`private_key`、`pre_shared_key` 也一律隐去。**隐去没有开关**：没有任何参数、设置或环境变量能打印出未隐去的值 —— `sc` 跑在一条只对它自己生效的免密 sudo 规则之下，提供这样一个开关等于把「只有 root 读得到的凭据文件」变成「任何人免密就能读」。被替换的只有值、键名照常保留，所以「配了哪些字段」仍然一眼可见；输出仍是一份合法 JSON，除被隐去的位置外与磁盘上的文件处处一致。配置文档写到标准输出、`sc` 自己的说明写到标准错误，因此 `sc config > current.json` 直接就是一份能被解析的 JSON，`sc config | grep ...` 也照常可用；标准错误上会写明该文件的绝对路径、凭据已隐去，以及（在存在漂移记录时）磁盘上这份文档是不是 `sc` 最近一次生成的内容 —— 没有记录就什么都不说（「没有记录」意味着未知，不是漂移），文档发生漂移本身也不算错误、退出码仍然是 0。**全程只读**：不创建、不修改、不删除任何文件（机器上没有 `/etc/sing-box` 时也不会去建它，这一点与 `sc doctor` 一致），不下载、不联网、不起子进程、不碰服务，也不判断配置是否有效 —— 那仍然是 `sc doctor` 的结论。**它的边界**：隐去覆盖的是 `sc` 自己写进去的凭据；写在你自己的 `override.json` 里、位置在 `outbounds` 之外、键名又不属于上面那六个的密钥（例如某个入站用户的 `auth_token`），仍会被原样打印 —— 两份 README 的对应小节里写明了这一点。

- **`sc status` 会说出每个规则集有多久没更新了，`sc update-rules` 的退出码不再与它实际干成的事情不符**：以前 `sc` 里没有任何一处报告 `.srs` 文件放了多久 —— 一台规则集半年没更新的机器，从 `sc status` 上看和刚装好的机器一模一样。现在 `sc status` 多出「=== 规则集 ===」一节：四个规则集各一行，写明状态和最后一次写入距今多久（天 / 小时 / 分钟 / 秒，只显示最大的那个单位）。这个时间戳与该行的状态出自**同一次读取**（在读完内容的那个文件句柄上 `fstat`，不是对路径再 stat 一次），所以不会出现「缺失」却带着一个时间的行，也不会出现两处对同一个文件的年龄各说各话；文件读不到时（缺失、坏掉的符号链接、目录、没有权限）这一格写「更新时间未知」，**绝不会**显示 0、也不会显示 1970 年的日期；文件时间比当前时钟还新时（时钟回拨、从备份恢复）显示「0 秒前」，不会出现负数、不告警、也不改变退出码。这一节排在 TUN 接口之后、服务状态判断之前，所以**服务停着、Clash API 不通时照样完整打印** —— 那恰恰是最需要看这份事实的时候；输出被重定向到文件时也是每个规则集一行、不含 `\r`。生成的 `config.json` 完全不受影响：规则集的时间不进入配置，也没有任何规则集会因为「旧」被丢弃、降级或重新下载 —— 这里只报告事实，不引入「多久算过期」的判断。**另一半是 `sc update-rules` 不再谎报成功**：以前只有「某个规则集在所有镜像上都下载失败」才会以非 0 退出，另外两种失败一律退出 0，被 systemd 记成一次成功的运行 —— 一是新规则集补齐后重新生成配置或 `sing-box check` 没通过（这时它还照样打印一行「配置已重新生成」），二是重启命令本身返回非 0（这时它照样打印「已重启 sing-box 以加载新数据」）。现在这两种情况都退出 1，而且输出只说真正发生过的事：重新生成没成功就不打印「配置已重新生成」，重启没成功时最后一行是「规则集已更新：{名称} —— sing-box 服务重启未成功」。每次运行仍然只有一行运行级结论，措辞集合只多出这一条；「完成」只在这次运行确实成功时打印。**没有改动的部分**：下载全部失败时的行为（非 0 退出、逐文件原因在 stdout、汇总一行在 stderr）、「内容一个字节都没变就退出 0、完全不碰服务」、以及每次运行最多重启一次的判断，全部与过去一致；systemd 单元和 OpenRC 定时脚本都不需要任何改动（单元是 `Type=oneshot`，非 0 退出即被记为失败单元），`sc reload` / `use` / `add` / `rm` / `ipv6` / `telemetry` 的行为也都没有变化。

- **默认拦截一份固定的遥测域名清单，新增 `sc telemetry block|allow|show`**：生成的配置里多出一条 DNS 规则，它携带 17 个域名 —— 微软 / Canonical 的桌面系统诊断端点、Firefox 遥测上报、Google Analytics / Firebase / Crashlytics / Adobe / Comscore 等全球分析与崩溃上报 SDK，以及百度统计 / 友盟 / CNZZ / 阿里 mmstat / 小米 MIUI 与 MiStat 等国内分析 SDK。查询名单内的域名或它的任意子域名时，由 sing-box 自己在本地返回 `NXDOMAIN`、零条记录（实测约 4 毫秒），**不向任何 DNS 服务器发出查询**；每一条的厂商、承载内容与类别都写在两份 README 的表格里，`sc telemetry show` 也会在机器上原样打印这份名单。收录标准是两条必须同时成立：该域名的唯一用途是把使用情况、诊断、崩溃或广告标识数据送给厂商，**并且**拦截它不会让所属产品的任何用户可见功能失效 —— 因此更新、激活、授权、认证、推送下发、CDN 内容、验证码和安全特性端点一律不收录（`analytics.google.com`、`omtrdc.net`、`googletagmanager.com`、`settings-win.data.microsoft.com` 就是被这一条挡在外面的例子）。匹配以标签边界为准：`crashlytics.com` 覆盖它本身和任意层级的子域名，但不会误伤 `notcrashlytics.com`。**默认是 `block`**，而且没有这个键就等于 `block`，所以全新安装与升级上来的机器行为一致 —— 已有机器升级后 `sc reload` 一次即可，不需要手改 `/etc/sing-box` 下的任何文件，也不会多出「配置被外部修改」的告警。承载它的规则排在两条路由模式规则之前、内置 hosts 表规则之后，因此 `rule` / `global` / `direct` 三种模式下同样生效（**切路由模式并不能解除拦截**），它也不引用任何规则集、不依赖任何可用节点，规则集缺失或一个节点都没有的机器照样生效。**两条退路**，都不需要改 `bin/sc`，也都能在 `sc reload` 之后继续生效：`sc telemetry allow` 关掉整份名单；或者在 `/etc/sing-box/override.json` 里用 `$after` 锚定 `{"server": "hosts_dns"}` 插一条自己的规则，只放行其中某一个域名 —— 同一个锚点也用来添加你自己的域名，两份写法在两份 README 里都有可直接复制的完整 JSON。`sc telemetry <值>` **只有在实际生效的设置真的变化时**才重新生成配置并重启服务，否则只说明设置无变化并提示 `sc reload`；`sc telemetry show` 不改设置、不重新生成配置、也不碰服务。**需要注意**：它匹配的是域名，而且只匹配会走到这份配置 DNS 规则里的域名 —— 自带 DoH/DoT 解析器或直连硬编码 IP 的应用不受影响，这里不在 IP 层或路由层拦截任何东西；名单固定写在 `bin/sc` 里，不会自行更新。

- **AAAA 查询不再拖着一个坏节点一起卡住，新增 `sc ipv6 on|off|auto|show`**：用不了 IPv6 的机器上，凡是被这份配置送往代理侧解析器的域名（`rule` 模式下就是 `hosts` 表、五个国内域名后缀和 `geosite-cn` / `geosite-private` 之外的全部域名），它的 AAAA 查询也要走到那里；节点「TCP 能连上、之后一直不回应」时这次查询什么都拿不回来，实测卡满 sing-box 自己的每次查询 10.0 秒上限（这个上限是 sing-box 内置的，1.13.15 里没有任何配置键能改动它，到点后它直接丢弃这次查询，既不回答也不改问别人）。现在生成的配置会在**第一条 DNS 规则**上用空的 `NOERROR` 就地回答 AAAA（类型 28）以及 SVCB / HTTPS（类型 64、65），根本不发出这次查询。这条规则排在两条路由模式规则之前，所以 `rule` / `global` / `direct` 三种模式下同样生效 —— 以前承载 64、65 的那条规则排在它们之后，切到 `global` 或 `direct`（恰恰是出问题时人们会切过去的模式）就会失效；它也不引用任何规则集，`.srs` 缺失的降级机器照样有这个能力。是否抑制由新设置 `ipv6` 决定：`on` 从不抑制、`off` 始终抑制、`auto`（默认，也是没有这个键时的含义）仅在本机没有全局 IPv6 地址时抑制 —— 判断依据是读一次 `/proc/net/if_inet6`，只认既不在回环、也不在 `sb-tun` 上的 `2000::/3` 地址，链路本地和唯一本地地址一律不算（本项目的 TUN 设备上必然有一个链路本地地址，所以「有任意 IPv6 地址」这种判断在这里是错的）；读不到地址列表时按「有 IPv6」处理并在 stderr 说明，绝不会因为探测失败反而让 IPv6-only 的目标变得不可达。`sc ipv6 <值>` **只有在实际结论真的变化时**才重新生成配置并重启服务，否则只打印一行「设置无变化」，不碰服务；`sc ipv6 show` 只是报告：它不会改动 `ipv6` 这个设置、不重新生成配置、也不碰服务 —— 但它和除 `sc doctor` 以外的所有子命令一样，仍然会先走一遍常规启动流程（全新机器上这一步会创建 `/etc/sing-box` 与 `/var/lib/sing-box`、写入初始的 `nodes.json` / `settings.json`；而**任何**还没记录下有效 Clash API 端口的机器上 —— 全新安装，或者从早于端口自动探测的版本升级上来的机器 —— 这一步都会探测一个空闲端口并写进 `settings.json`）。**需要注意**：这里没有第二个解析器，也没有可配置的等待时间 —— 代理侧解析器连得上却不回答时，仍然是没有回答，且不会去问别的解析器（`NXDOMAIN` / `SERVFAIL` 也原样转达，不会重问，因此不会有域名因为一次失败被暴露给国内解析器）；地址只能通过 IPv6 解析的节点需要 `sc ipv6 on`。已有机器升级后 `sc reload` 一次即可，不需要手改 `/etc/sing-box` 下的任何文件，也不会多出「配置被外部修改」的告警。同时修掉一处归因错误：`sc` 自己生成的配置片段出错时，报错不再指着用户的 `override.json` 说事 —— 只有确实是用户那份文档出错时才会点它的名。

- **节点自动故障切换 + `sc ls` 显示延迟**：以前 `proxy` 是一个 selector —— 它只是「选中并保持」，选中的节点开始丢包、握手挂起时，它不会重新考虑，只有人工执行 `sc use <别的节点>` 才会换；而 `remote_dns` 走的是 `detour: proxy`，所以一个变差的节点会同时拖垮流量和域名解析。现在只要至少有一个节点，生成的配置里就会多出一个标签为 `auto` 的**自动选择组**（`urltest`，它自己也是 `proxy` 的一个成员）：由 sing-box 每 3 分钟用 `https://www.gstatic.com/generate_204` 探测每个节点，把流量切到响应最快的那个，切换过程不需要任何人敲命令、也不重启服务（`tolerance: 50` 毫秒，避免在两个差不多快的节点之间来回抖动；`idle_timeout: 30m`，组没被使用时停止探测）。**切换的粒度就是这个探测周期，不是瞬时的**：节点变慢或开始拒绝连接时，实测约一个探测间隔（3 分钟上下）之后组才会换走，这段时间里发出的请求仍然是坏的；而「TCP 还能连上、之后一直不回应」这种挂死型故障它覆盖不了 —— 探测本身会被挂住，而不是判定这个节点不可用，一次永远结束不了的探测也就永远不会更新选择，实测组会一直留在这种节点上不换走，这种情况仍然需要人工 `sc use <名称>` 切到别的节点。`sc use auto` 选中这个组，`sc use <名称|序号>` 仍然是钉死单个节点，解析规则、序号编号和输出文字都没有任何改动。**已有机器不会被动到**：升级后 `active` 仍然是原来那个节点，`sc reload` 一次即可，不需要手改 `/etc/sing-box` 下的任何文件，也不会多出「配置被外部修改」的告警；只有过去由 `sc` 替用户随便挑一个节点的三个位置（首次 `sc add`、删掉当前节点、修复失效的选择）改成了选中这个组。`auto` 同时成为保留标签：分享链接的名字正好是 `auto` 时会自动改成 `auto #2`，不会和这个组重名；反过来，如果你机器上本来就有一个叫 `auto` 的节点，这台机器不会生成这个组（两个出站不能同名），`sc use auto` 在那里钉的还是那个节点 —— 改个名字，组就会自己出现。`sc ls` 增加最后一列「延迟」：显示正在运行的 sing-box 自己存下来的每个节点的探测结果（`sc` 只读一次 Clash API，不做任何测量、不新增任何超时），没有存下延迟的节点显示 `-`（不是 0、不是空格）；组被选中时它单独占一行、没有序号，地址列写明此刻实际在用的哪个节点。服务没运行时这一列全是 `-` 且完全不发起查询，API 不通或返回内容异常时表格照常打印、不会抛 Python 报错。

- **新增 `sc doctor` 一键体检**：一条命令按因果顺序打印七项事实 —— sing-box 可执行文件与版本、四个规则集（状态与字节数来自同一次读取，不看 `st_size`）、配置文件与 `sing-box check` 的结论（检查器自己的报错原样引用，最多五行，多余的行数会如实说明）、服务是否运行与是否开机自启（两件独立的事实，直接问 init 系统）、TUN 接口与地址、Clash API 端口与是否响应、出口 IP（服务停着也照样查，这时它最能说明问题）。每行标注 `正常 / 异常 / 未知` 三种结论之一，`未知` 表示这项检查根本没能执行（工具缺失、权限不足），而不是「被检查的东西坏了」；任何一项探测失败都不会中断其余六项，七项永远都会打印，也不会抛出 Python 异常。**全程只读**：不生成配置、不下载、不重启、不启用、不修复，而且与其他子命令不同，它连启动阶段那步也不做 —— 不会创建 `/etc/sing-box`、不会写入 `nodes.json` / `settings.json`、不会探测并保存 Clash API 端口，因为机器已经坏掉或全新时，这些路径的「空」本身就是诊断结论。退出码：0 = 全部正常，1 = 至少一项异常，2 = 没有异常但至少一项未知。输出被重定向到文件或粘贴进 issue 时不含任何 `\r` 和终端控制字符（外部工具输出里的也会被清掉）。`sc status` 的行为与输出没有任何改动。

- **规则集多镜像回退 + 下载校验 + 进度显示**：`sc update-rules` 不再只认 GitHub 一个地址，而是按顺序尝试 jsDelivr / testingcf / ghfast / raw.githubusercontent 四个镜像，任何一个失败（连不上、非 2xx、内容不合法）就换下一个；同一次运行中已失败的源会被跳过，所以四个镜像的总耗时与过去单个地址一致，超时时间没有任何改动。前面的镜像失败、后面的镜像成功时，失败原因会追加在该规则集那一行结果之后（`；已回退，前序镜像未成功：<镜像> -> <原因>`），镜像配错或某个源挂掉不会因为有备用源就被悄悄掩盖，`/var/log/sing-box/install.log` 里一眼可见；四个镜像都正常时这一段不会出现，输出与过去完全一致。下载改为分块读取：内容必须带 `SRS` magic、达到最小体积，且在响应声明了 `Content-Length` 时收到的字节数必须与之相等，校验通过后才原子替换到正式路径 —— HTML 错误页和传输不完整的响应永远不会落盘。终端下显示实时进度（字节数 / 百分比），输出被重定向到文件或日志时保持每个规则集一行结果、不含任何 `\r`。临时文件名带上进程号，定时任务与手工执行并发时不会互相破坏；上一次被杀死的运行残留的临时文件会在下次下载该规则集时清掉。新增 `--mirror <基地址>`（可重复）与 `SB_RULES_BASE` 环境变量用于指定镜像，两者都是替换内置列表而非追加。

- **安装过程显示每个下载部分的进度**：以前 `install.sh` 里三处下载全都用 `curl -fsSL`，其中体积最大的 sing-box 二进制包（几十 MB）在下载时屏幕上没有任何输出，看不出是在下载、卡住了、还是已经结束。现在下载参数由脚本顶部统一决定：sing-box 二进制包在终端下显示 curl 自带的单行进度条，输出被重定向到文件或日志时（`... > install.log 2>&1`、CI、issue 粘贴）保持与过去逐字节一致的静默，日志里不会出现 `\r` 和进度残影 —— 判断依据是**标准错误**是不是终端（curl 的进度条写在标准错误上），所以 `... | tee install.log` 这种「终端还在、只是标准输出被接走」的用法照样能看到进度条。另外，远程安装（`curl | bash`）时逐个报出正在获取的文件名（`↓ 获取 bin/sc ...` 等五个），下载二进制包前先报出解析到的版本号与架构（`↓ 获取 sing-box v1.10.0 (amd64) ...`）——这一行终端和日志里都有，卡住时能一眼看出卡在哪一步。下载失败的处理、超时与重试行为、下载内容与来源均无任何改动。

### 修复

- **一条生成不出可用配置的分享链接，不再永久留在 `nodes.json` 里拖垮此后每一次重新生成**：`sc add` 以前的顺序是「先把节点写进 `nodes.json`、再重新生成配置」。当 `sing-box check` 拒绝这份文档时（实测触发条件是 `ss://` 里带一个当前 sing-box 版本不认识的加密方式，例如 `unknown method: bogus-cipher`；注意 `server` 为空这类问题 `check` 是**放行**的，要到运行时才失败），`config.json` 保持不动是对的 —— 但那个坏节点**已经躺在 `nodes.json` 里了**，于是此后每一次 `sc reload`、`sc use`、`sc rm`（删的是别的节点）以及无人值守的规则集定时器触发的重新生成，全都会被同一个节点拒同一次，直到有人读懂那行提示并执行 `sc rm`。而它当时打印的是 `已添加：{tag}（⚠️ 配置检查失败）` —— 一句自相矛盾的话。现在把 `config.json` 早已贯彻的「**先校验、再保留**」补齐到 `nodes.json` 上：追加之前先深拷贝一份原始文档，`reload_or_restart()` 为假（它为假当且仅当 `generate_config()` 为假，而那种情况下 `config.json` 逐字节未变）或抛出 `OverrideError`（`override.json` 不可用，同样等于「没能生成配置」）时，`nodes.json` 被逐字节还原，命令打印 `未添加：{tag} —— 无法据此生成可用的配置；nodes.json 未改动` 并以 **1** 退出。拒绝的原因仍然由 `generate_config()` 原样引用检查器打在标准错误上，就在这一行的上面。**成功路径完全没有改动**：通过校验的链接照常写入、照常重启、照常打印原来那句 `已添加：{tag}（{type} → {server}:{port}）` 并以 0 退出。

- **`sc use` 不再在失败时一声不吭**：走「服务没在运行、因而必须重新生成配置并重启」这条路径时，如果重新生成失败，函数会走到末尾却什么都不打印，退出码 **0** —— 屏幕上一片空白，而它读起来只能是成功；实际发生的是选择写进了 `nodes.json`，此外什么都没发生。现在这条路径与快速路径一样，每次都欠用户一行结论：失败时打印已有的那句 `重新加载失败` 并以非 0 退出（复用现有译文，没有新增字符串）。经由 Clash API 即时切换的快速路径没有任何改动。

- **`sc mode` 不再报告一件不会发生的事，`settings.json` 里那个只写不读的 `mode` 键随之删除**：服务没在运行时，这条命令会跳过 Clash API 调用、把模式写进 `settings.json`，然后照样打印 `路由模式 → global`。可是 `settings["mode"]` 在整个 `bin/sc` 里**没有任何读者** —— 生成配置时不读它，`sc status` 显示的模式来自正在运行的 Clash API。实测（sing-box 1.13.15）确认了模式的真正住处：它由 sing-box 的 `cache_file` 带着跨越重启，而且文档里的 `experimental.clash_api.default_mode` **打不过**缓存里已经存下的那个值。换句话说，停机时设的这次模式永远不会生效，而且没有任何一种写文件的办法能让它生效。现在这条命令只做它真能做到的事：服务没在运行时，一行说明「路由模式是即时生效的设置」并给出 `sc on`，非 0 退出，**不写任何文件**；Clash API 没有接受这次切换时（以前这个返回值是被丢弃的）同样一行说明并非 0 退出。`_init_files()` 和 `install.sh` 的种子里都不再写入 `mode` 键；已有机器 `settings.json` 里留下的那个键会被直接忽略。服务正在运行时的成功路径逐字未变（模式仍按用户输入的小写发送，实测 API 会自行规范化）。

- **`sc status` 在没有 iproute2 的机器上不再抛调用栈**：`=== TUN 接口 ===` 一节直接裸调 `ip`，机器上没有这个命令时整条命令以 `FileNotFoundError` 中止，它下面的规则集、当前节点、路由模式、Clash API 端口、出口 IP 全都不会打印。`sc doctor` 对同一个设备的读取本来就是包了 `OSError` 的，现在 `sc status` 与它一致：用已有的 `（错误：{e}）` 译文渲染原因，后面每一节照常打印。

- **`sc help` 里 `doctor` 的项数与实际一致**：中英两份帮助都还停在「七项只读检查」（中文那份连 `退出码 0 = 七项全部正常` 一并如此），而 `DOCTOR_SECTIONS` 早已是九项 —— 两份 README 和代码注释写的也都是九项，只有帮助文本没跟上。

- **自举 sudo 不再写死安装路径**：以前是 `sudo /usr/local/bin/sc`，于是以普通用户在仓库里运行源码副本时，实际执行的是**已安装的那一份** —— 改了代码却看不出任何变化，而这恰恰是最难自查的一类现象。现在重新执行的是**这个文件自己**：`argv[0]` 带目录分量时取其绝对路径，否则用 `shutil.which()` 重做一次 shell 的 PATH 查找（`sc ls` 这种写法下 `argv[0]` 就是 `sc`，直接取绝对路径会指到当前工作目录）。重新执行的**形式保持 `sudo <sc 的绝对路径> <参数>` 不变**，因为这正是 `/etc/sudoers.d/sc` 授予 NOPASSWD 的那条命令 —— 中间插一个解释器（`sudo python3 <路径>`）会匹配不上该规则，让每一台已安装机器的每一次调用都弹出密码提示。同时给仓库里的 `bin/sc` 补上可执行位（此前是 0644，`./bin/sc` 根本跑不起来；安装时本来就是 `install -m 755`，已安装机器不受影响）。

- **`sing-box check` 不通过的配置不再落到 `/etc/sing-box/config.json` 上，也不再成为漂移基准**：以前 `sc` 的顺序是「先写 `config.json`、再记录摘要、最后才问 `sing-box check`」，检查不通过时它只是打一行错误、不重启服务 —— 但那份被拒绝的文档**已经躺在磁盘上了**，而 systemd 单元每次启动读的正是这个文件。于是故障被推迟到与出错时刻完全脱节的将来：重启、断电重来、`sc on`，或者最要命的那种 —— 每周的规则集更新定时器在没有重新生成配置的情况下重启服务，无人值守地让 sing-box 起不来（`Restart=on-failure` 会一直重试同一份坏配置）。同时那份坏文档还被记成了「`sc` 最近一次生成的内容」，把唯一一个「磁盘上这份不可信」的信号也抹掉了，之后 `sc doctor` 的配置改动一行只会说「与记录一致」。现在顺序变成**先验证、再落盘**：文档先写到 `config.json` 同目录下一个新建的临时名字上（同样是 0600、同样经由那唯一一个凭据写入函数，权限从第一个字节之前就是对的），`sing-box check` 检查的是**那一份**，只有它不拒绝，`config.json` 才会被替换、漂移记录才会被写入；无论哪种结果（通过、拒绝、出异常）那个临时文件都会被删掉，`/etc/sing-box` 下不会留下任何多余的东西。**被拒绝时磁盘上一个字节都不会变**：`config.json` 和漂移记录都保持运行前的样子（本来就没有这两个文件的全新机器上，运行完仍然没有），错误信息点名 `config.json` 说明它没有被改动，并原样引用检查器自己的说法（终端颜色控制符会被清掉，粘进 issue 里是干净的一行；检查器什么都没输出时则说明退出码）。**另一半修复是不再抛异常**：机器上没有 `sing-box`、有但根本执行不了、或者它输出了解码不了的字节 —— 这三种情况以前会直接抛出 Python 调用栈，`sc update-rules` 那次运行连一行结论都没有。现在「无法验证」不等于「验证不通过」：文档照常写入、照常记录，stderr 上多一行说明它未经检查以及原因，运行照常算成功。**请注意这一条的代价**：在一台 `sing-box` 装坏了的机器上，以前会中途抛异常停下的运行，现在会走完并让调用方重启服务。每次运行仍然只起**一个** `sing-box check` 子进程，生成的配置字节、`sc doctor` 的所有行均无任何改动；**在 `sing-box check` 能运行、输出也能解码的机器上**，`sc reload` / `sc add` / `sc update-rules` 的**标准输出与退出码**同样无任何改动 —— 变化的只有**标准错误**上的那句话（就是上面重新措辞过的那两行）。

- **`settings.json` 读不动时不再被默认值悄悄顶替、写不进去时不再是一段调用栈，所有文件读写都显式声明 UTF-8**：以前 `/etc/sing-box/settings.json` 一旦变成读不了的文档（手改坏了、写到一半被打断只剩半截、或者根本不是一个 JSON 对象），`sc reload` / `use` / `add` / `rm` / `mode` / `ipv6` / `telemetry` 这些会重新生成配置的命令只在开头打一行「无法使用 …」的告警，然后**照样用默认值把 `config.json` 整个换掉** —— 界面语言、路由模式、IPv6 决策、遥测拦截、已记录的 Clash API 端口全部按默认值重写，等于把你从没选过的选择装进了正在运行的配置里。现在这类运行**直接拒绝执行**：一行说明点名该文件和原因，非 0 退出，`config.json`、漂移记录 `.config.sha256` 和 `settings.json` 三者一个字节都不会变，服务也不会被碰。**只报告的命令不受影响** —— `sc doctor` / `config` / `ls` / `now` / `status` / `log` 以及 `sc ipv6|telemetry|update-interval show` 照常在文档化的默认值上运行，退出码也照旧。**回到能用状态的办法是删掉这个文件**：文件不存在等于「全部按默认」，下一次运行会重新生成它 —— 「不存在」从来都不算「读不动」。第二处：写 `settings.json` 失败（文件被设成只读、磁盘满、或者手工塞进了一个 UTF-8 编不出来的字符）以前是一段 Python 调用栈，现在与写 `nodes.json` 失败共用同一句话「无法写入 {路径}：{原因}」并以非 0 退出；唯一的例外是首次探测 Clash API 端口时那次顺手的保存 —— 它仍然静默失败、不改变这次运行的结果，所以只读机器上的只读命令不会因此报错。第三处：`sc` 里每一处文本读写现在都写明 UTF-8，不再由进程 locale 决定怎么解读文件的字节。实测在 `LC_ALL=C`（且关闭 Python 的 UTF-8 模式）的环境下，只要 `config.json` 里有一个非 ASCII 的节点名，`sc config` 以前会直接以「无法读取 …: 'ascii' codec can't decode byte 0xe8」失败、什么都不打印，`sc doctor` 的 AAAA 一行也以同一句话报「未知」；现在两者的结论与 UTF-8 环境下逐字一致（编码不了的字符按既有约定降级成反斜杠转义，不再中断整个命令）。`settings.json` 可用时生成的 `config.json` 一个字节都没有变化，该文件的权限（0644）和写入方式也都没有改动。
- **`sc doctor` 的三行结论现在只说自己真的查到的事**：以前有三行是拿「代理指标」当结论的。**IPv6（AAAA）** 一行只检查那条规则在不在 `dns.rules` 里，不看位置 —— 可这个项目对外承诺的正是**位置**：这条规则排在两条 `clash_mode` 规则之前才能在 `rule` / `global` / `direct` 三种模式下都生效，而排到后面（例如被 `override.json` 往前插了别的规则）它在 `global` 和 `direct` 下就是死的，此时旧版仍然报 `[正常]`；现在这一行只有在该规则是 `dns.rules` 第一条时才是 `[正常]`，否则报 `[异常]`，并同时给出两条修复线索：运行 `sc reload` 重新生成，以及检查 `override.json` 是不是自己往前插了规则。**节点延迟**一行以前在「没有 init 系统」的机器上从不发出 `/proxies` 请求，却照样打印 `0/N` —— 一个没有任何请求支撑的数字；现在 `sc doctor` 已经确认 Clash API 有响应之后，读到的就是真实的计数（`n/N`），读不到时那句话也改成只陈述「读到了什么」。**DNS 解析**一行以前说「解析成功（经由正在运行的 sing-box）」，可这个应答完全可能直接来自 sing-box 自己的 DNS 缓存（这次探测本身也会写入该缓存），毫秒数并不代表上游真的解析了一次；现在三种结果都只陈述「正在运行的 sing-box 给出了什么应答」，并点明缓存是应答的可能来源，两个异常分支也不再暗示换节点一定能绕开已缓存的应答。顺带修掉 `sc ipv6 <值>` 在决策没变时那句话的缺口：它现在和 `sc telemetry` 用同一句已有的译文，明确告诉你「若当前配置生成于该设置之前，请运行 `sc reload`」。退出码是一组**标签**而不是严重程度的刻度（`[正常]`→`0`、`[异常]`→`1`、`[未知]`→`2`），所以它根本没有「方向」这个属性可谈；这次改动能产生的退出码变化**恰好是下面三种，没有第四种**：**`0 → 1`** —— `config.json` 里那条规则不在第一位、此前每一行都 `[正常]` 的机器（这正是本次要纠正的判断）；**`2 → 1`** —— 同样是那条规则不在第一位、但本来就有 `[未知]` 行且没有任何 `[异常]` 行的机器（例如还没有漂移记录的升级机器），它的退出码**变小了**；**`1 → 2`** —— 没有 init 系统、Clash API 正常应答、且至少有一个节点读到了延迟的机器，节点延迟一行从 `[异常]` 变成 `[正常]`，而「服务」与「开机自启」两行本来就是 `[未知]`（没有 init 系统可问，新旧版本都一样），此前被节点延迟那一行 `[异常]` 压住，现在没有任何一行 `[异常]` 了，退出码报的就是「未知」。本来就带着 `[异常]` 行的机器退出码不变；**DNS 解析**那一行只改了措辞，它能返回的判定类别一个都没有变，所以不产生任何退出码变化。生成的 `config.json` 一个字节都没有变。

- **输出层现在只有一套约定：英文界面不再把翻译键当标题打印，同一个事实在 `sc status` 和 `sc doctor` 里用同一种标点，计数短语在任何数字下都只有一种写法，重定向到文件时子进程的输出也不会再跑到标题上面**：以前英文用户执行 `sc ls`，表头读作 `ls.idx  ls.active  ls.type  ls.name  ls.address  Delay` —— 五个键原样打印、还和一个正常英文单词混在一起（这个缺陷从 T-02 起就已知，并被此后每一个任务推迟）；现在读作 `#  On  Type  Name  Address  Delay`，列宽、列序和中文表头一个字节都没变。`sc status` 的规则集行以前是 `可用, 30 天前`（ASCII 逗号），`sc doctor` 却是 `可用，5572 字节`（全角逗号）—— 因为前者的标点写在格式串里、后者写在翻译条目里；现在标点一律属于翻译条目本身，两屏一致，英文渲染逐字节不变。`_age_text()` 以前会渲染 `1 days ago`，现在**所有** 14 个计数短语（含字节数）一次性改为在任何数字下都成立的单一写法，不引入任何复数机制。`sc status > file` 以前会把 `ip` 和 `systemctl` 子进程的输出打到第一个标题**上面** —— 因为 Python 的 `print()` 在管道上是块缓冲、而子进程直接写 fd 1 —— 恰好只在「把输出贴进 bug 报告」这个场景发生；现在标准输出在 `main()` 里一次性配置为行缓冲，`sc update-interval` 同样的问题一并修好，`sc -h` 的输出也在保证范围内。同一次配置还让**标准输出编码不了的字符不再中断整个命令**：在非 UTF-8 的 locale 下，`sc ls` 里带非 ASCII 标签的节点、以及 `sc add` 成功行里的 `→`，以前会让命令在把数据正确写盘之后仍以非 0 退出，现在字符降级显示、命令照常走完并正常退出。**行为边界**：终端下的渲染除上述字符串改动外与之前完全一致；未做任何列宽或对齐的重新设计；`sc config` 输出的「可被解析器接受的 JSON 文档」这一承诺，在标准输出编码无法表示文档内容时不再无条件成立，两份 README 已相应写明。

- **`override.json` 里任何一种用不了的写法，现在都只剩一行说明和一个非 0 退出码，不再是一段 Python 调用栈、也不再悄悄换掉你的数组**：以前有八种写法会走岔 —— 文档嵌套深到 JSON 解析本身就栈溢出；浅一点、但仍深到合并时的深拷贝栈溢出；往 `dns.rules` 和 `route.rules` 里塞进一个不是对象的元素（合并之后过滤规则时抛 `AttributeError`）；以及在**当前值是数组**的那个键上写一个对象、一个标量、一个 `null`、或者一个只有 `"0"` 这类下标样式键的对象。前四种直接吐出一段 Python 调用栈；后四种要看你写在**哪个**键上 —— `dns.rules`、`route.rules`、`route.rule_set` 这三个键在写盘之前另有一道检查，过去也已经是一行说明加非 0 退出、`config.json` 一个字节都不变（本项没有改变这三个键上的结果，只是把说法收敛成了下面那一句）；而其余当前值是数组的键（`dns.servers`、`inbounds`、`outbounds` 这些）没有这道检查，那里才是更糟的那一种：不报错，把你那份数组**静默替换**成你写的那个值，再把这份已经坏掉的文档正常写进 `config.json`，并且**在校验之前**就把漂移记录也更新成它 —— 原来那份能用的配置就此被覆盖，漂移记录也已经把这份坏掉的文档记成了「`sc` 最近一次生成的内容」；随后 `sing-box check` 才会拦下它、让这次运行以非 0 退出（`dns.servers` / `inbounds` / `outbounds` 三个键上实测都是如此）。现在从「读取 `override.json` 的字节」到「生成配置文档的字节」这一整段是**一个整体**：其中任何一步出错都渲染成**一整行** `无法使用 /etc/sing-box/override.json：…`，随后以非 0 退出：写错在哪儿说得清的（比如下面那条数组规则），这一行点的就是那个位置；说不清的（栈溢出、`AttributeError` 这类），带上一句指明故障类别的说明 —— **只写异常的类名**，异常自己那句可能抄着你文档内容的消息不会被打印出来；`config.json` 与漂移记录（`.config.sha256`）**一个字节都不会变**，`sing-box` 一次都不会被调用，服务也不会被碰。同时把数组位置的规则收成一句：**当前值是数组的那个键只接受指令对象**（`$prepend` / `$append` / `$replace` / `$before` / `$after` 之一）——在那里写对象、标量、`null` 或裸数组，得到的是**同一句**报错并列出这五个指令；其中「裸数组」这一条的措辞和触发条件与过去完全一致，要清空一个数组请用 `$replace` 配 `[]`。**能用的 override 没有任何变化**：两份 README 里公布的每一份写法、以及五个指令各自的用法，生成出来的 `config.json` 与过去逐字节相同。

- **`settings.json` / `nodes.json` 坏掉时不再是一段 Python 报错，而是一句写明文件与原因的话；这两份文档也不再受系统语言环境影响**：这两个文件是 `sc` 自己写、也自己读的**状态文档**，此前**全文只有三处**读它们的代码，都是 `json.loads(路径.read_text())` —— `read_text()` 用的是进程的语言环境编码，于是文件里只要有一个非 ASCII 字节，在 `LC_ALL=C` 这类非 UTF-8 环境下就会抛 `UnicodeDecodeError`（它是 `ValueError`，**不是** `OSError`，本项目习惯的 `except OSError` 一律拦不住）。加上文件不是 JSON、顶层不是对象、`nodes.json` 里没有 `nodes` 这几种情况，`sc ls` / `now` / `use` / `add` / `rm` / `status`、配置生成，以及 `sc lang` / `mode` / `ipv6` / `telemetry` / `default-tun` / `on` / `off` / `update-interval` 全都会直接吐出 Python 调用栈。现在**读取只有一处**：文件按 **UTF-8 显式解码**（与系统语言环境无关），顶层必须是 JSON 对象、`nodes.json` 的 `nodes` 必须是数组，失败时统一交给 `sc` 已有的那一处渲染，打印**一句** `无法使用 <文件>：<原因>`（原因是「不是有效的 UTF-8 文本」/「不是有效的 JSON（…）」/「顶层必须是一个 JSON 对象」/「"nodes" 成员必须是 JSON 数组」/「无法读取（…）」之一）并以非 0 退出；**要写状态文档的命令一律在写之前中止**，坏掉的文件保持一个字节都不变，仍可以手工修好。只读的四个设置读取器（语言、Clash API 端口、`ipv6`、`telemetry`）行为不变 —— 仍然回落到各自原有的默认值（`en` / 未记录 / `auto` / `block`），一次运行**只在 stderr 上多出一行**说明 `settings.json` 用不了（这一行必然是英文：语言偏好正躺在那份读不出来的文件里）；`settings.json` 不存在时和过去完全一样，不打印这一行。`sc doctor` 依旧不会因为这两个文件中止，整张表照打，节点延迟那一行照旧写明它读不了哪个文件。**另一半是写入**：`config.json`、`nodes.json`、漂移记录和 `settings.json` 现在都显式以 UTF-8 写出，非 ASCII 字符仍然原样写入（不转成 `\uXXXX`），所以在非 UTF-8 环境下 `sc add` 一个密码含非 ASCII 字符的分享链接，**写入不再失败、凭据按 UTF-8 原样落盘**（写进文件的就是那串字符本身，而不是 `\uXXXX`，也不再是一段 `UnicodeEncodeError`），机器上已有的中文节点名也不会在下一次写入时被毁掉；写入失败（包括参数根本无法编码成 UTF-8）会渲染成已有的那句「无法写入 …」并带上原因，原文件保持不变、不留临时文件。`settings.json` 的首次写入也归拢到唯一的那个写入函数上。**注意**：本项终结的只是这两份文档在**磁盘**上的编码问题，并没有让任何命令在非 UTF-8 环境（例如 `LC_ALL=C` 且未启用 Python 的 UTF-8 模式）下变得可用 —— 剩下的那一半是**输出流**的编码，另有一项在跟进。具体地：在这类环境下 `sc add` 仍然会在打印它自己那行成功提示（`Added: … (… → …)`，其中的 `→` 是 `sc` 自己写的字符，因此**分享链接全是 ASCII 时同样会发生**）时失败，并以非 0 退出 —— 但此时**节点已经正确写进 `nodes.json` 了**：磁盘上的数据是完整的，不需要、也不应该重新添加一遍，回到 UTF-8 环境下 `sc ls` 就能看到它。同样地，把一个中文节点名**打印**到终端也仍然会失败。文件权限、写入方式、以及配置漂移记录仍以文件**字节**计算的做法都没有改动。

- **分享链接里的密码没能完整存下来：tuic 节点的密码一直是空的，trojan / hysteria2 的密码在冒号处被截断**：`sc add` 解析分享链接时，读 userinfo（`@` 之前那一段）的五个解析器各自对「它到哪里结束、哪个冒号是字段分隔符、什么时候做百分号解码」有一套自己的判断，其中四个用的是 Python 标准库的 `username` 字段——而它在 userinfo 的**第一个**冒号处就停住了。**已经存下来的节点里，受影响的正好是这四类**：（1）**每一个 tuic 节点**，无论分享链接里带的是什么密码，存下来的 `password` 都是空字符串（tuic 的 userinfo 是 `uuid:密码`，密码整段被丢掉了，因此存下来的这个节点手里已经没有它的分享链接带来的那份凭据）；（2）**trojan 与 hysteria2** 节点中，密码本身含有**未经转义的冒号**的，密码被从那个冒号处截断；（3）**shadowsocks** 节点中，**从 base64 还原出来的**密码含有 `%XX` 这类转义序列的，被多做了一次百分号解码而变了样（那一段本来就不是 URI 文本，不该解码）；（4）**vless / tuic** 的 id 在分享链接里做过百分号转义的（例如 `%2D`），存下来时仍是转义后的样子。**其他节点都没有受影响**：shadowsocks 密码里的冒号、以及 trojan / hysteria2 / shadowsocks 明文形式密码里的百分号，今天存下来的就是对的，不必去动它们。**修复已存节点的办法只有一个**：`sc rm <节点>`，然后 `sc add '<分享链接>'`。`sc reload` **修不好**它——`sc add` 只在添加时解析一次，存进 `nodes.json` 的是解析完的节点对象，分享链接本身从来没有被保存过，所以重新生成配置只会把同一个错误的密码原样再写一遍。**这次改了什么**：整个文件现在只有一处判断 userinfo——它是 authority 里**最后一个** `@` 之前的那一段，字段边界是其中**第一个未经转义的冒号**，切分在原始文本上完成、之后每个字段只做**一次**百分号解码，因此 `%3A` 永远只是数据里的一个冒号、不会变成分隔符（`tuic://a%3Ab:pw@主机:端口` 的 uuid 就是 `a:b`、密码就是 `pw`），`100%2525` 也正好解码成 `100%25`。tuic 读 `uuid:密码`（第一个冒号之后的每一个冒号都属于密码），trojan / hysteria2 把**整段** userinfo 当作密码（里面的冒号都是密码的字节），shadowsocks 从 base64 还原出来的内容不再被二次解码，vless 的 id 会被解码一次。附带两处小变化：shadowsocks 明文形式的 userinfo 不含冒号时（如 `ss://aes-256-gcm@主机:端口`），过去会漏出一行 `Error: not enough values to unpack`，现在按「缺的字段就是空的」处理；`vless://主机:端口` 这种根本不带 userinfo 的链接，`uuid` 由 `null` 变成空字符串。**没有改动的部分**：vmess（它的凭据来自 base64 JSON，不经过 userinfo）、所有解析器的主机 / 端口 / 节点名 / 传输方式 / TLS 处理、每一条命令、每一句提示文字，以及上面四类之外的每一个已存节点。

- **本机 Clash API 不回应、或回应内容异常时，不再把一段 Python 报错打到屏幕上**：以前只有「连不上」和「返回 HTTP 错误状态」这两类失败被当作失败处理，另外五类会直接变成一段 Python 报错——端口被别的程序占着、连上了却一直不回应（要等满 3 秒超时）；回应是 2xx 但正文不是合法 JSON（例如一个 HTML 错误页）；正文不是合法 UTF-8；正文比它自己声明的长度短（连接中途断了）；以及正文是合法 JSON、但不是一个对象（`5`、`"x"`、`[1,2]` 这类），最后这一类过去甚至不报错，而是被当成正常回答继续使用，比报错更糟。现在这五类一律与「连不上」得到完全相同的处理，也就是「这次没有拿到回答」：`sc status` 的路由模式那一行显示「（不可用）」，它前后的各行照常打印，退出码仍然是 0；`sc ls` 的延迟列显示 `-`，表格照常打印；`sc use <节点>` 不会再打印一次并没有真正发生的切换，而是像端口连不上时那样重新生成配置并重启服务，然后打印带「（服务已重启）」的那一行（节点选择在此之前就已经存下，重启后即生效）；`sc mode <模式>` 照常保存设置并打印「路由模式 → <模式>」，但正在运行的进程要到下次重启或重新加载才真正换过去——这一点与端口连不上时的表现完全一致；`sc doctor` 的「Clash API 是否响应」一行报「异常」，而且端口那一行仍然会打印出来，不再整节塌成一行「未知」（正文不是对象的那一类过去这一行反而显示「正常」），也就是这一节由「未知」或「正常」变成「异常」，因此在没有任何其他检查项报「异常」的前提下，这次运行的退出码相应地由 2 或 0 变成 1。一切正常的机器上的行为、所有输出文字、3 秒超时，以及每条命令向 Clash API 发起的请求次数，都没有任何改动。

- **凭据文件在写入的那一瞬间也不会被别人读到**：`config.json` 和 `nodes.json` 里都带着节点的密码 / UUID / Reality 公钥，以前的写法是「先把内容写进目标文件，再把权限收成 600」——两步之间有一个窗口，而且文件**第一次被创建**时用的是系统默认权限（常见的 umask 下就是 644，`/etc/sing-box/` 目录本身任何本地用户都能进），凭据是在这个 644 的文件里落的盘；如果目标文件本来就是 644（比如从旧版本升级上来的机器），那整个写入过程它都是 644。现在改成：先在同一个目录里建一个新文件，在它还是空的时候就把权限设成正好 600，然后才写内容，最后原子地移动到正式路径。因此这两个文件在任何一个瞬间都不会以宽于 600 的权限持有任何一个字节，无论系统 umask 是多少（umask 设成 0277 时结果仍然是 600 而不是 400），也无论目标文件原来是什么权限。顺带的两个改进：`config.json` 的替换现在是原子的，另一个进程（比如每周的规则集更新定时任务）在读它时只会读到完整的旧版本或完整的新版本，不会再读到写了一半的文件；写入失败时（磁盘满、只读文件系统、权限不足）原来的文件保持一个字节都不变，并按当前语言打印一行说明写不进哪个路径、系统给的原因是什么，而不是抛一段 Python 报错。安装器也在结尾加了一段权限检查：逐个报出 `config.json` / `nodes.json` 的当前权限，宽于 600 的收紧到 600 并说明「原来是多少、现在是多少」，改不动的如实报出并给出可直接执行的 `chmod` 命令，文件不存在就说不存在；这一段永远不会中断安装，也不会改变安装结尾的横幅和退出码。**升级方式**：重新跑一遍安装命令即可——安装器会覆盖 `sc`、重新生成配置，再由这段检查兜底修掉历史遗留的宽权限。注意 `config.json` 变成 600 之后，普通用户直接执行 `sing-box check -c /etc/sing-box/config.json` 会因为读不到文件而失败，这是预期行为，加 `sudo` 即可。`settings.json` 的权限没有任何改动（它不含凭据），生成出来的配置内容、检查顺序、超时也都没有任何改动。

- **规则集缺失不再导致服务起不来**：以前 `config.json` 里那四条 `rule_set` 定义是无条件写入的，只要 `.srs` 没下载下来，sing-box 就会在路由初始化阶段 FATAL（`parse rule-set[0]: open .../geoip-cn.srs: no such file`），配置校验失败，服务永远起不来，后续每次 `sc add` 都重复同样的报错 —— 一个可选的分流优化变成了硬性启动依赖。现在生成配置时会逐个判断规则集是否可用（存在、是普通文件、带 `SRS` magic、达到最小体积），只写入可用的定义，并把 `dns.rules` 和 `route.rules` 中引用了不可用规则集的规则一并剔除（引用了多个规则集的规则只保留可用的那些），因此配置里不会再出现指向未定义标签的引用。降级是逐个文件的，不是全有全无：四个里坏一个就只掉那一个。降级不算错误，`sc add / rm / use / mode / default-tun / reload` 照常成功返回。每次生成降级配置都会在 stderr 用双语提示「几个 / 共几个不可用、分别是哪些、原因是什么、用哪两条命令恢复」，并区分「全部不可用 = 已降级为无分流模式」和「部分不可用 = 已跳过引用它们的分流规则」两种措辞。`sc update-rules` 补齐规则集后会自动重新生成配置并（在服务正在运行时）重启，无需再手动执行 `sc reload`。

- **Clash API 端口自动探测**：以前 `external_controller` 写死为 `127.0.0.1:9090`，与 xray / clash / cockpit 等同样默认占用 9090 的服务安装在同一台机器时会 `bind: address already in use`，导致 sing-box 启动即崩溃、TUN 建不起来。现在改为在高位区间（`29090` 起）自动探测一个空闲端口，写入 `settings.json` 的 `clash_api_port` 后固定复用（不会在重启时漂移）；`settings.json` 中已存在的值会被原样沿用，可手动指定。`sc status` 会显示当前 Clash API 端口。
- **安装器如实报告安装结果**：以前 `install.sh` 第 7 步先执行 `sc reload` 再 `systemctl enable --now`，一旦规则集下载失败导致配置校验不通过，脚本会在 `set -e` 下直接中断，开机自启和自动更新定时器都没来得及注册；第 6、7 步的错误又全部丢进 `/dev/null`，结尾还无条件打印「✅ 安装完成」。现在：先无条件注册开机自启（`systemctl enable` / `rc-update add`，失败也不中断），再按配置生成是否成功决定要不要 `start` 服务；第 6、7 步的输出统一追加到 `/var/log/sing-box/install.log`（权限 0640，写不进去时如实说明而不是假装已记录）；结尾横幅与退出码都由记录的阶段状态推导——成功照旧，失败则打印失败原因、修复命令（`sc update-rules` / `sc reload` / `systemctl status sing-box`）和日志路径，并以非 0 退出。
- **规则集自动更新的 systemd 定时任务从来没跑起来过**：`sing-box-rules-update.service` 的 `ExecStart` 一直指向 `/usr/local/bin/proxy`——这个项目从未安装过叫 `proxy` 的可执行文件（安装器装的是 `/usr/local/bin/sc`），所以每周定时器一触发就以 `203/EXEC` 失败，README 里承诺的「规则集自动更新」在任何一台 systemd 机器上、任何一个版本里都没有真正执行过，`systemctl --failed` 里还会一直挂着这个单元。现在该单元执行 `/usr/local/bin/sc update-rules`。**升级方式**：重新跑一遍安装命令（`sudo bash -c "$(curl -fsSL .../install.sh)"`，或从仓库目录执行 `sudo ./install.sh`）即可——安装器会覆盖单元文件并自己执行 `systemctl daemon-reload`，无需手工改单元、无需重启定时器。**升级后会发生什么**：在定时器一直正常触发的机器上不会立刻补跑一次（触发时间戳始终在正常推进，与被触发的服务失败与否无关），第一次真正的自动更新在下一个每周触发点 + 最多 1 小时随机延迟；若定时器此前被停用过、时间戳已经过期，`Persistent=true` 会在安装结束后立刻补跑一次；想立刻跑一次可执行 `sudo systemctl start sing-box-rules-update.service`，注意这条命令只有在规则集内容确实发生变化时才会重启 sing-box（那几秒连接会中断）；内容没变时不会碰服务；残留的 `failed` 状态可用 `sudo systemctl reset-failed sing-box-rules-update.service` 立即清掉，不清也会在下一次成功运行后自动消失。
- **规则集更新不再无谓重启**：以前 `sc update-rules` 只要跑完就重启 sing-box —— 每周定时任务把四个文件重新下载一遍、内容一个字节都没变，也照样重启，把所有连接（包括远程管理自己的 SSH）断掉几秒。现在每次运行会在下载前后各观察一次磁盘上的规则集，只有当某个规则集的内容真的发生变化（按完整内容比对，不看修改时间、不看文件大小、不看「请求成功了」）时才会重启，并在输出里点名是哪几个规则集变了；内容没变就完全不碰服务。规则集从不可用变为可用时，仍然会重新生成配置并（在服务正在运行时）重启，行为与之前一致。每次运行的最后一行都会如实说明这次到底动没动服务。

- **安装器获取 sing-box 版本失败时不再一声不响地中止**：以前 `install.sh` 第 2 步用 `SB_VER=$(curl … | grep … | sed …)` 取最新版本号，而在 `set -euo pipefail` 下这条赋值语句会带上整条管道的退出状态，所以网络不通（DNS/连接/TLS 失败）、GitHub API 返回 403（未认证的速率限制，很常见）/404/5xx、或者返回了 200 但内容里根本没有版本号（公共 WiFi 的登录跳转页、公司代理的拦截页、空响应）时，脚本会**在赋值那一行**直接结束 —— 它自己写在下面几行的「下载失败 / 请检查网络后重试」提示根本没机会执行，结尾的安装结果报告也不会打印。最难受的是第三种：curl 认为自己成功了，于是连它那行英文报错都没有，屏幕上什么都不留，安装就这么没了。现在这条赋值放进 `if` 判断里，「取到的版本号能不能用」由下面那条校验语句唯一决定：以上几种情况一律用你在安装开始时选择的语言打印「✗ 下载失败：GitHub API (sing-box version)」与「请检查网络后重试」，并以状态码 1 退出；临时目录照常清理，`/etc/sing-box/` 下不会留下任何文件（这一步发生在创建配置目录之前），重跑安装命令仍然是推荐的恢复方式。同时管道里的 `head -1` 换成了等价的 `sed -n '1s…p'`：`head` 读够一行就关掉管道，可能让上游的 curl/grep 死于 SIGPIPE，在 `pipefail` 下反而会把一次**成功**的获取误判成失败。成功安装时的行为、下载地址、curl 参数、超时与重试均无任何改动。需要说明的是，这次只修好了版本号获取这一处：`install.sh` 里 `tar` / `install` / `python3` 等命令失败时仍会中止而不作说明，「安装器在任何情况下都会说明结果」目前还不成立。

## [0.1.0] - 2026-05-08

首次发布。

### 功能

- `sc` CLI 命令行工具，包含 `ls / now / use / add / rm / on / off / status / mode / default-tun / sysproxy / update-rules / update-interval / log / reload / lang / uninstall / help`
- 支持的分享链接协议：vless, vmess, trojan, ss, hysteria2, tuic
  - 包括 VLESS Reality + Vision、WebSocket、gRPC、HTTP/2、HTTPUpgrade 等传输层
- systemd 集成，开机自启，TUN 在用户登录前就已建立
- Clash API 即时切节点 / 切路由模式（无需重启服务）
- 规则集自动更新（默认每周，可通过 `sc update-interval` 配置）
- **中英双语 UI**：默认英文，安装时由用户选择，`sc lang en|zh` 随时切换；install.sh / uninstall.sh / sc 全部覆盖
- 一行 curl 一键安装：`sudo bash -c "$(curl -fsSL .../install.sh)"`，无需 git clone
- 一键卸载（`sc uninstall` / `./uninstall.sh` / 一行 curl 三种方式），可选连同 sing-box 与 APT 源一并清掉，零残留

### 系统要求

- Debian / Ubuntu
- Python 3.6+
- systemd
